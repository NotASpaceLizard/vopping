# Backlog

**Scope (per PO, 2026-09-04):** single-user, fully offline, self-contained webapp — used almost
exclusively on a phone, in a grocery store. This supersedes the stale top-of-file framing in
`project-notes.md`, which was written when this was conceived as a multi-user app requiring a
backend (a hard blocker at the time). That requirement is now moot: every multi-user-editing and
shareable-link requirement from `project-notes.md` has been dropped entirely, not deferred. Every
other requirement in that file's Requirements section is intact below as a real story.

**Tech stack decision (locked, 2026-09-04):** plain HTML/CSS/JS, no framework, no build step, no
backend, persistence via `localStorage` — identical architecture to sibling project vacking
(confirmed via vacking's own BACKLOG.md tech-stack decision note). Works entirely via `file://` or
a static host; no network calls of any kind are part of this app's normal operation.

**Row density/interaction — LOCKED, 2026-09-04 (PO's final pick from `density-picker.html`):** vacking's row density (44px
checkboxes, no row-to-row margin, ~52px/row) was too sparse for grocery use in practice — excessive
scrolling, real risk of missing an item while scanning aisles mid-shop. The Orchestrator has built
`density-picker.html` in this project's root: a real interactive side-by-side comparison of 4 row-
density options. **PO's final pick:** the entire row is the tap target for toggling crossed-off
state (Option C), with no checkbox or dot glyph of any kind (Option D) — since the whole row
already toggles on tap, a dedicated glyph isn't needed for the everyday cross-off gesture;
crossed-off visual state is strikethrough + dimmed item-name text only. This is now the real spec
for S1's base row, S2's tap/cross-off behavior, and S5's up/down buttons — see each story's own row
for the full locked detail. Exact CSS/markup still needs a Developer pass and Tester
re-verification before Status can move to Done on any of the three; see the Tracked follow-up note
below.

**Note, 2026-09-08:** this paragraph documents the original PO pick and its Done-gate history for
S1/S2/S5 — it is not being retroactively rewritten. S13 (drag-and-drop reorder, supersedes S5's
Up/Down buttons) and S14 (~25% shrink of the remaining row-control buttons) will further evolve
this row's exact layout going forward, per new PO decisions from the crowded-row review (Tracked
follow-up #2, resolved) — see S13's and S14's own rows, and S5's dated technical note, for the
newer changes.

**Tracked follow-up, required before S1/S2/S5 can move to Done (QA finding M3, 2026-09-04):** the
pick landing in QUESTIONS.md is not by itself sufficient closure. A CSS/markup follow-up pass
applying that pick to S1's base row, S2's tap/cross-off row behavior, and S5's up/down buttons must actually happen
and be Tester-verified before any of those three stories' Status is flipped to Done — this note
exists specifically so that commitment doesn't silently evaporate once attention moves elsewhere,
the exact stale-status-drift shape the playbook's §6 warns about.

**Tracked follow-up #2, required before S7/S8 can move to Done (QA finding R7, 2026-09-08):** S7's
note-toggle and S8's aisle-toggle affordances landed as two more nested per-row controls alongside
S3's delete and S5's Up/Down — a real worst-case row (note set AND aisle set on the same item) now
renders 5 nested controls beside the item name. The PO's original whole-row-tap/no-glyph pick
(Option C/D from `density-picker.html`, see the row-density note above) was made before S7/S8
existed and never showed this density — same "mockup didn't include controls that would later
share the row" failure class already caught once on this project (QA finding R4). Developer's
`flex-wrap` mitigation prevents overflow but does not confirm the PO has actually seen or approved
a row this crowded. Before S7/S8's Status can move to Done, the PO needs a fresh look at a real or
emulated worst-case row (same decision-tool-artifact precedent as the original density pick,
playbook §3/§7) and either confirms it holds up or asks for an adjustment. This note exists so the
commitment doesn't silently evaporate once attention moves elsewhere — same rationale as the
Tracked follow-up above. Does NOT block Developer's ongoing self-verification pass or Tester's
eventual formal pass from proceeding against today's spec in the meantime — same non-blocking
carve-out as the QUESTIONS.md Blocking-table entries above used.

**Gate retargeted, 2026-09-08 (scrum-master's call, no PO input needed for this specific call):** the
PO half of this gate is now answered — they tried the row live, confirmed the crowding is real, and
gave 6 concrete remediation decisions (drafted as S13-S17 + parked S18). That answer was "no, it
doesn't hold up as-is, here's the fix," not "yes, it's fine" — so per this exact same gate's own
precedent (M3 above: "the pick landing... is not by itself sufficient closure... must actually
happen and be Tester-verified"), a remediation *decision* doesn't close this gate any more than M3's
pick-landing did; the gate stays OPEN until S13 (drag-and-drop, removes the Up/Down buttons driving
the crowding) and S14 (25% control shrink) actually ship and Tester verifies the worst-case row is
no longer the crowded one the PO rejected. S7/S8 remain "In Review," not "Done," until then — their
own functional AC is otherwise fully verified (see each row's own Tester-formal-pass citation).

**Update, 2026-09-08 (same day, scrum-master's resume-time doc re-read):** S14 has since been
implemented and Tester-formally-verified (180/180, zero defects — see S14's own row, now Done) —
half of this gate's closing condition is met. **S13 (drag-and-drop) is the sole remaining blocker**:
it is Locked but Developer has not yet started implementing it — this is the main open thread on
the doc-pipeline side right now. Once S13 ships and Tester re-verifies the worst-case row is no
longer crowded, this gate closes and S7/S8 move to Done — no further PO or scrum-master decision is
pending on this gate.

**Update, 2026-09-09 (same day, continued):** S13 has now also shipped and been independently
Tester-formally-verified (216/216, zero defects — see S13's own row, now Done). **This gate is
still NOT closed, though — not yet, on a specific technical gap, not a product question:** S13's and
S14's own formal passes each verify their own story's functional AC (drag mechanic; button-shrink
math) — neither one, nor any other test-plan file, contains a check that directly measures the
actual worst-case row (an item with both a note AND an aisle set, S7+S8 both present) post-S13+S14
and confirms it's no longer the crowded/overflowing row the PO originally rejected. This gate's own
closure text calls for exactly that dedicated verification — same standard the original M3 density
gate already set (a pick/fix "landing" via individually-passing sub-parts was explicitly ruled
insufficient there; a dedicated pass against the real, full row was required). Inferring "S13 removed
2 controls and S14 shrank the rest by 25%, so the row must be fine now" would repeat exactly the kind
of unverified inference this project's own M3/R7 history warns against. **Recommending one cheap,
targeted Tester check** — render the worst-case row under today's shipped code and assert control
count/row height/no horizontal overflow at a mobile viewport width, the same shape of check S1/S2/S5's
own density Done-gate already used — before flipping this gate closed and moving S7/S8 to Done.

**Gate CLOSED, 2026-09-09 (same day, continued):** Tester delivered exactly the dedicated check
recommended above — rendered the actual worst-case row post-S13+S14 and measured both crowding axes
directly (screenshots + DOM data, 17/17 checks), not inference. Axis A (S7's note AND S8's aisle
both set) collapses to item-name + 1 delete icon on line one. Axis B (both empty — the likely real
source of the original "5 controls" figure) is down to 3 icons at 19.5px each (was 5 at 26px). Both
confirmed no longer crowded. **S7, S8 → Done** (see each row's own citation to this evidence, in
`test-plans/S7-notes.md`/`S8-aisle-designation.md`). **Icon-count figures now point-in-time — dated
softening note, 2026-09-09 (does NOT reopen this gate or R7):** the specific per-axis icon counts quoted
just above (Axis A 1, Axis B 3) were measured before S15's always-visible edit-icon shipped (sha
`10c804e`). Because item names are never empty, that edit-icon — unlike the note/aisle toggles, which
render only while their field is empty — is always present, adding one icon to every row and taking
Axis A to 2 and Axis B to 4 (Developer-flagged). Not a regression: what this gate actually turned on —
no horizontal overflow and acceptable row height at a mobile viewport width — still passes clean, and
the PO has since explicitly accepted the reintroduced crowding (S19). Deliberately NOT re-stating a
fresh exact count here — S19 (restores Up/Down, +2 icons) and S20 (frameless glyph-fills restyle) are
both about to change this row's layout again, so a re-count now would just go stale a third time, the
same repeat-drift-at-one-seam the playbook §6 says to fix structurally rather than by re-stating. Read
this gate's conclusion (no longer the crowded/overflowing row the PO rejected) as resting on the
overflow/height verification plus the PO's own acceptance, not on any frozen icon tally. **Deferred
re-measurement now DISCHARGED, 2026-09-09:** S19's formal pass measured the real 6-icon worst-case row
(both fields empty: note-toggle + aisle-toggle + S15 edit + Up + Down + delete — the post-revert
worst case) at all four target widths (320/360/375/390px) and it fits cleanly, zero horizontal overflow,
no wrapping (222/222, Tester's pass) — so this gate's conclusion is now confirmed against real shipped
post-S19/S20 code, not left resting only on the softened earlier evidence. Loop fully closed. **This closure stands independent of S13's own,
separate, temporary Status reversion (see S13's row) — deliberately not held up by it:** QA's C1
finding is a functional drag-interaction crash in a specific cross-row-editor combination, orthogonal
to this gate's own subject (the row's physical layout/markup, which is unaffected by C1 and remains
exactly as measured above). Treating two independently-decidable questions as if one blocked the
other would be a new kind of over-caution this project hasn't practiced elsewhere — cross-story
supersession (S5 staying Done through S13's UI change) already established that unrelated stories'
status doesn't need to move in lockstep.

**QA confirmation, 2026-09-08:** independently verified that `density-picker.html` already covers
both worst-case row axes (empty-field icon-crowding, and populated-field second-line growth) —
confirming the PO's earlier crowded-row review wasn't working from an incomplete mockup. No action
needed; recorded here since it's direct supporting evidence for this gate's own PO-review half.

**Accessibility scope (PO decision, 2026-09-04):** this app has one, maybe two users, none
keyboard-restricted — keyboard-only navigation and screen-reader support are explicitly OUT of
scope, not a real requirement for this project. The one real accessibility need: any UI that uses
color to convey state must use a colorblind-safe palette — default to Okabe-Ito, matching the PO's
already-settled preference from vacking's console work (playbook §7). Not yet relevant to this
app's own UI today (nothing color-codes state as of this backlog), but will matter once S8's aisle
designation lands if aisle groups get any color treatment — see S8's row. Does NOT require
touching what Developer already shipped for S1/S2/S5 (existing ARIA/keyboard code works and isn't
hurting anything, just not something to keep investing in) — this note governs future work, not a
retroactive cleanup of already-done work.

**Latent bookkeeping item, 2026-09-09 (Tester-flagged, non-blocking — NOT a current gap):** the
project's `--accent` CSS token is `#4DA6FF` (a dark-theme blue), not the playbook §7 colorblind-safe
reference `#0072B2`. This is pre-existing — it shipped with S16's accent-colored aisle icon (U+2691)
and passed that formal pass; S20's frameless restyle only preserved the existing binding, it did not
introduce the color. It does NOT currently trigger the colorblind-safety requirement above, because
that requirement is explicitly scoped (by the PO) to "any UI that uses color to convey state" — and
the app deliberately uses a SINGLE accent color decoratively/uniformly, not per-category or per-state
color-coding (S17 explicitly chose no per-aisle colors). A single uniform accent isn't the
state-color-coding the requirement governs. **Action: none now.** Tracked here so that IF per-category
or per-state color-coding is ever introduced (e.g. a future aisle-color feature), `--accent` — and any
sibling state colors added at that time — must at that point be reconciled against the Okabe-Ito
colorblind-safe palette per playbook §7 and this note, rather than shipped on the current dark-blue
value. PO has been told it's noted-for-later, nothing to act on now.

**Status vocabulary (controlled, do not invent new labels):** Not Started / In Progress / In
Review / Locked / Done.

**Undo scope note (applies across S6-S10, stated once here to avoid repeating it on every row):**
per PO decision, Undo is a single-last-action buffer covering exactly four mutation types — add,
check/uncheck, delete, reorder. Note edits (S7) and aisle edits (S8) are deliberately outside that
scope, not an oversight. Sorting (S9) is view-only and never touches the undo buffer at all.
**Extended, 2026-09-08:** S13's drag-and-drop reorder is still the same "reorder" mutation type —
remains undo-eligible, same as S5's swaps were. S15's in-place item-text editing is a working
default of NOT undo-eligible, treated the same as S7/S8's note/aisle edits (open to override, see
QUESTIONS.md — non-blocking). S14, S16, and S17 are all visual/rendering-only changes with no data
mutation of their own — none of them touch the undo buffer.

---

## Priority Queue (Scrum Master draft, 2026-09-04, updated 2026-09-08 — Sprint 1 (S1-S6, S12) fully
Done; **Sprint 2: S7/S8/S9/S10 all Done** (Tester's independent formal pass landed, 154/154, zero defects —
REGRESSION_LOG.md's 2026-09-08/154-total row; S7/S8 additionally cleared the Tracked follow-up #2
crowded-row gate on 2026-09-09 via a dedicated real measurement — 17/17, both crowding axes, screenshots
+ DOM data — not inference; see that note and S7/S8's own rows); **Sprint 3 (S13-S17) drafted
2026-09-08: S13/S14/S16/S17 all → Locked 2026-09-08** (all four clean through Developer sanity-check,
Tester testability-check, and QA's final gate; S13 took 6 iterative rounds of edge-case findings before
QA's Lock-gate re-read found zero remaining contradictions) — **S14/S16/S17 implemented and → Done,
2026-09-08** (Tester's independent formal pass, 180/180, zero defects — REGRESSION_LOG.md's
2026-09-08/180-total row; S16's placeholder-icon note also resolved, PO's hand-picked glyph U+2691
landed via commit `8724733`). **S13 implemented (sha `33deddf`), formally passed 216/216 (REGRESSION_LOG.md's 2026-09-09/216-total
row), then reverted from Done to "In Review" the same day** — QA's adversarial post-Done review found
a real bug (C1: a stale DOM reference in `beginDrag()` goes stale across the cross-row editor commit's
own `render()` call) that the 216/216 pass's own assertions didn't happen to combine; fix landed (sha
`90983e3`) and Tester added dedicated regression coverage for the exact triggering combination
(TC13.11 strengthened + new TC13.27, both editor types), re-verified 219/219 — **S13 → Done again,
2026-09-09** (see S13's own row for the full reinstatement account). **Correction, same day:** QA's
own follow-up trace found C1's original description (thrown exception, permanently unscrollable list)
was inaccurate — the real pre-fix failure was a silent stale-DOM-clone bug, self-resolving the instant
that one drag ended, no persistent broken state; reclassified **R13 (Real, not Critical)** — root
cause and fix are unaffected by this correction. A disclosed iOS Safari long-press-collision fix (sha
`50c57c8`) is separately tracked non-blocking pending a real-device re-test. **S15 → Locked, 2026-09-08**
— R10, the icon-pairing (PO explicitly confirmed the pencil moves to this story's own edit-icon), and
QA's whole-AC re-read (R11/M16/M17, all resolved) all cleared; sequenced after S13/S14 per its own AC —
now **implemented and → Done, 2026-09-09** (Developer sha `10c804e` reused S7/S8's shared editor
infrastructure; Tester's independent formal pass 246/246 — 219 regression baseline + 27 new S15 checks —
zero defects, citation of record). **S18** (collapsible aisle groups) drafted
alongside them but parked like S11, explicit PO stretch goal; S11 remains parked. **New direction
change, 2026-09-09 (2 PO decisions, drafted as S19/S20 — both now **Locked 2026-09-09**, full pipeline
cleared (Developer sanity-check → Tester testability-check → QA gate, zero open findings), clear for
Developer implementation; both sequenced after S15, now Done, so their assumed edit-icon exists) — **both now → Done 2026-09-09** (Tester's formal pass 222/222, zero defects, worst-case 6-icon row fits cleanly at 320/360/375/390px), which completes the entire active Sprint 3 set (S13/S14/S15/S16/S17/S19/S20 all Done; S18 parked):** after a SECOND real-device
drag failure (the drag gesture now loses to native scroll on the PO's phone; the first was the iOS
text-select collision), the PO decided to **abandon drag-and-drop and restore S5's Up/Down buttons
(S19)** — S13 stays Done (it shipped/passed its verified 219/219 AC; real-device was always a tracked
non-blocking signal), and S19 supersedes its live mechanism the mirror-image way S13 superseded S5,
(escape hatch now CLOSED 2026-09-09 — diagnosis came back non-trivial: an iOS touch-action-latched-at-touchstart
scroll-latch; the PO declined the drag-handle alternative and chose Up/Down for good) — and to **restyle all per-row icon buttons frameless with glyphs scaled to fill
their footprint (S20)**, the PO's own mitigation for the crowding S19 reintroduces (which the PO has
explicitly pre-accepted — "i'll take crowded over not working" — so NOT a reopening of the R7
crowded-row gate))

**Sprint 1 — core mutable-list loop (must ship together to be usable at all):**
1. **S1** — render + single-item add + localStorage persistence (foundation everything else builds on).
2. **S2** — check/uncheck.
3. **S3** — delete (distinct from crossing off).
4. **S4** — paste-to-ingest bulk add.
5. **S5** — reorder via up/down buttons.
6. **S6** — single-last-action undo. Sequenced **last** of the original 6 on purpose: it has a hard
   dependency on all four other mutation types (S1/S4's add, S2's check, S3's delete, S5's reorder)
   already existing to reverse.
7. **S12** (new, added 2026-09-04) — bulk "clear crossed-off items," PO-approved resolution to QA
   finding R3. Sequenced after S6 specifically because it's undo-eligible as one atomic action
   (same precedent as S4's paste-batch) — needs the undo buffer to exist first.

**Sprint 2 — organization/enrichment on top of a working core list:**
8. **S7** — text notes per item.
9. **S8** — aisle designation (suggested + custom free text).
10. **S9** — sort view (manual/alphabetical/by-aisle). Sequenced after S8 because by-aisle sort
   needs the aisle field to exist first.
11. **S10** — "What am I missing?" frequency suggestions. Sequenced after S1 and S4 (both of
    sprint 1) because its history counter has to hook into both add paths (single-add and
    paste-ingest) from day one; also meaningfully more useful in practice now that S12 (sprint 1)
    exists to actually clear items between trips (QA finding R3).

**Sprint 3 — row-density remediation + aisle-sort polish (6 concrete PO decisions from the
crowded-row review, 2026-09-08 — all six drafted, none through the doc pipeline yet):**
12. **S13** — replace S5's Up/Down reorder buttons with drag-and-drop, with a deliberate pickup
    delay so scrolling/editing doesn't trigger accidental drags. Supersedes S5's UI mechanism only
    — S5 stays Done, its underlying reorder/persistence logic carries over unchanged (see S5's
    dated technical note). Sequenced first — frees the row space S14 spends.
13. **S14** — shrink the remaining row-control buttons (~25%). No hard technical dependency on
    S13, but the PO paired the two asks together, and finalizing size once against S13's final row
    layout avoids resizing twice.
14. **S15** — in-place item text editing (no more delete+recreate for a fix like "zucchini" →
    "2 zucchini"). Gesture/icon settled via the PO's decision-tool pick (dedicated edit-icon, pencil
    glyph), AC Locked 2026-09-08, and now **implemented → In Review 2026-09-09** (sha `10c804e`,
    Tester's formal pass running) — see S15's own row for the full history.
15. **S16** — suppress the per-row aisle tag when sorting By Aisle (S9 already shows it via the
    group header). Drafted as a follow-on to S9 rather than folded into S9's AC, specifically so
    Tester's just-started S9 formal pass isn't tested against a moving target.
16. **S17** — more visual distinction for aisle group headers when sorting By Aisle. Same
    follow-on-to-S9 rationale as S16; independent of S16, can proceed in parallel with it.

**Sprint 3 continued — real-device drag-failure remediation (2 PO decisions from a 2026-09-09 review,
after drag-and-drop failed a second time on the PO's actual phone):**
17. **S19** — restore S5's Up/Down reorder buttons and remove S13's drag-and-drop, after the drag
    gesture lost to native scroll on the PO's device ("i'll take crowded over not working"). Reverts
    S13's interaction mechanism; S13 stays Done, S19 supersedes its live mechanism the mirror-image
    way S13 superseded S5. Data model unchanged. Carries an escape hatch pending Developer's parallel
    scroll-collision root-cause diagnosis. Sequenced after S15.
18. **S20** — restyle ALL per-row icon controls frameless (drop the square outline, scale each glyph
    to fill the button's footprint) — the PO's own mitigation for the crowding S19 reintroduces,
    bigger-looking/more-tappable targets with no change to footprint or row height. Complementary to
    S14 (does not undo its shrink). Sequenced after S15, pairs with S19.

**Sprint 5 — Auto-Aisle (on-demand automatic aisle assignment that learns; spec `AUTO_AISLE_SPEC.md`, sliced 2026-09-14):** the PO + Orchestrator's researched feature — auto-assign aisles so the user rarely hand-sets one, ON-DEMAND (never silent-on-add), at two scopes (a whole-list button and a per-item picker option), learning from every manual correction so a wrong guess is never repeated. Sliced from the spec's §6 suggested slicing with SM judgment and the Dev/Tester/QA pre-Lock convergence folded into each AC. S28 + S29 → **Done** (2026-09-14, 345/345 @`e0a0f77`, 0 defects) and S30 → **Done** (2026-09-15, 379/379 @`fbc046e`, 0 defects); S31/S32 remain **Locked** — the three gating PO questions are now ANSWERED (2026-09-14, QUESTIONS.md Blocking) and each AC is finalized to the decision: **Q1** bulk-run single-undo = YES (S31), **Q2** clear-to-no-aisle as a learned override = NO / learn non-empty only (S30), **Q3** taxonomy name-drift = RENAME-MIGRATION (S28, option B — NOT the pure-union default). **Pre-Lock gate CLEARED → all five LOCKED, 2026-09-14:** S28/S30/S31/S32 cleared on the fold; S29's last hold — QA Real finding R16 (the runtime dictionary-load path must degrade gracefully, never throw — a PO typo in the hand-edited file must not blank the offline page; hard fail-fast is test-only) — is now closed: the degrade-gracefully fix folded into S29's AC and QA's R16 re-read came back CLEAN. All five Locked, clear for Developer implementation; S28 + S29 have since been built + Tester-verified **Done** (2026-09-14, 345/345 @`e0a0f77`) and S30 built + Tester-verified **Done** (2026-09-15, 379/379 @`fbc046e`); S31/S32 IMPLEMENTED + Tester formal pass 404/404 + function confirmed on the PO's phone (sha `3ac58fb`); their Done was GATED on the S34 icon-picker glyph fix (the ⭍/U+2B4D glyph boxed on iOS) — that gate is now DISCHARGED: S34 shipped and the PO picked ↯ U+21AF on-device for the Auto-aisle button, so **S31/S32 → Done 2026-09-16** (the 404/404 REGRESSION_LOG row + S31/S32 test-plan docs ship with this Done bundle). See Sprint 6 (icon picker) + Sprint 7 (dictionary/taxonomy follow-ups). Consolidated Tester+Dev+QA sharpenings folded into each row (M27-M30, R16, the frozen S31 toast string + when-shown UX, the named test-hook + matcher `{aisle,tier,matchedText}` contract, the S30 override key + M28 drop-not-blank cascade, S32's top-option/no-guard resolution). Dependency chain (taxonomy+migration → dictionary+matcher → override map → the two UI triggers):
- **S28** — expand the seeded taxonomy to the spec's 18 aisles (17 real + `Other`) via a VERSIONED one-time re-seed that unions the new starters into the already-seeded state (editing `AISLE_STARTER_LIST` alone is a SILENT no-op — `migrateAisles()` short-circuits on an existing `state.aisles`), without resurrecting user-deleted starters or re-mapping any `item.aisle`; seeded starter names byte-identical (after `normalize()`) to the dictionary keys. Old↔new name drift resolved (Q3 = rename-migration): `Dairy`→`Dairy & Eggs` and `Household`→`Household & Cleaning` rename + one-time re-tag of their items; `Meat/Seafood` kept as a custom aisle with `Meat`+`Seafood` added as new starters. Foundation of the feature.
- **S29** — graduate `AISLE_DICTIONARY.draft.js` into the app (loaded via `<script>` before `script.js`) + build the matcher: two ordered maps (canonical, alias) + a WHOLE-TOKEN tier, first-hit-wins precedence resolving the ~68 cross-aisle collisions, fail-fast integrity asserts + a standing data-integrity test, a DOM-free test hook. Pure engine, no UI. Depends on S28.
- **S30** — a persisted personal-override map (its own localStorage key, defensive parse) recorded ONLY on hand picks; adds matcher tier 1; S23 rename/delete cascades into it; matcher-assigned aisles get added to `state.aisles`. Clear-to-no-aisle is NOT learned (Q2 = no); non-empty hand picks only. Depends on S29.
- **S31** — the list-wide "Auto-aisle" header button (in `.sort-controls` next to the sort select; ⭍/↯ + label; fill-empty-ONLY; locked toast summary; single-undo confirmed (Q1 = yes): one undoable action snapshotting `{id, prevAisle}` for the touched empty-only items). Depends on S29 + S30. **DONE 2026-09-16** (prior 404/404 @`3ac58fb`; the S34 glyph gate is discharged — the Auto-aisle button now renders ↯ U+21AF).
- **S32** — the per-item "⭍ Auto-detect" option inside the ⌖ aisle `<select>` (structural reserved-value detection à la S24's sentinel; rides the existing R14-safe change→`saveAisle`→`render()` path; MAY overwrite but never records a learning override; NB-6 device gate on the option glyph). Depends on S29 + S30. **DONE 2026-09-16** (prior 404/404 @`3ac58fb`; the S34 glyph gate discharged).
- **Sequence:** S28 → S29 → S30 → (S31, S32).

**Sprint 6 — Settings icon picker (all app icons; spec `ICON_PICKER_SPEC.md`, sliced 2026-09-16):** the recurring iOS-glyph-tofu problem — most recently U+2B4D on S31's list-wide Auto-aisle button + S32's per-item Auto-detect option, and before that a string of "placeholder pending PO pick" one-off guesses (S7/S16/S21/S26) — is fixed DURABLY, not by yet another blind swap (the old pre-agreed U+2B4D→U+21AF fallback is SUPERSEDED by this feature). A panel in the existing S21 Settings menu lists every icon-bearing action with its current glyph + a "change" affordance that opens a candidate-glyph palette; the PO picks on their ACTUAL phone (seeing tofu vs. real render), the choice PERSISTS in localStorage (`vopping-icons-v1`), and it applies EVERYWHERE that icon is drawn via a central registry. Full icon set enumerated from the code (10 glyph-bearing actions — `settings-gear` ⚙, `settings-close` ✕, `auto-aisle-run` ⭍, `note-toggle` 🗋, `name-edit` ✎, `move-up` ▲, `move-down` ▼, `delete-item` ✕, `aisle-marker` ⌖, `auto-detect-option` ⭍; text-only controls like Undo/Clear/Add out of scope — see spec §1). Sliced into a foundation story + a PO-facing UI story. One genuine PO fork logged (Q4, QUESTIONS.md Blocking: treat the S31 button glyph and the S32 picker-wheel-option glyph as ONE shared pickable icon or TWO independent ones). **NB-6:** on-device glyph rendering is the real-device acceptance signal; desktop verifies registry / persistence / apply-everywhere structure only. Do NOT regress S26/S27 row density + icon layout, the R14 render-timing/focus fixes, or the S21 shell / S23 aisle-CRUD settings machinery.
- **S33** — central icon registry (ONE source of truth: `action-id → DEFAULT glyph`, the current glyphs) + an `iconFor()` accessor routing EVERY draw-site through it (the JS-rendered row icons AND the three static `index.html` glyphs) + a persisted override map (`vopping-icons-v1`, R1-defensive parse, override-else-default resolution). NO visible change — with no override stored, every icon renders byte-identically to today; this is the regression-safety foundation. Prerequisite for S34. **Pre-Lock ACs folded 2026-09-16 (spec §8):** R17a escape the glyph at every `iconFor()` innerHTML draw-site (note/name/up/down/delete/aisle-glyph; auto-detect option already escapes); R17b a CONTENT constraint in the parse (string / non-empty-trimmed / length-capped ≤8 / no control-or-markup chars → drop to default); M31 reject a top-level array; M32 parse the map ONCE (not per-icon per-render); `applyStaticIcons()` owns the 3 static glyphs, run at init + on every write, targeting the `#auto-aisle-btn` NESTED aria-hidden span via a new selector hook, never the button textContent (N18/N19); accessible names stay glyph-independent (N20); `iconFor('unknown-id')` returns a defined `FALLBACK_GLYPH` (`□` U+25A1), never throws. **The `window.__voppingIcons` test hook lands WITH S33** (mirrors `window.__voppingAutoAisle`; `iconFor`/`getRegistry`/`getOverrides`/`ICONS_KEY`/`setIcon`/`resetIcon`/`resetAll`/`applyIcons`) so S33 is verifiable in its own formal pass before S34's UI exists — named assertions R-DEFAULT / R-OVERRIDE / R-EVERYWHERE / R-ISOLATION / R-NOTHROW. **LOCKED 2026-09-16 → DONE 2026-09-16 — Tester formal pass 483/483 @8322b59, 0 defects; QA re-read CLEAR (R17a/R17b + R18 @f6e08c3); registry / persistence / apply-everywhere desktop-verified.**
- **S34** — the Settings "Icons" panel: each action lists its label + current glyph + a "change" affordance → a curated candidate palette (PLAIN Unicode, NO emoji, several glyphs known to render broadly on iOS) → a pick applies + persists live, with a reset-to-default. The PO picks the `auto-aisle-run` glyph (and any other they want) ON-DEVICE, resolving the U+2B4D tofu; the shipped picker doubles as the on-device decision tool. Depends on S33. **Pre-Lock ACs folded 2026-09-16 (spec §8):** R18 a candidate tap must NOT collapse the open palette or lose scroll position (persist an open-palette action-id as render state + preserve scroll, OR update the glyph node in place — `renderSettings()`'s full-innerHTML rebuild currently preserves only two inputs); M33 no per-glyph S27 height tuning and NO `overflow:hidden` on the icon boxes (would hide the tofu signal); M34 preview each candidate at its target draw-site font/size; the candidate palette is FROZEN (per-action sets in spec §4) with **⭍ U+2B4D explicitly WHITELISTED** in the non-emoji/avoid guard (deliberate tofu-confirm candidate, like the lemon→'difficult' integrity whitelist); reset scope PINNED to BOTH per-action + reset-all; Dev-3 after any row-icon/aisle-marker glyph change, require an on-device density re-confirm (NB-6; `note-toggle` is the S27 height reference); **N23 (Tester impl note): the non-emoji palette guard keys on EMOJI-PRESENTATION-DEFAULT (`Emoji_Presentation=Yes` / VS16 U+FE0F), NOT mere RGI-emoji membership, so text-default frozen candidates (✳/✷/✦/✧/★) aren't false-failed.** **LOCKED 2026-09-16 → DONE 2026-09-16 — Tester 483/483 @8322b59, 0 defects; PO picked ↯ U+21AF (Auto-aisle) + ☰ U+2630 (note-toggle) via the on-device picker; the 2 defaults were PROMOTED from those picks — auto-aisle-run ↯ replaced ⭍ U+2B4D, note-toggle ☰ replaced astral 🗋 U+1F5CB.**
- **Sequence:** S33 → S34.
- **Pre-Lock review outcome, 2026-09-16:** the standard Dev-feasibility → Tester-testability → QA-hazard review ran; QA returned a HOLD with 2 Real findings (R17, R18) plus minors M31–M34 and nitpicks N18–N20, and the Dev + Tester added sharpenings. ALL folded into the S33/S34 ACs above + `ICON_PICKER_SPEC.md` §8 (the finding-by-finding ledger + the QA re-read checklist). **QA's re-read came back CLEAR — R17a/R17b + R18 confirmed closed at f6e08c3 — with one Tester implementation note N23 (the non-emoji guard keys on emoji-presentation-default, not RGI-membership) folded into S34's AC + spec §4/§8, and the master story-table rows synced to the folded spec (D1). S33 + S34 → Locked, 2026-09-16 — ready to build (S33 foundation first, then S34).** **BUILT + DONE 2026-09-16 — Tester formal pass 483/483 @8322b59, 0 defects; both desktop (structure / persistence / apply-everywhere) AND NB-6 on-device acceptance met — the PO used the shipped picker on their phone, saw the candidates render, and picked ↯ U+21AF (Auto-aisle) + ☰ U+2630 (note-toggle); those 2 picks were promoted to the registry DEFAULTS, retiring the boxing ⭍ U+2B4D and the astral 🗋 U+1F5CB.**

**Sprint 7 — Auto-Aisle dictionary/taxonomy follow-ups (from the PO's on-device use + QA's dictionary audit, sliced 2026-09-16):** two data/taxonomy follow-ons to the shipped Auto-Aisle feature (S28-S32). Neither changes the matcher engine (S29) or the UI (S31/S32). Both edit `aisle-dictionary.js`, so they are SEQUENCED (S35 → S36) to avoid two passes stepping on the same data file even though they are otherwise independent.
- **S35** — dictionary correctness fix pass (data-only, low risk): add the MISSING bare head-word staples the PO's items keep falling through on (`cheese`→Dairy & Eggs; `rice`/`pasta`/`noodles`→Pasta, Rice & Grains; `beef`/`steak`/`sausage`→Meat; `fish`→Seafood; `vinegar`→Condiments & Sauces; `batteries`/`battery`→Household & Cleaning; lower-freq `pork`→Meat, `oil`→Condiments & Sauces, `cream`→Dairy & Eggs, `juice`→Beverages, `vitamins`→Personal Care & Health) as canonical entries, and RE-HOME `chicken` to Meat (add a `chicken` CANONICAL in Meat so bare "chicken" resolves to Meat via canonical-beats-alias, leaving `canned chicken` + its `chicken` alias in Canned & Jarred). Leave the deliberate judgment-calls (peas, tuna, colby / condensed-milk / broth-stock labeling) untouched. Depends on S29 (Done). **Pre-Lock review CLEAR (Dev BUILDABLE / Tester TESTABLE / QA CLEAR to Lock — no hold), sharpenings folded 2026-09-16:** the tier-4 broadening's side-effects are explicitly IN-SCOPE and guarded by regression assertions — compound-token cases `canola oil`→Condiments & Sauces and `chicken parmesan`→Meat (Dev); `tuna steak`→Seafood, `chicken broth`→Canned & Jarred, `fish sticks`→Frozen must each still resolve to their EXISTING aisle at tier-2 canonical-exact (the full phrase beats the new bare head-word); bare `chicken` pinned to tier-2 (→Meat) after the re-home; and for singular/plural pairs (batteries/battery, curries/curry, chutneys/chutney) assert the resolved AISLE only, NOT the tier (a plural may resolve via a different tier than its singular) (Tester). **LOCKED 2026-09-16 → DONE 2026-09-16 — Tester formal pass 441/441 @e549ac2, 0 defects; fully desktop-verifiable (no NB-6 device gate).**
- **S36** — new International aisle (taxonomy 18→19 + the S28-style versioned re-seed migration + dictionary population): add `International` to `AISLE_STARTER_LIST` at a sensible store-walk position, bump `AISLE_SEED_VERSION` 2→3, add a once-only version-gated migration (mirroring `migrateAislesV2`) that UNIONS `International` into `state.aisles` (runs exactly once, N13 no-resurrect, merge-not-duplicate, never drops user aisles / never re-maps item aisles), and populate an `International` dictionary key (tortillas + variants MOVED out of Bakery; taco shells; tostadas; kosher staples matzah/matzo, gefilte fish, Manischewitz; a modest international starter set). Naan/pita/flatbread stay in Bakery; well-placed items (soy sauce, etc.) are NOT aggressively re-homed — candidates flagged to the PO (QUESTIONS.md). **The migration is the data-safety-critical part (same class as the S28 short-circuit blocker) — see the AC's hazard note.** Depends on S28 + S29 (both Done). **PO SCOPE EXPANSION + Pre-Lock review CLEAR, folded 2026-09-16 (Dev BUILDABLE / Tester TESTABLE / QA CLEAR to Lock — no hold). International is now a FULL multi-cuisine aisle.** Populate `International` by MOVING each named existing term out of its current aisle (remove-from-source + add-to-International — a MOVE, never a duplicate; `validateDictionary()` enforces cross-aisle canonical uniqueness) and ADDING the net-new ones: **Mexican/Latin** — tortilla/tortillas/corn tortilla/flour tortilla (MOVE from Bakery), taco shells, tostadas, salsa + salsa verde (MOVE the Condiments & Sauces canonicals), refried beans (MOVE from Canned & Jarred), + enchilada sauce, taco seasoning (net-new). **Asian (broad)** — the soy sauce family incl. low/reduced-sodium, lite, gluten-free (MOVE from Condiments & Sauces), sriracha, hoisin (MOVE), ramen (MOVE from Pasta, Rice & Grains), rice noodles (MOVE), coconut milk (MOVE from Canned & Jarred), sesame oil (MOVE), rice vinegar + its `rice wine vinegar` alias (MOVE from Condiments & Sauces), + nori/seaweed wraps, teriyaki sauce, korean bbq sauce, plum wine vinegar, mirin, miso, fish sauce, oyster sauce, wasabi, curry paste (net-new). **Mediterranean/Middle-Eastern** — tahini (MOVE from Condiments & Sauces), hummus (MOVE from Deli), couscous (MOVE from Pasta, Rice & Grains), + falafel mix, harissa (net-new). **Indian (NEW category)** — curry/curries, chutney/chutneys, butter chicken sauce, tikka masala sauce, korma sauce, curry paste (net-new; prepared sauces/pastes/packaged only), + papadum, ghee (MOVE ghee from Dairy & Eggs — flagged below). **Kosher** — matzah/matzo, gefilte fish, Manischewitz + a modest set. **STAYS put:** naan/pita/flatbread stay in Bakery (PO's earlier call); pure SPICES (garam masala, curry powder, turmeric) stay in Baking & Spices — only prepared sauces/pastes/packaged items go to International. **CRITICAL matcher rule:** any International multi-word term whose individual tokens S35 routes elsewhere — `sesame oil`, `rice wine vinegar`, `rice vinegar`, `plum wine vinegar`, `rice noodles` — MUST be International CANONICALS so they win at tier-2 canonical-exact over S35's new bare `oil`/`vinegar`/`rice`/`noodles`→their aisles (which would otherwise catch them at tier-4). **Migration (Dev CRITICAL fix):** `migrateAislesV2`'s gate AND stamp must BOTH become the LITERAL `2` (never the global constant — half-applying silently reintroduces the V2-re-run/resurrect bug), plus a new `migrateAislesV3` gated `>= 3` / stamping `= 3` whose union set is ONLY `['International']` (never the full starter list); run V2 THEN V3 in order; `AISLE_SEED_VERSION` becomes just the current/highest marker (`3`). **Not a bug — state in AC:** existing users get `International` APPENDED at the tail via the V3 union push; only FRESH installs get the store-walk slot (position between Condiments & Sauces and Snacks & Candy in `AISLE_STARTER_LIST`). **Tester gates:** `assertSeedDictParity()` CLEAN post-S36 — `International` present in BOTH `AISLE_STARTER_LIST` AND as a dictionary key, byte-identical post-normalize (QA N22); a `tortilla chips`→Snacks & Candy (and `nacho cheese tortilla chips`→Snacks & Candy) regression so the tortilla MOVE never over-reaches the `tortilla` substring; and `window.__voppingAutoAisle.SEED_VERSION` now reads `3` (QA N21 — Tester fixtures move 2→3). **LOCKED 2026-09-16 → DONE 2026-09-16 — shipped the versioned V2/V3 migration (International added on the PO's next app-open, N13-safe) + the multi-cuisine dictionary population; Tester formal pass 441/441 @e549ac2, 0 defects; fully desktop-verifiable (International is a data string, not a glyph — no NB-6 device gate).**
- **Sequence:** S35 → S36 (both then run the standard pre-Lock review — Dev feasibility → Tester testability → QA hazard — before Lock + build; S36's migration is the QA data-safety focus).
- **Pre-Lock review outcome + Lock, 2026-09-16:** the review returned Dev BUILDABLE, Tester TESTABLE, QA CLEAR to Lock (no hold — the folded items are AC-tightenings, not new hazards). All Dev/Tester/QA sharpenings + the PO's International multi-cuisine scope expansion are folded into the S35/S36 rows above (mirrored in SPRINT_LOG). **S35 + S36 → Locked.** Borderline moves FLAGGED to the Orchestrator/PO (executed as specified above, but called out): (1) `ghee` moves OUT of Dairy & Eggs — it is a dairy-adjacent staple; (2) `hummus` moves out of Deli, but its flavored variants (`garlic hummus`, `roasted red pepper hummus`) are LEFT in place by default — moving them along is optional; (3) `salsa` exists in TWO aisles today (the Condiments & Sauces `salsa`/`salsa verde` canonicals, MOVED; and Deli `fresh salsa`/`salsa fresca` aliases, LEFT in Deli); (4) bare `curry`/`curries`→International means `chicken curry` resolves to Meat via the tier-4 leftmost `chicken` token while `curry powder` correctly stays Baking & Spices at tier-2 — flagged as expected behavior, not a defect.

**Sprint 8 — field-test fixes (3 stories from the PO's real-store field test, greenlit to build; sliced 2026-09-17):** three fixes the PO hit while shopping — the sort view not surviving a refresh, two competing add controls, and a fixed aisle order that doesn't match a real store walk. Sliced from the "Inbox — field-test feedback (2026-09-17)" items (FT1/FT2/FT4, with FT3 folded into S39) after the PO greenlit building them; ACs grounded in the real code (script.js sort/add/aisle paths + index.html add controls, read this session). **Pre-Lock review DONE 2026-09-17 — Developer feasibility / Tester testability / QA hazard all in and consistent (S37 clean; S38 one Real, R-S38-1; S39 three Real, R-S39-1/-2/-3, + a pinned migration constraint); every sharpening folded into the S37-S39 ACs below → all three LOCKED 2026-09-17, then BUILT + all three → DONE 2026-09-17 (app code live @3ed4100). Tester formal pass 516/516 @3ed4100, 0 defects; QA don't-regress CLEAR; PO confirmed on-device (S38 unified add box + S39 aisle-header reorder — the NB-6 gates — working; S37 sort-persistence desktop-complete). Sprint 8 (field-test fixes) COMPLETE — S37/S38/S39 all Done.** Built in the sequenced order below; none of the three depend on each other, so the build order was smallest/lowest-risk first (de-risk), not a hard dependency chain:
- **S37** (from FT1, SMALL, rank 1 — quick win) — persist the selected sort mode (Manual / Alphabetical / By Aisle, S9) to its own localStorage record and restore it on load, so the chosen view survives a refresh instead of resetting to Manual. Strictly view-only — must NOT rewrite stored item order (S9's `getSortedItems()` slice-copy invariant). Defensive parse (unknown value → Manual). No hard dependency; sits on S9 (Done).
- **S38** (from FT2, MEDIUM, rank 2) — consolidate the two add controls (S1's always-visible single-add field + S4's separate collapsible "Paste a list" panel) into ONE field that accepts one-or-many: type + Enter adds one; a multi-line paste adds many via S4's existing `parsePasteLines()` ingest (leading-marker strip, blank-line skip, no de-dup) as ONE undo batch (S6). Removes the paste panel, keeps the primary add field; the exact element / paste-handling (a single-line `<input>` can't retain pasted newlines) is a flagged Dev/PO build-time call. Sits on S1/S4/S6/S10 (all Done).
- **S39** (from FT4, ABSORBS FT3, LARGE, rank 3) — up/down arrows ON the By-Aisle group headers (in-context, NOT in Settings — the PO's "save a click when I'm in the middle of the grocery store") that move a whole aisle within a PERSISTED aisle order, which drives BOTH the list's by-aisle grouping order AND the per-item aisle-picker `<select>` option order (the latter resolves FT3's "the fixed store-walk dropdown order is unhelpful"). Makes `state.aisles` order authoritative for grouping too (today grouping is alphabetical; the dropdown already follows `state.aisles`). Reuses the EXISTING move-up/down registry glyphs (U+25B2 / U+25BC, S19 + the S33 icon registry) — no new glyphs. Edge cases (first/last aisle disable, no-aisle bucket pinned last, newly-created aisle appends, dangling-value R15 fallback) + non-regression (S16/S17/S26/S28-S36, the S29 iOS re-open fix, R14) folded into the AC. **FT3 seed-order sub-decision flagged to the PO (non-blocking): keep the current store-walk seed (SM recommendation — it is now user-fixable) or switch the seed to alphabetical.** Sits on S9/S16/S17/S22/S26/S28 (all Done).
- **Sequence:** S37 → S38 → S39 (independent of each other; smallest/lowest-risk first). All three cleared the pre-Lock review, Locked, built, and → Done in this order (2026-09-17); Sprint 8 complete.
- **PO-vetoable defaults folded as the working default (logged non-blocking in QUESTIONS.md; the PO may veto pre-build):** (1) **FT3 seed order** — KEEP the store-walk seed (`AISLE_STARTER_LIST`), now user-fixable via S39; (2) **S38 element** — a small `<textarea>` whose full value routes through S4's `parsePasteLines`/`addPastedItems` (plain Enter submits, Shift+Enter = literal newline), which means a typed leading list-marker is now stripped like a paste; (3) **S39 move-semantics** — an Up/Down tap repositions the aisle relative to its nearest VISIBLE (item-bearing) neighbor so every tap visibly moves the group (empty, headerless aisles can't be positioned in-context — a logged known limitation).

**Unscheduled / parked (explicitly not in any sprint):**
- **S11** — recipe-paste alternate ingest mode. Low-priority future stretch only, per explicit PO
  direction not to build this now. Revisit only if asked.
- **S18** (new, drafted 2026-09-08 alongside Sprint 3) — collapsible aisle groups when sorted By
  Aisle. Explicit PO-designated stretch goal ("that can be a stretch goal"), same treatment as S11
  — parked, not built now. Revisit only if asked, likely after S9/S16/S17 have been lived with for
  a while.

**Inbox — field-test feedback (2026-09-17):** captured so they aren't forgotten. **SLICED into
Sprint 8 — field-test fixes (2026-09-17):** the four field-test items were greenlit by the PO and
sliced into sequenced, AC-bearing stories (see the Sprint 8 narrative above + the S37-S39 rows in
the story table) — **FT1 (aisle sort doesn't survive refresh) → S37**; **FT2 (redundant add paths)
→ S38**; **FT4 (reorder entire aisles up/down) → S39, which ABSORBS FT3 (default aisle-dropdown
order is unhelpful)** — the persisted aisle order S39 introduces drives the dropdown option order
too, which is FT3's fix. Original verbatim intent is preserved in git history (the commit that first
captured these). One inbox item remains UNSIZED / not sequenced (a separate epic, not a field-test
fix):
- **Multi-user sync (2-phone shared list)** — epic; spike-first; see `MULTI_USER_SYNC_PLAN.md`.

---

| # | Story (As a... I want... so that...) | Acceptance Criteria | Status | Sprint Assigned |
|---|----------------------------------------|----------------------|--------|------------------|
| S1 | As a user, I want to see my grocery list and add items to it one at a time, so that I can build up what I need to buy and have it survive closing and reopening the app. | On first load with no saved data, the list renders an empty state with a clear "no items yet" message (not a blank screen); an always-visible text input + Add control lets the user type one item name and add it as a new row at the bottom, Enter key also submits; submitting empty/whitespace-only input is a no-op, no blank row created; new items appear immediately with no page reload; the full item list (names, order, checked state, and any per-item fields later stories add) persists to localStorage on every mutation and an ordinary refresh restores the exact prior list in the exact prior order (a full "clear site data" wipe is an accepted, non-blocking exception per PO, not a bug to design around); single-user/fully-offline — no network calls, no backend, no multi-device sync of any kind; no de-duplication against existing items — adding the same name twice (e.g. "milk" typed again) is allowed and creates a second, independent row, same policy S4 already states explicitly for paste-ingest, now stated explicitly for this story too rather than left to be inferred (QA finding M5); **defensive load-time guard (QA finding R1, sourced from vacking's own already-shipped S30/S31 crash — same storage architecture, same risk):** this story's persisted data is stored as one object — `{ items: [...], nextId: N }` — under a single localStorage key, not a bare array directly (Developer sanity-check finding, 2026-09-04: storing the array directly at the top level would make R1's own "is this a plain object" shape-check incorrectly flag valid data as corrupted, since arrays fail that check); before using any parsed value, verify the top-level parsed result is a plain object AND that its `items` field specifically is an array — `JSON.parse` succeeding but returning `null`, a string, a number, a bare array at the top level, or an object whose `items` isn't an array, are all treated as corrupted, not as valid empty data; on any parse failure or shape mismatch, fall back to `{ items: [], nextId: 0 }` as if no data existed, and never let a corrupted read throw before the render loop runs.<br>**Row density/interaction — LOCKED, 2026-09-04 (PO's final pick from `density-picker.html`):** the entire row is the tap target for toggling crossed-off state (Option C) — no checkbox or dot glyph of any kind (Option D); since the whole row already toggles on tap, a dedicated glyph isn't needed for the everyday cross-off gesture. Crossed-off visual state (see S2) is strikethrough + dimmed item-name text only. **Per QA finding M3: this story's Status may not be marked Done until a CSS/markup pass implementing this locked spec has actually been applied and Tester-verified — the pick landing alone is still not sufficient, same gate as before, now pointed at a locked target instead of a TBD one.** Tester's formal pass already ran against the pre-lock placeholder layout (part of the combined 69/69 Sprint 1 run, 2026-09-04 — see REGRESSION_LOG.md's current/second row, not the superseded 67/67 one) and found zero functional defects in add/render/persist themselves; only the row's visual/interaction spec needed finishing, which this update now provides. Route to Developer for the CSS/markup pass, then Tester re-verifies against this locked spec before Status can move to Done. **Done, 2026-09-04 — Tester's density-picker.html CSS/markup Done-gate re-verification: 74/74 assertions passed, zero defects, zero regressions across all of Sprint 1 (cited to REGRESSION_LOG.md's latest/current row, script `vopping-tests-tester-s1-s2-s5-density-formal.js`).** | Done | 1 |
| S2 | As a user, I want to check an item off, so that I can mark it as already in my cart without deleting it from the list. | Each row has a tap/click target that toggles crossed-off status. **Row density/interaction — LOCKED, 2026-09-04 (PO's final pick):** the tap target is the ENTIRE row, not a separate checkbox — there is no checkbox or dot glyph of any kind; crossed-off rows are shown via strikethrough + dimmed item-name text only, nothing else. **Nested-control precedence (required for S3's delete button and S5's Up/Down buttons to stay usable once the whole row is tappable):** the row's own cross-off toggle fires ONLY when a tap doesn't land on a more specific interactive control already inside the row (delete, up/down, and later note/aisle-edit affordances) — those controls' own taps perform only their own action and must NOT also toggle the row's crossed-off state. Crossed-off items stay in the list, not hidden or removed; toggling updates localStorage immediately, no reload; crossing off/on does NOT change row position — no auto-move-to-bottom-when-crossed-off behavior (decided directly per the project's "no surprise mutations" guardrail; flagged non-blocking in QUESTIONS.md since this is a real in-store UX call worth a PO gut-check, not just an implementation detail); this action is one of the four types S6's single-last-action undo must cover once S6 ships (dependency note only — this story stands and works on its own before S6 exists). **Per QA finding M3: this story's Status may not be marked Done until the CSS/markup pass implementing the locked spec above has been applied and Tester-verified — see top-of-file Tracked follow-up note.** Tester's formal pass already ran against the pre-lock placeholder layout (part of the combined 69/69 Sprint 1 run, 2026-09-04 — see REGRESSION_LOG.md's current/second row, not the superseded 67/67 one) and found zero functional defects in the toggle mechanic itself; only the visual/interaction spec needed finishing, which this update now provides. **Done, 2026-09-04 — Tester's density-picker.html CSS/markup Done-gate re-verification: 74/74 assertions passed, zero defects, zero regressions across all of Sprint 1 (cited to REGRESSION_LOG.md's latest/current row, script `vopping-tests-tester-s1-s2-s5-density-formal.js`).** | Done | 1 |
| S3 | As a user, I want to permanently remove an item from the list, so that I can get rid of something added by mistake or no longer needed, distinct from just crossing it off. | Each row has a delete control that is a distinct, separately-tappable element within the row — per the locked row-density spec (S1/S2), the whole row is itself a tap target for crossing off, so the delete control's tap must be captured before it reaches the row's cross-off handler (nested-control precedence, see S2): tapping delete performs ONLY delete, never also crosses the item off; deleting removes the item entirely from the data model and localStorage immediately, no reload; no confirmation dialog on individual delete (originally justified as "no bulk/destructive action exists to reserve a confirm for" — now stale since S12's bulk clear-crossed-off-items exists, added 2026-09-04; S12 also deliberately has no confirm, mitigated by undo instead, so this decision doesn't need reconsidering on "no bulk case" grounds, just re-justified on the same undo-based-mitigation logic S12 uses); deleting is one of the four action types S6's undo must cover — the delete must retain enough state (name, crossed-off state, note/aisle fields once S7/S8 ship, and original position/index) to fully restore the row on undo, not just re-append it at the end losing its old position (this requirement is not independently verifiable until S6 itself ships — same forward-reference relationship S6 already has to S7/S8's note/aisle fields, flagged here per Tester's testability-check so the dependency is visible in BACKLOG.md itself, not only in Tester's test-plan file). **Resolved, 2026-09-04 — PO picked Option A for QA finding R2's cross-story tension (this no-confirm-on-delete decision vs. S6's fragile single-slot undo):** deleting an item shows a brief toast/snackbar at the moment of deletion — e.g. "Deleted 'Milk' — Undo" — visible for a few seconds, in addition to (not instead of) the existing always-visible Undo control; this does NOT reintroduce a blocking confirmation dialog (the no-confirm decision above stands unchanged) and does NOT change S6's undo-buffer mechanics — the toast is purely a UI surfacing of the delete-and-recoverable moment, made more visible right when it matters most, not a new data-model or undo-eligibility rule. Exact toast wording/duration is a Developer-level detail, not locked here. **Done, 2026-09-04 — Tester's independent formal pass: 67/67 assertions passed (Sprint 1's first formal pass, script `vopping-tests-tester-s1-s6-s12-formal.js`), zero defects for this story specifically. Citation corrected 2026-09-08 (scrum-master doc re-read; same citation-drift class already fixed on S1/S2/S5 on 2026-09-04 but missed on this row until now): the 67/67 figure is a superseded row per REGRESSION_LOG.md — current canonical citation is its 74/74 row (script `vopping-tests-tester-s1-s2-s5-density-formal.js`), whose full functional regression re-run explicitly re-verified delete with zero regressions.** | Done | 1 |
| S4 | As a user, I want to paste freeform text (from my Notes app, or an ingredient list copied off a recipe site) into a box and have it add one item per line, so that I don't have to type each item individually. | A paste/ingest textarea + Add control, separate from S1's single-item input; splitting rule is intentionally simple: split on newlines, each non-blank line has any leading list-marker glyph stripped first — a fixed, small set of copy-paste formatting-noise patterns only (a single "-", "*", or "•" character, or a leading number+"."/")" like "1." or "2)"), each followed by whitespace before the real item text — then the remaining text (leading/trailing whitespace trimmed) becomes exactly one new item added verbatim: e.g. a pasted line "- Milk" or "1. Eggs" becomes "Milk"/"Eggs", not left as "- Milk"/"1. Eggs" (resolves QA finding R5, PO-confirmed 2026-09-04 via Orchestrator: stripping formatting noise only, not real ingredient parsing); beyond that one fixed strip, no quantity/unit stripping, no aisle-guessing, no de-duplication against existing items; blank lines are skipped, no empty rows created; if the entire pasted input is blank/whitespace-only (zero non-blank lines), this is a no-op — same precedent as S1's existing empty/whitespace-only single-item submit — the textarea is left exactly as typed and nothing is added, no new rows created; new items append to the end of the existing list, preserving the top-to-bottom order they appeared in the pasted text; the textarea clears after a successful ingest so the user can see the result land on the list below; the whole paste operation counts as ONE action for S6's undo — undoing after pasting 8 lines removes all 8 in a single step, not one at a time (this requirement, like S3's undo-state-retention clause, is not independently verifiable until S6 itself ships — flagged here per Tester's testability-check so the dependency is visible in BACKLOG.md itself, not only in Tester's test-plan file); messier recipe-text parsing (quantity/unit stripping, a separate "recipe mode" toggle) is explicitly OUT of scope — see S11 (parked future story), not built now. **Dated technical note, 2026-09-04 (Tester's formal pass, disclosed defect — see test-plans/S4-paste-ingest.md Findings):** a line that reduces to fully empty AFTER marker-stripping (e.g. "- " or "1. " with nothing else on the line) must be treated exactly the same as an originally-blank line — skipped, not added as a junk item. This was underspecified before (blank-line-skipping and marker-stripping were stated as separate clauses without addressing their interaction); testing found a real defect where the current implementation's blank-check runs on the raw line BEFORE marker-stripping, so a marker-only line's trailing whitespace is consumed by that early check and the bare marker character is then pushed as a real item, unstripped. **Decided directly (scrum-master's call, no PO input needed): fix now, not backlogged** — narrow, well-understood, and cheap (Tester's own proposed fix: run the blank/empty check on the POST-marker-strip result, not the raw pre-strip line). **Resolved, 2026-09-04:** Developer's fix landed (marker-regex/blank-check ordering corrected) and Tester re-verified — **Done, 2026-09-04, cited to REGRESSION_LOG.md's second 2026-09-04 row (the canonical one, supersedes the first): 69/69 assertions passed** (67 original + 2 new TC4.3 fix-verification checks, zero regressions in the other 67), same script `vopping-tests-tester-s1-s6-s12-formal.js`, updated in place and re-run in full. No density-CSS gate applies to this story (that's S1/S2/S5 only). | Done | 1 |
| S5 | As a user, I want to move an item up or down using buttons, so that I can manually arrange my list (e.g. to roughly match my store's layout) without relying on drag-and-drop. | Each row has an Up and a Down button (PO explicitly chose buttons over drag-and-drop, citing vacking's mobile-reliability precedent — drag-and-drop was never built there, and buttons are far more reliable on a phone); clicking Up/Down swaps the item with its immediate neighbor in that direction; the top row's Up button and the bottom row's Down button are disabled/no-op (nothing to swap with); a swap updates the persisted array order immediately (localStorage), survives refresh; each single swap is one of the four action types S6's undo must cover; button sizing must fit within the now-LOCKED row height/spacing from `density-picker.html` (see S1/S2's rows for the full locked spec — whole-row tap target, no checkbox/dot glyph, strikethrough+dim crossed-off state) — the swap mechanic itself doesn't depend on the visual details. **Nested-control precedence (see S2):** tapping an Up or Down button performs ONLY that swap and must NOT also trigger the row's whole-row cross-off toggle — same requirement as S3's delete button. **Per QA finding M3: this story's Status may not be marked Done until the corresponding CSS follow-up pass has been applied and Tester-verified — see top-of-file Tracked follow-up note.** Tester's formal pass already ran against the pre-lock placeholder layout (part of the combined 69/69 Sprint 1 run, 2026-09-04 — see REGRESSION_LOG.md's current/second row, not the superseded 67/67 one) and found zero functional defects in the swap mechanic itself; only the visual/interaction spec needed finishing, which this update now provides. **Done, 2026-09-04 — Tester's density-picker.html CSS/markup Done-gate re-verification: 74/74 assertions passed, zero defects, zero regressions across all of Sprint 1 (cited to REGRESSION_LOG.md's latest/current row, script `vopping-tests-tester-s1-s2-s5-density-formal.js`).** **Superseded-UI note, 2026-09-08 (PO decision, S13 drafted — cross-story supersession per playbook, NOT a reopening):** the Up/Down-button reorder mechanism specified and shipped here is being replaced by S13's drag-and-drop reorder, per the PO's explicit crowded-row-review feedback ("we just don't have the space for those buttons"). This story stays Done — it shipped and passed exactly as specified for its time. Once S13 ships, this row's Up/Down-button detail is historical, not current; see S13's own row for the new mechanism (S13's underlying data-model/persistence logic is unchanged from what's specified here). **Closed, 2026-09-09:** S13 shipped (sha `33deddf`) and this story's own 7 test cases (which directly probed the now-physically-removed `[data-role="up"/"down"]` buttons) are retired accordingly — see `test-plans/S5-reorder-buttons.md`'s dated RETIRED note for the full accounting; this story's own DONE verdict above is unaffected and stands unchanged, per this project's transparency convention (nothing here is silently rewritten, the underlying guarantees were re-verified via S13's own test cases instead, cited on that note). **Disclosed interim side-effect, 2026-09-08 (Developer, not hidden — see S14's own row):** while implementing S14 (25% control shrink), Developer found these Up/Down buttons share S14's targeted `.icon-btn` CSS class and weren't named in S14's AC (which was drafted assuming S13 would land and remove them first — order flipped in practice). Rather than splitting a separate CSS class just to hold them at the old size for what's expected to be only a few days until S13 ships and deletes them outright, Developer let them shrink too as a side effect, disclosed via an inline CSS comment. Purely cosmetic, purely temporary — no functional change, and moot the moment S13 ships. Accepted as-is, no AC change needed to this already-Done story beyond this disclosure note. **Mechanism restored (un-supersession), pending — 2026-09-09:** the PO has decided to abandon S13's drag-and-drop and return to this story's Up/Down button mechanism after a second real-device drag failure (see S13's row); drafted as new story **S19**. This is cross-story un-supersession — the mirror image of this row's own 2026-09-08 supersession note above — this story stays **Done** (its data model and reorder/persistence logic never changed through either direction and always carried forward), and S19 restores the Up/Down UI that S13 physically removed. This story's 7 test cases retired when S13 shipped (see `test-plans/S5-reorder-buttons.md`'s dated RETIRED note) become behaviorally relevant again under S19 — Tester's call how to structure (S19 will get its own test plan; the underlying Up/Down guarantees are unchanged from what this row originally specified). | Done | 1 |
| S6 | As a user, I want to undo my most recent action, so that I can quickly recover from an accidental check, delete, add, or reorder without a full history stack. | A single, always-visible Undo control reverses only the single most recent mutating action: add (a single item via S1, or a whole paste-ingest batch via S4 — treated as ONE atomic action), check/uncheck (S2), delete (S3), or one up/down swap (S5); the undo buffer holds exactly one slot — performing any new mutating action overwrites it, discarding the ability to undo whatever came before; undo itself is not further undoable — pressing Undo clears the buffer and the control disables/no-ops until a new mutating action creates a fresh target (no redo); undo restores complete prior state for the reversed action (e.g. undoing a delete restores the item's name, checked state, and note/aisle fields once S7/S8 exist, at its original position — not re-appended at the end); the undo buffer is transient/in-memory only and does NOT persist across a page reload (decided directly — scoped deliberately small, avoids the added complexity of reasoning about a stale undo target surviving a reload); S9's view-only sort is explicitly NOT a mutating action and never populates or clears the undo buffer; **resolving QA finding M1:** S7's note edits and S8's aisle edits are likewise NOT mutating actions for this buffer's purposes in either direction — they neither create a new undo target (unsurprising, they're not undo-eligible) NOR clobber/clear whatever undo-eligible action is already pending (e.g. checking off item A, then editing a note on item B, still leaves A's check/uncheck undoable afterward) — only the four undo-eligible types themselves (add/check/delete/reorder) ever touch this buffer; depends on S1-S5 all existing first (sequenced last in sprint 1 for exactly that reason). **Resolved, 2026-09-04 — PO picked Option A for QA finding R2 (this buffer's single-slot fragility vs. S3 having no delete confirmation):** this story's single-slot undo-buffer rule stays exactly as specified above, unchanged — the resolution is entirely on S3's side (a delete-moment toast/snackbar surfacing the existing Undo control more visibly, see S3's row), not a change to this buffer's mechanics, eligibility, or clobber behavior. **Done, 2026-09-04 — Tester's independent formal pass: 67/67 assertions passed (Sprint 1's first formal pass, script `vopping-tests-tester-s1-s6-s12-formal.js`), zero defects for this story specifically. Citation corrected 2026-09-08 (scrum-master doc re-read; same citation-drift class already fixed on S1/S2/S5 on 2026-09-04 but missed on this row until now): the 67/67 figure is a superseded row per REGRESSION_LOG.md — current canonical citation is its 74/74 row (script `vopping-tests-tester-s1-s2-s5-density-formal.js`), whose full functional regression re-run explicitly re-verified undo with zero regressions.** | Done | 1 |
| S12 | As a user, I want to clear all crossed-off items off my list in one tap, so that I can quickly reset for a new shopping trip without deleting each crossed-off item one at a time. | A single "Clear crossed-off items" control (a page-level control, not per-row) removes every item currently crossed off (S2) from the live list in one action; items that aren't crossed off are completely unaffected, unchanged in content or position; if zero items are currently crossed off, this is a no-op (control may be disabled/greyed when nothing to clear, or simply do nothing when tapped); no blocking confirmation dialog — decided directly, consistent with the PO's stated preference for a fast in-store workflow and mirroring S3's own no-confirm-on-individual-delete precedent — the safety net is undo (below), not added friction on this common, deliberate "I'm done shopping" gesture; **undo-eligible as ONE atomic action for S6's undo buffer** — clearing N crossed-off items and pressing Undo restores all N at once, to their exact prior position and crossed-off state, not one at a time, extending the same atomic-batch precedent S4's paste-ingest already established, rather than excluding this action from undo the way S7/S8's note/aisle edits are (this action's risk profile is far closer to S3's delete — one of the four PO-named undo-eligible types — than to a low-stakes note/aisle edit, so undo-eligible is the right default here, decided directly per Orchestrator's delegated call); this bulk-undo requirement is not independently verifiable until S6 itself ships, same forward-reference relationship as S3's/S4's own existing hedges; clearing does NOT change any cleared item's historical count in S10's separate frequency counter, in either direction — same "counter never decrements on removal" rule S10 already applies to S3's individual delete. **Forward-reference hedge closed, 2026-09-08:** independently verified by Tester's S7-S10 formal pass (TC10.5, before/after counter values compared directly, not hardcoded) — see REGRESSION_LOG.md's 2026-09-08/154-total row. Persists to localStorage immediately, no reload required. **Decided directly (scrum-master's call, per Developer's flagged question, 2026-09-04):** this action also shows a brief toast/snackbar at the moment it fires — e.g. "Cleared 12 items — Undo" — additive to the existing always-visible Undo control, same pattern as S3's delete-moment toast (R2), but for a different reason: Developer correctly noted S3's toast exists because a single delete can be accidental (wrong-row mis-tap), whereas this is a deliberate, single, well-understood "I'm done shopping" gesture with no comparable wrong-target risk, so S3's accident-prevention rationale doesn't carry over as-is. The toast is kept anyway because this action can remove many items at once — showing the actual count cleared gives useful feedback on the scope of what just happened, which a silent bulk removal wouldn't, at zero added friction (no blocking dialog, no extra tap) and keeps the recovery-affordance pattern consistent across every list-scrubbing action in the app rather than only some of them. **New story, added 2026-09-04 — PO-approved resolution to QA finding R3** (the missing bulk-removal mechanism R3 flagged as undermining S10's entire "items must actually leave the list between trips" premise). Sequenced after S2 (needs check state) and S6 (needs the undo buffer for the bulk-undo requirement above) — placed last in Sprint 1. **Done, 2026-09-04 — Tester's independent formal pass: 67/67 assertions passed (Sprint 1's first formal pass, script `vopping-tests-tester-s1-s6-s12-formal.js`), zero defects for this story specifically. Citation corrected 2026-09-08 (scrum-master doc re-read; same citation-drift class already fixed on S1/S2/S5 on 2026-09-04 but missed on this row until now): the 67/67 figure is a superseded row per REGRESSION_LOG.md — current canonical citation is its 74/74 row (script `vopping-tests-tester-s1-s2-s5-density-formal.js`), whose full functional regression re-run explicitly re-verified clear-crossed-off-items with zero regressions.** | Done | 1 |
| S7 | As a user, I want to attach a short free-text note to an item (e.g. "half gallon", "red"), so that I can capture size/color/brand detail without cluttering the item name itself. | Each row supports an optional note field; a small per-row affordance (e.g. tap a note icon) reveals a text input, typing and confirming (blur or Enter) saves it; when non-empty, the note displays alongside/below the item name; when empty, no clutter is shown by default (a discreet "add note" affordance only, not an always-visible empty field); note persists via localStorage alongside the item, survives refresh; no enforced hard character limit — layout must not break with a long note (wraps within the row, never overflows/clips unreadably); editing or clearing a note is explicitly NOT covered by S6's undo buffer (undo is scoped to check/uncheck, delete, add, reorder only, per PO decision) — an accidental note edit/clear is not recoverable via Undo, this is a deliberate scope boundary, not an oversight — and per QA finding M1 (resolved on S6's row), editing a note also does NOT clear/clobber whatever undo-eligible action was already pending from S1/S2/S3/S5, since note edits aren't mutating actions for the undo buffer's purposes in either direction; a whitespace-only note trims to empty on save and reverts to the discreet "add note" affordance, same precedent as S1's empty/whitespace-only item-name handling (QA finding M6). **Developer sanity-check finding, 2026-09-04 (real landmine, same treatment as S1's storage-shape note):** this is the app's first multi-keystroke, in-progress UI state — an in-progress note edit must survive a re-render triggered by an unrelated action (Undo, another row's reorder, a new add), with its draft text intact, not silently discarded. Same capture-before-rebuild/restore-after pattern as S1/S2/S5's focus-preservation fix. **Sprint-2 sanity-check additions, 2026-09-04 (scrum-master):** (1) the note-edit affordance is a nested control per S2's precedence rule, which already names "note/aisle-edit affordances" by name — tapping it fires ONLY the note-edit action, never also the row's whole-row cross-off toggle; (2) S1/S2's "crossed-off visual state is strikethrough + dimmed text only, nothing else" describes the cross-off INDICATOR specifically (no checkbox/dot/icon), not a ban on other row content — this note is additive content within the row, not in tension with that phrase; (3) a row with a non-empty note may grow to a second line to fit it; a row with no note stays exactly as tall as S1/S2's locked single-line spec — content-driven, not a base-row redesign. **Implementation status, 2026-09-08:** Developer live-verified this story via Playwright (not just code trace) and found/fixed 2 real bugs in the shared S7/S8 inline-editor code — focus loss on opening the editor, and a focusout-triggered re-render that could swallow a pending click elsewhere in the list; both are exactly the class of risk the multi-keystroke/re-render-survival guarantee above exists to cover, now confirmed fixed and verified live, not just self-checked by code trace. **Tester's independent formal pass landed, 2026-09-08: 154/154 assertions passed (TC7.1-17, 23 checks for this story specifically, including 3 added to close out the 2 bugs above with dedicated regression coverage), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/154-total row (script `vopping-tests-tester-s7-s10-formal.js`), current canonical citation.** This story's own functional AC is now fully verified — **Gate closed, 2026-09-09 — Tracked follow-up #2 (top-of-file) is now satisfied with real measurement, not inference:** Tester rendered the actual worst-case row post-S13+S14 and measured both crowding axes directly (screenshots + DOM data, 17/17 checks) rather than assuming which was worse — Axis A (this story's note AND S8's aisle both set) now collapses to item-name + 1 delete icon on line one; Axis B (both empty, the likely real source of the original "5 controls" figure) is down to 3 icons at 19.5px each (was 5 at 26px). Both confirmed no longer crowded. Full evidence in this story's own `test-plans/S7-notes.md`. **Icon counts now point-in-time, 2026-09-09:** the per-axis figures above (Axis A 1 / Axis B 3) predate S15's always-visible edit-icon (sha `10c804e`) — since item names are never empty it always renders, +1 icon per row, taking Axis A→2 / Axis B→4; S19/S20 will change the row again. Not a regression and not a reopening of this gate or R7: this closure rests on the still-passing overflow/row-height check plus the PO's accepted-crowding decision (S19), not a frozen icon tally — see the top-of-file Gate CLOSED note's dated addendum; the definitive re-measure was to happen once S19/S20 land — now DISCHARGED, 2026-09-09: S19's formal pass measured the real 6-icon worst-case row at 320/360/375/390px and it fits cleanly, zero overflow/wrapping (222/222). **Done.** This closure is independent of and unaffected by S13's separate, temporary Status reversion (see S13's own row) — that reversion is about a functional drag-interaction bug (C1) in a different code path, not the row's physical layout/markup, which is what this gate was always specifically about; holding this story's Done status hostage to an unrelated bug in a different story would conflate two independently-decidable questions. | Done | 2 |
| S8 | As a user, I want to assign an aisle/category to an item, either from a short suggested list or my own custom free text, so that I can group my shopping by store layout. | Each row supports an optional aisle field, editable inline similar to S7's note field; a small starter set of suggested values is offered via dropdown/datalist (e.g. Produce, Dairy, Meat/Seafood, Bakery, Frozen, Pantry, Beverages, Household, Other — exact wording is a Developer-level content detail, not a locked product requirement, freely adjustable at implementation time); the field also accepts arbitrary free text — suggestions are a convenience, never a closed enum, since the PO's own store may use different section names; aisle assignment is optional, items with none set are treated as "Unassigned" for S9's sort-by-aisle grouping. **Forward-reference hedge closed, 2026-09-08:** independently verified by Tester's S7-S10 formal pass (TC9.1-17 cover By-Aisle sort including Unassigned grouping) — see REGRESSION_LOG.md's 2026-09-08/154-total row. Aisle persists via localStorage; editing aisle is explicitly NOT covered by S6's undo buffer, same reasoning and same deliberate scope boundary as S7's note field (and per QA finding M1, does not clobber a pending undo target either, same resolution as S7's note field); **aisle-value comparisons for S9's grouping and for suggestion-matching use a normalized form — trimmed and case-folded — so "Produce", "produce", and "Produce " (trailing space) are treated as the same aisle and grouped together, same normalization approach S10 already applies to item names (QA finding R6); normalization is comparison-only and never rewrites what's actually stored/displayed for any individual item's own aisle text.** **Clarifying Tester's testability-check question, 2026-09-04 (scrum-master, confirming the intended reading):** "suggestion-matching" refers to a previously-implicit mechanism now made explicit — the dropdown/datalist is NOT purely the static starter set; it also includes every distinct free-typed aisle value already used elsewhere in the list, so a custom aisle name a user typed on one item becomes reusable/suggested for later items (this is the actual reason free-text aisle values need suggestion-matching normalization at all — a purely static 9-item list wouldn't need it). Deduplication for that dynamic pool uses the same normalized (trimmed, case-folded) comparison stated above: "Produce" typed on one item and "produce" typed on another collapse into ONE suggestion entry, not two. **Corrected per QA finding R8, 2026-09-08 (AC now matches actual implementation — folded in per Tester's request so the test plan matches what's built; documents existing behavior, is not itself a behavior change):** the dynamic suggestion pool is derived live from the current live list's aisle values every time it's computed — it is NOT a separate persisted record the way S10's frequency counter is; if the last item using a given aisle value is deleted, that value drops out of the dynamic pool (the static starter set is unaffected, and S10's own counter is a wholly separate record unaffected by any of this). Tie-break for which casing displays in a merged entry is NOT actually based on which was typed chronologically first (the originally-stated rule, now corrected) — it is based on current array position: whichever qualifying item appears first in the live list's current manual order wins the displayed casing. This means S5's Up/Down reorder buttons alone, with no retyping, can flip which casing displays for a merged entry; this is a cosmetic-only effect — it never changes which distinct aisle values are suggested, only which casing is shown once two typed variants have merged. **Developer sanity-check finding, 2026-09-04 (same landmine as S7's note editor, same fix):** an in-progress aisle edit must survive a re-render triggered by an unrelated action (Undo, another row's reorder, a new add), with its draft text intact, not silently discarded — same capture-before-rebuild/restore-after pattern as S1/S2/S5's focus-preservation fix. **Accessibility note (see top-of-file Accessibility scope):** if aisle groups get any color-coded treatment (e.g. a colored tag/swatch per aisle) at implementation time, that palette must be colorblind-safe — default to Okabe-Ito. Plain text labels with no color-coding at all also fully satisfy this story's AC as written; color is not itself required. **Sprint-2 sanity-check addition, 2026-09-04 (scrum-master):** the aisle-edit affordance (and its dropdown/datalist) is a nested control per S2's precedence rule, same treatment as S7's note-edit affordance — tapping it fires ONLY the aisle-edit action, never also the row's cross-off toggle. Same row-height allowance as S7: a row with a non-empty aisle tag may grow to fit it; a row with neither note nor aisle stays exactly as tall as S1/S2's locked spec. **Implementation status, 2026-09-08:** Developer live-verified this story via Playwright (not just code trace) — same shared-editor bug-fix pass as S7's (focus loss on open; a focusout-triggered re-render that could swallow a pending click), both fixed and confirmed live. **Tester's independent formal pass landed, 2026-09-08: 154/154 assertions passed (TC8.1-18, 18 checks for this story specifically, including 3 shared with S7 to close out the 2 bugs above with dedicated regression coverage), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/154-total row (script `vopping-tests-tester-s7-s10-formal.js`), current canonical citation.** This story's own functional AC is now fully verified — **Gate closed, 2026-09-09 — Tracked follow-up #2 (top-of-file) is now satisfied with real measurement, not inference:** Tester rendered the actual worst-case row post-S13+S14 and measured both crowding axes directly (screenshots + DOM data, 17/17 checks) rather than assuming which was worse — Axis A (this story's aisle AND S7's note both set) now collapses to item-name + 1 delete icon on line one; Axis B (both empty, the likely real source of the original "5 controls" figure) is down to 3 icons at 19.5px each (was 5 at 26px). Both confirmed no longer crowded. Full evidence in this story's own `test-plans/S8-aisle-designation.md`. **Icon counts now point-in-time, 2026-09-09:** the per-axis figures above (Axis A 1 / Axis B 3) predate S15's always-visible edit-icon (sha `10c804e`) — since item names are never empty it always renders, +1 icon per row, taking Axis A→2 / Axis B→4; S19/S20 will change the row again. Not a regression and not a reopening of this gate or R7: this closure rests on the still-passing overflow/row-height check plus the PO's accepted-crowding decision (S19), not a frozen icon tally — see the top-of-file Gate CLOSED note's dated addendum; the definitive re-measure was to happen once S19/S20 land — now DISCHARGED, 2026-09-09: S19's formal pass measured the real 6-icon worst-case row at 320/360/375/390px and it fits cleanly, zero overflow/wrapping (222/222). **Done.** This closure is independent of and unaffected by S13's separate, temporary Status reversion (see S13's own row) — that reversion is about a functional drag-interaction bug (C1) in a different code path, not the row's physical layout/markup, which is what this gate was always specifically about; holding this story's Done status hostage to an unrelated bug in a different story would conflate two independently-decidable questions. **Cross-story supersession, 2026-09-10 (superseded by S22/S24 — story STAYS Done, S19→S13 convention):** S22 supersedes this story's MECHANISM — the free-text aisle input + datalist + dynamic getAislePool are replaced by a persistent per-row native select drawing from a persisted state.aisles set (the iOS focus-zoom fix), and the aisle editingField / commit-on-blur path is replaced by a select change handler. This story's INTENT (assign an aisle/category to an item; a free-form set, not a closed enum) is unchanged and stays Done; only the input mechanism moved. See S22, and S24 for adding a new aisle from the item's dropdown. | Done | 2 |
| S9 | As a user, I want to view my list sorted by aisle or alphabetically, in addition to my own manual order, so that I can shop more efficiently by store layout or find an item quickly. | A sort control offers at minimum: Manual (default — S5's up/down-controlled order), Alphabetical (A-Z by item name), and By Aisle (grouped using S8's aisle field, "Unassigned" items grouped last, alphabetical order used as the secondary/tie-break order both between groups and within each group); **sorting is a view-only concern** — it changes only on-screen render order and never rewrites the underlying stored item array/order in localStorage; switching back to Manual, or simply reloading the page, always returns to the exact same manual order as existed before any sort was applied, with zero data mutation from having sorted (this story is what actually implements the project's "list only changes when items are added/removed" guardrail — sorting must be provably non-destructive, testable by sorting, reloading, and diffing the stored array); whenever a non-Manual sort is active, manual-order-only reordering (S5's original Up/Down buttons, superseded 2026-09-09 by S13's drag-and-drop per S13's own row — see the closed forward-reference note below) is hidden/disabled per row (visual position no longer matches the manual-order swap/drop target, so allowing it would be confusing) and reappears automatically when the user switches back to Manual; check/uncheck, delete, and note/aisle editing remain fully available in every sort mode; **grouping uses S8's normalized (trimmed, case-folded) aisle-value comparison (QA finding R6), so casing/whitespace differences in how an aisle was typed never fragment a group; resolving QA finding M2:** a newly-added item (via S1's single add, S4's paste-ingest, or S10's suggestion-tap) appears immediately at its correctly-computed position within whichever sort is currently active — e.g. its alphabetical slot, or inside its aisle's group — not only at the true end of the underlying manual array; the visible render always reflects the live sort mode for every mutation, add included, consistent with every other action already being fully available in every sort mode; the current sort selection does not need to persist across reload — reload always returns to Manual view (decided directly, low-stakes default, cheap to change later if the PO wants the last-used sort remembered). **Implementation status, 2026-09-08:** Developer live-verified this story via Playwright (not just code trace) — zero bugs found. **Dated note, 2026-09-08 (Developer-flagged, tracked so it isn't lost, NOT a Done-blocker):** style.css has zero rules yet for this story's new UI classes (`.sort-controls`, `.aisle-group-header`) — functions correctly per live verification but renders unstyled; no visual spec is locked in this story's own AC (unlike S1/S2/S5's density spec or S7/S8's PO-gated row above), so this does not gate Done the way those do — a follow-up styling pass for Developer to pick up whenever convenient. **Done, 2026-09-08 — Tester's independent formal pass: 154/154 assertions passed (TC9.1-17, 18 checks for this story specifically), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/154-total row (script `vopping-tests-tester-s7-s10-formal.js`), current canonical citation.** **Forward-reference notes, 2026-09-08 (do not change what Tester tests against right now — Tester's formal pass should run against today's implementation as described above; these are future-supersession pointers only):** (1) **Closed, 2026-09-09:** the AC clause above is now updated to point at S13's drag mechanic instead of S5's buttons, since S13 shipped (sha `33deddf`) — same cross-story-supersession handling as S5's own dated note, not a defect in this story; Tester's TC9.8/TC9.9 were rewritten (not dropped) to assert the real S13-era equivalent (drag-pickup arm/no-arm gated by sort mode via a real pointerdown-past-delay probe, instead of button visibility) — see `test-plans/S9-sort-view.md`'s own dated update note and REGRESSION_LOG.md's 2026-09-09/216-total row; (2) S16 (suppress the per-row aisle tag when sorting By Aisle) and S17 (more visual distinction for aisle group headers) are drafted as follow-on stories to this one rather than folded in now, specifically so they don't disrupt Tester's already-started formal pass — see S16/S17's own rows. **Forward-reference note, 2026-09-09 (reorder mechanism reverting again — do not change what Tester tests against today):** the PO has decided to revert the reorder mechanism from S13's drag-and-drop back to S5-style Up/Down buttons after a second real-device drag failure — drafted as **S19**. Once S19 ships, this AC's non-Manual-sort clause (currently "S5's original Up/Down buttons, superseded 2026-09-09 by S13's drag-and-drop") re-points to S19's restored Up/Down buttons — the hide-during-non-Manual-sort / reappear-in-Manual behavior itself is unchanged, only the control it governs swaps back to what this clause originally described pre-S13. Left pointing at S13's drag mechanic until S19 actually ships, so Tester's coverage keeps matching what's implemented today — same handling this clause already used across the S5→S13 transition. **Cross-story supersession, 2026-09-10 (superseded by S22 — story STAYS Done, S19→S13 convention):** the by-aisle Unassigned group label is superseded by state.unassignedLabel (default Other); Unassigned now shows only after the Other label is deleted, and the group-label lookup moves from items-derived to state.aisles-derived, guarded by a defensive fallback (S22 R15). The by-aisle sort/grouping behavior itself (including the no-aisle bucket sorting last) is unchanged and stays Done; only the label source moved. See S22. **Further ripple, 2026-09-11 (S26 Locked — cross-story supersession per playbook, S19→S13 convention; S9 STAYS Done):** S26 changes how the per-item aisle affordance RENDERS in By-Aisle mode — it becomes a primary-line icon-footprint `<select>` shown in ALL modes (NOT suppressed in By-Aisle, preserving this story's edit-in-every-sort-mode guarantee), so the previously-redundant full-width second-line select under a group header goes away. This story's by-aisle sort/grouping BEHAVIOR (no-aisle/Other bucket last, alpha within/between groups, view-only and non-destructive) is unchanged and stays Done; only the per-row aisle affordance's rendering moved. See S26. | Done | 2 |
| S10 | As a user, I want the app to suggest items I've frequently added in the past but haven't added to my current list yet, so that I don't forget staples while shopping. | Data model: a separate, persistent localStorage record — independent of the live list's own contents — tracks a count per distinct item name (matched case-insensitively/trimmed), incremented by 1 every time that name is added: once for a single S1 add, once per line for an S4 paste-ingest batch; deleting an item from the live list (S3) does NOT decrement its historical count — the count reflects cumulative past usage, not what's currently on the list; a "What am I missing?" affordance shows suggestion chips for item names whose historical count is at or above a threshold (starting default: 2, i.e. added at least twice before — an easily-tunable constant, flagged non-blocking in QUESTIONS.md, not a hard product lock-in) AND that are not currently present anywhere in the live list (same case-insensitive/trimmed name match); suggestions are sorted by descending historical count; tapping a suggestion adds it to the live list via the same mechanic as S1's single add (appends to the end, covered by S6's undo, increments its own historical count again — no special-case needed); no dismiss/snooze mechanic for an unwanted suggestion — explicitly out of scope for this story, a candidate future story only if the PO finds repeat unwanted suggestions annoying in real use; this story's real-world usefulness also depends on items actually leaving the live list between trips, which S12 (bulk "clear crossed-off items," added 2026-09-04 to resolve QA finding R3) now provides — without S12, checked-off staples would never resurface as suggestions since they'd never leave the list; hard dependency on S1 and S4 both existing first (the counter must hook into both add paths from day one) — sequenced after sprint 1 for exactly that reason. **Same defensive load-time guard as S1 applies to this story's own separate frequency-counter record (QA finding R1): a parse failure, or a parsed value that isn't the expected plain-object/map shape, falls back to an empty counter, never throws.** **Implementation status, 2026-09-08:** Developer live-verified this story via Playwright (not just code trace) — zero bugs found. **Dated note, 2026-09-08 (Developer-flagged, tracked so it isn't lost, NOT a Done-blocker):** style.css has zero rules yet for this story's new UI classes (`.suggestions-root`, `.suggestion-chip`) — functions correctly per live verification but renders unstyled; no visual spec is locked in this story's own AC (unlike S1/S2/S5's density spec or S7/S8's PO-gated row above), so this does not gate Done the way those do — a follow-up styling pass for Developer to pick up whenever convenient. **Done, 2026-09-08 — Tester's independent formal pass: 154/154 assertions passed (TC10.1-13, 21 checks for this story specifically, including TC10.5 which also closes S12's forward-reference hedge), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/154-total row (script `vopping-tests-tester-s7-s10-formal.js`), current canonical citation.** | Done | 2 |
| S11 | As a user, I want a separate, more forgiving input mode for pasting messier recipe ingredient lists (with quantities, units, and prep notes mixed in), so that I get cleaner results than S4's simple line-per-item paste box would produce on recipe text specifically. | Not yet scoped in detail — no locked AC. Likely needs quantity/unit-stripping heuristics or a small structured parser, meaningfully bigger than S4's plain newline-split; PO was explicitly fine leaving this as a separate toggle/mode alongside S4's simple box, not a replacement for it. **Explicitly out of scope for the current backlog/sprint — do not build.** Revisit only if the PO asks for it after living with S4's simple version for a while. **Idea preserved, 2026-09-08 (PO, not yet scoped or prioritized — parking here so it isn't lost, not a commitment to build):** PO's own words — "when we get to ingestion enhancements, we should consider a way to auto-detect notes (if we don't already have that)" — i.e. distinguishing which pasted lines are meant as S7 notes vs. new list items during paste-to-ingest (S4/S11 territory). No AC, no design, no priority yet; revisit whenever ingestion work (S4 follow-on or this story) comes up again. | Not Started | Unscheduled — parked, low priority, future |
| S13 | As a user, I want to reorder items by dragging them, so that I can rearrange my list to match my store's layout without dedicated Up/Down buttons taking up row space. | **Drafted 2026-09-08, PO decision from the crowded-row review (QA finding R7):** PO's own words — "we just don't have the space for those buttons, unfortunately... make sure there's a bit of a delay in the pick up action so that i'm not accidentally moving things all over the place when i'm trying to edit." Supersedes S5's Up/Down-button reorder mechanism at the UI/interaction layer only — S5's underlying data model (persisted manual array order, immediate localStorage write, survives refresh) carries over unchanged; S5 itself stays Done (see S5's dated technical note), this is cross-story supersession, not a reopening. Up/Down buttons are removed from the row entirely once this ships, freeing the row space S14 spends. A press-and-hold with a deliberate pickup delay initiates drag mode (exact delay duration is a Developer-level tuning detail, not PO-locked to a specific value) — must be long enough that ordinary scrolling and tapping the row's other nested controls (whole-row cross-off tap, note/aisle-toggle, delete, S15's edit-icon, S16's icon-only aisle affordance) never accidentally trigger a pickup; a quick tap below that threshold still performs the existing whole-row cross-off toggle (S2) unchanged, and every other nested control still performs only its own action (nested-control precedence, see S2, now also covering this new drag-pickup gesture in both directions). Dragging inserts the item at an arbitrary new position (not limited to swapping with the immediate neighbor as Up/Down was), persisted immediately on drop. **Tester's testability-check landed, 2026-09-08 — three real gaps, resolved directly here (no PO input needed, narrow technical-shape decisions needed for a deterministic test, same category as e.g. S1's storage-shape or S6's buffer-shape calls):** (1) **Drop-position rule:** while dragging, the list shows a live placeholder/insertion-point indicator that moves as the drag hovers over different rows; whether the current hover position is above or below the midpoint of the currently-hovered row determines insert-before vs. insert-after that row; releasing commits the dragged item to wherever the placeholder currently sits at that moment — this is the exact, deterministic rule Tester needs to assert "drag item A to position X, resulting order is Y." (2) **Jitter tolerance during the pickup delay:** the touch point must stay within a small movement/jitter tolerance (exact px value is a Developer-level tuning detail) for the full delay duration; if the touch moves beyond that tolerance before the delay elapses, the pickup is cancelled and the gesture is treated as an ordinary scroll — no drag begins, and the delay timer does not resume from where it left off on a later hold, it restarts fresh from a new press. (3) **Drop released outside the list's own scrollable bounds** (e.g. released over the header, or past the very top/bottom edge during auto-scroll): commits to the nearest valid boundary position (top of list if released above; bottom of list if released below) — never a silent no-op or cancel, and like any other drag-drop reorder, undo-recoverable if the resulting position wasn't what the user wanted. (4) **QA finding R9, 2026-09-08, folded in directly (technical edge-case, no PO input needed, same category as (1)-(3) above):** a Pointer Events `pointercancel` (the drag interrupted by something outside the user's control — OS-level gesture takeover, app backgrounding, etc. — not a normal release) aborts the drag entirely: the item returns to its original position, nothing is committed, no undo entry is created — consistent with this project's own no-surprise-mutations guardrail; a `pointercancel` is never treated as an implicit drop. **Two additional testability requirements, folded in directly:** the drag interaction must be implemented using Pointer Events, not a touch-only listener, so it is automatable via Playwright and behaves consistently across input types. Starting a drag-pickup on one row must commit (never silently discard) an in-progress note/aisle/name draft still open on a different row — same cross-row commit-on-unrelated-action guarantee S7/S8 already require (and already needed two bugfixes for), now explicitly extended to cover this story's new drag-pickup gesture as one more "unrelated action" that must trigger it. Same as S5: hidden/disabled during any non-Manual sort (S9) — drag is a manual-order-only operation — and reappears automatically when the user switches back to Manual. Each single drag-drop reorder remains one of the four action types S6's undo buffer covers (same undo-eligible status S5's swaps already had); undo restores the item to its exact prior position. No dedicated drag-handle icon is required by this AC — whether the whole row (outside other nested controls) is the drag surface, or a small dedicated handle icon is added, is a Developer-level implementation choice. **QA gate findings M9-M12, 2026-09-08, folded in directly (technical edge-cases, no PO input needed, same category as R9 and (1)-(3) above):** (M9) **Same-row drag vs. an already-open editor:** a row with its own note/aisle/name editor currently open cannot be drag-picked-up — a press-and-hold on that row is captured by the open editor's input field as ordinary text-input interaction (e.g. cursor placement), not a drag attempt; to drag that row, its editor must first be committed/closed (e.g. by tapping elsewhere, which already auto-commits it per the existing cross-editor rule), returning the row to its normal drag-eligible state. (M10) **Jitter/delay rule applies identically regardless of drag-surface choice:** whichever Developer-level surface choice is made above (whole-row press, or a dedicated handle icon), the same press-and-hold-with-jitter-tolerance gate applies to that specific surface before a pickup begins — a dedicated handle does not get an instant-pickup-on-first-touch shortcut. (M11) **No-op on a same-position drop:** if a drag ends with the item released back at its exact original position (no net change), this is a no-op — no undo-buffer entry is created, consistent with add/delete's own established empty/no-op precedents elsewhere in this project. **QA gate finding M15, 2026-09-08, folded in directly (tie-break, no PO input needed):** M11 and the drop-outside-bounds rule above ((3)) both reach a real, reachable case — nudging the top item just above the list and releasing lands at the nearest boundary (position 0), which is also that item's own original spot — with nothing previously stating which rule wins. **M11's no-op wins whenever both conditions coincide** (list order is identical either way; the only real difference is whether a bookkeeping undo-buffer entry gets created) — this matters because letting the boundary rule "commit" in this exact-same-position case would create a spurious undo entry for a net-zero move, silently clobbering whatever undo-eligible action was already pending before the drag, which is precisely the kind of surprise mutation this project's own guardrail exists to prevent; the boundary rule's own "never a silent no-op" intent is unaffected, since it was about not silently ignoring an out-of-bounds drag attempt, not about forcing an undo entry when the visible outcome is provably unchanged. (M12) **Scroll suppression during an active drag:** once a drag is actively underway (post-pickup, distinct from the arming delay before pickup begins), normal touch-scrolling of the page/list is suppressed for the drag's duration — the only scrolling that happens during an active drag is the auto-scroll-near-viewport-edge mechanic specified below, not an independent user scroll gesture fighting with it; suppression ends the instant the drag ends (drop, cancel, or `pointercancel`). **Accessibility:** consistent with this project's already-settled accessibility scope (top-of-file Accessibility scope note, playbook §7) — touch-only drag-and-drop with no keyboard equivalent is accepted, not a gap; keyboard/screen-reader support remains explicitly out of scope project-wide. **PO decision, 2026-09-08 (resolves the auto-scroll question Developer's sanity-check raised):** auto-scroll during drag is a REQUIRED part of this AC, not optional — PO's own words: "the grocery lists can sometimes be quite long, so that level of complexity is unfortunately important." While a drag is in progress, dragging the held item near the top or bottom edge of the visible viewport must auto-scroll the list in that direction (continuing to scroll for as long as the drag stays near that edge), so a long list can be reordered across its full length without the user having to release, manually scroll, and re-pick-up; exact auto-scroll trigger-zone size and scroll speed are Developer-level tuning details, not PO-locked to specific values. Resolved in QUESTIONS.md's Blocking table. **QA re-confirmed R9 is fully resolved, 2026-09-08** (`pointercancel` aborts cleanly, no partial commit, no undo entry — exactly as specced). **Same QA gate pass also caught 4 previously-unaddressed Minors (M9-M12) — decided directly to fold all four in now rather than defer as non-blocking follow-ups** (same technical-shape category as R9 and the earlier testability-check gaps; each is a one- or two-sentence clarification, cheap to resolve now, and this project's own playbook explicitly warns that deferred items risk silently evaporating) — see M9-M12 folded in above. **QA re-confirmed M9/M10/M12 clean, 2026-09-08; M11 surfaced one more finding (M15, a tie-break vs. the drop-outside-bounds rule) — resolved directly above, adopting QA's own recommendation.** **QA's final Lock-gate re-read, 2026-09-08 — no new contradictions found across all 6 rounds of fixes (drop-position/jitter/out-of-bounds/pointercancel, then M9-M12, then M15's tie-break); no objection to Locking.** **Locked, 2026-09-08 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA gate, six iterative rounds), clear for Developer implementation.** **Implemented, 2026-09-09 (pushed sha `33deddf`, part of `635f906`):** whole-row press-and-hold drag surface (no dedicated handle icon), raw Pointer Events throughout, Developer self-verified (27/27 new checks, zero unexpected regressions against the existing suites). **Done, 2026-09-09 — Tester's independent formal pass: 216/216 assertions passed (67 Sprint-1 retrofitted-and-reconfirmed + 81 Sprint-2 retrofitted-and-reconfirmed + 26 Sprint-3 S14/S16/S17 reconfirmed unchanged + 42 new S13-specific checks, TC13.1-13.26), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-09/216-total row (script `vopping-tests-tester-s1-s13-formal.js`), current canonical citation. This retrofit also retired S5's own 7 now-inapplicable Up/Down-button test cases (see S5's row) and rewrote S9's TC9.8/TC9.9 for the drag era (see S9's row) — both cross-story consequences already anticipated in this project's own docs before S13 shipped, not surprises.** **Disclosed fix, 2026-09-09 (Developer, not hidden; pushed sha `50c57c8`):** PO found a real bug testing on an actual iPhone — the drag pickup's press-and-hold collided with iOS Safari's own native long-press-to-select-text/callout gesture. Fixed via `-webkit-user-select: none` + `-webkit-touch-callout: none` on the row (S1/S2's existing plain `user-select: none` alone doesn't cover iOS Safari's own prefixed/callout mechanisms), with an explicit override on the note/aisle editor's own input so normal text editing stays unaffected. **Tracked, non-blocking:** this fix is verified in Chromium only for correct CSS parsing/scoping — Chromium doesn't implement iOS Safari's long-press-select/callout gestures at all, so the actual behavioral fix on a real iPhone is unconfirmed by any automated pass by construction; needs the PO to re-test on their actual device to close out, same "real hardware is the real acceptance signal, emulation only approximates it" standard this project has applied to every other device-dependent claim. **Status reverted, 2026-09-09 (scrum-master's call, no PO input needed — first time this exact scenario has come up on this project):** QA's adversarial post-Done review found **C1 (Critical)** — a confirmed, reproducible crash: starting a drag-pickup on one row while a note/aisle editor is open on a DIFFERENT row throws an uncaught exception (`beginDrag`'s captured `li` DOM reference goes stale the instant the cross-row-commit's own `render()` call rebuilds the whole list, six lines before that stale reference gets dereferenced) and leaves the entire list permanently unscrollable until reload — a real, easily-reachable, ordinary-use crash, not a contrived edge case, in code the 216/216 pass above genuinely did exercise for its OTHER assertions but never combined with a still-open cross-row draft in a way that continued on to check the drag itself actually completed. Full trace in QA_FINDINGS.md (C1). Fix already sent to Developer (re-fetch `li` fresh from the DOM immediately after `commitEditor()`, mirroring the already-fresh `idx` re-derivation right below it). **This project's controlled Status vocabulary (Not Started / In Progress / In Review / Locked / Done) has no "Reopened" label, and inventing one would silently break the console's filter (per playbook §3/§6) — reusing "In Review" here instead, same label S7/S8 used, but for a different reason: S7/S8's "In Review" meant "functionally correct, blocked by an external gate"; this one means "a confirmed defect exists in shipped code, fix in progress, Done is not currently an accurate claim." Flagging this distinction explicitly so a future reader doesn't conflate the two.** Reinstating Done requires: Developer's fix landing, plus Tester adding dedicated regression coverage for the exact combination that triggers it (open an editor on row B, drag-pickup row A, confirm no exception and the drag completes normally) — same "technical-shape, no PO input needed" resolution category as R9/R11, per QA's own recommendation. M18 (a Minor, mischaracterized-risk code comment near `setPointerCapture`) is cheap and non-blocking — worth folding in alongside C1's fix rather than as a separate pass, not itself a reason to hold anything up. **This does NOT reopen or affect S5's or S9's own Done status** — their own dated notes/closed forward-references above describe a real, already-shipped markup/behavior change (buttons physically removed, TC9.8/TC9.9 rewritten) that stands regardless of this unrelated interaction bug elsewhere in S13's own code. It also does NOT reopen S7/S8's Tracked follow-up #2 gate closure (see their own rows) — that closure was about the row's physical layout/crowding, a fact unaffected by this functional drag-interaction bug in a different code path. **Reinstated to Done, 2026-09-09 (scrum-master's verification against this row's own stated reinstatement condition — both halves independently confirmed, not assumed):** **(1) Developer's fix landed** — sha `90983e3`, pushed: `beginDrag()` now re-fetches the row element fresh by id immediately after `commitEditor()`, mirroring the already-fresh `idx` re-derivation right below it, exactly the fix shape QA recommended; also hardened M18's `setPointerCapture` fallback in the same commit. **(2) Tester added dedicated regression coverage for the exact triggering combination** — `test-plans/S13-drag-drop-reorder.md`'s TC13.11 was strengthened (now also asserts the LIVE row, not a stale detached clone, receives the `dragging` class) and a new TC13.27 was added covering the identical combination via an AISLE editor (C1's root cause is field-agnostic); both pass, cited to a fresh independent run of `c:\tmp\pw-test\vopping-tests-tester-s1-s13-formal.js` (45 S13-specific checks + the full 174-check regression baseline = 219/219, zero defects, zero regressions). Tester also independently settled a discrepancy between QA's original trace and Developer's own repro by reproducing both pre-fix and post-fix behavior directly against an isolated pre-fix temp copy (`git show 8b17aac`, script `vopping-c1-independent-repro.js`) — confirmed the fix genuinely closes the bug regardless of exact mechanism. **Severity/mechanism correction (per QA_FINDINGS.md's own 2026-09-09 "Correction" entry, carried forward here for accuracy):** C1's description earlier in this row (thrown exception, list permanently unscrollable until reload) is confirmed inaccurate — `listRoot.innerHTML` replacement only detaches the old `<ul>`, not its descendants, so the stale `li` reference's `insertBefore` call succeeds silently into a dead subtree rather than throwing; the real pre-fix bug was a silent stale-clone visual/positional defect (wrong/missing `dragging` styling, a skewed insertion-index calculation), scoped to that one gesture only, self-resolving the instant it ended — no persistent broken state, no data loss survived past it. QA has retroactively reclassified this as **R13 (Real, not Critical)**; the root cause identified and the fix shipped are both unaffected by this correction, only the downstream failure-mode description and severity label change. **Ledger citation reconciled, 2026-09-09:** the superseding 219/219 run IS recorded in REGRESSION_LOG.md as its own dated row (correctly marked superseded by the later 246/246 S15 row) — an earlier note here flagged it as not-yet-appended, which was a stale read during work concurrent with Tester's own edits; nothing is owed there. Cite REGRESSION_LOG.md's 219 row for this fix's count (superseded-chain intact). **S13 → Done.** **Reorder mechanism reverted — new PO decision, 2026-09-09 (drafted as S19, see its row):** the PO reports drag-and-drop has now failed a SECOND time on their real device — this time the drag gesture loses to the phone's native scroll ("when i try to drag the item, it scrolls the screen instead of moving the item"), the first being the iOS text-select collision (sha `50c57c8`, itself still never confirmed fixed on-device). The PO has decided to abandon drag-and-drop and return to S5's Up/Down buttons — their words: "i'll take crowded over not working." Drafted as new story **S19** (restore Up/Down, remove drag), mirroring the exact cross-story-supersession shape this row itself used to supersede S5: **S13 stays Done** (it shipped and passed its full AC — 219/219 — in the only environment we can verify; real-device behavior was always a separately-tracked, non-blocking signal, never part of this Done claim, same principle as the iOS fix's own tracked-non-blocking note above), rather than reopened, since nothing about its verified AC changed; S19 supersedes its live mechanism the way S13 superseded S5's. **Escape hatch (per Orchestrator):** this revert is the PO's decision UNLESS Developer's separate, parallel root-cause diagnosis of the real-device scroll-collision (likely a `touch-action`/non-passive-listener/`preventDefault` interaction desktop Chromium structurally can't reproduce) proves it trivially fixable AND the PO, shown that, reconsiders — in which case S19 would be parked and this mechanism retained. Proceeding with the revert regardless in the meantime; the diagnosis does not block drafting/locking S19. If that diagnosis instead confirms a genuine S13 implementation defect (not inherent touch-drag fragility), that could retroactively warrant the same "In Review" treatment C1 got — a call to make once the diagnosis actually lands, not preempted here. **Escape hatch CLOSED — 2026-09-09, PO's final call with the full diagnosis in hand (decision is final, no longer conditional):** Developer's root-cause diagnosis came back NON-trivial — it's an iOS `touch-action`-latched-at-`touchstart` conflict: Mobile Safari commits to native scrolling at the moment of `touchstart`, before the drag's JS pickup-delay timer can claim the gesture, so a press-and-hold drag can never out-compete scroll on that engine (desktop Chromium doesn't latch touch-action this way, which is exactly why S13 passed 219/219 in our test environment yet failed on the PO's real phone). The Orchestrator laid out the one drag-preserving alternative to the PO — a dedicated drag-handle, one icon fewer than restored Up/Down (5 vs 6) and keeping drag — and the PO explicitly DECLINED it, choosing the proven/familiar Up/Down mechanism after being burned by drag twice. So the conditional above is resolved: drag-and-drop is out for good, the handle-based-drag alternative is explicitly declined, and S19's revert proceeds unconditionally — no longer "revert unless the diagnosis is trivial / the PO reconsiders." This diagnosis does NOT trigger the "In Review" branch flagged above: the resolution is supersession by S19, not a fix-in-progress, and the root cause is a platform-level touch-action/scroll-latch behavior rather than a discrete implementation bug being actively fixed — so S13's Done (the record of the shipped-and-verified drag feature) stands, its live mechanism superseded by S19. | Done | 3 |
| S14 | As a user, I want the remaining per-row control buttons to be about 25% smaller, so that removing the Up/Down buttons (S13) actually frees up usable row space instead of leaving it empty. | **Drafted 2026-09-08, PO decision from the crowded-row review:** PO's own words — "i'm not having any issues hitting them right now. if they were 25% smaller, and we got rid of the up/down arrows, there would be a lot more real estate." Applies a uniform ~25% size reduction to the row's remaining nested icon/button controls — note-toggle (S7), aisle-toggle (S8), delete (S3), and S13's drag-handle affordance if a dedicated one exists; does NOT affect item-name/note/aisle-tag text size (that's control-chrome sizing only, distinct from S17's aisle-group-header text treatment). **QA gate finding (Nitpick, 2026-09-08, folded in directly — no PO input needed):** to remove any ambiguity, this also does NOT affect the note/aisle tag's own surrounding chrome (background/border/padding of the tag itself, if any) — the shrink applies to the row's icon/button controls only, never to the tag/text content they sit beside; currently moot since neither tag has any fixed chrome in the shipped CSS to begin with, but worth stating explicitly so a future styling pass doesn't get misread as in-scope for this story. The PO owns this sizing tradeoff directly, not something Developer/Tester need to re-litigate for comfort. No functional/behavioral change to any control's action — visual/sizing-only. Sequenced immediately after S13: no hard technical dependency, but the PO paired the two asks together, and finalizing size once against S13's final row layout avoids resizing twice. **PO decision, 2026-09-08 (resolves the tap-target-floor certainty Developer's sanity-check raised — the math guarantees a 25% reduction breaches Tester's earlier informational >=24px tap-target check, e.g. 26px × 0.75 ≈ 19.5px, not a maybe):** the PO is fine with controls landing under 24px — their own words: "if it's a problem, we can adjust again later or figure out a different strategy." This is an ACCEPTED TRADEOFF, not a violation to keep re-flagging — the >=24px figure was always informational per this project's narrow accessibility scope (playbook §7, top-of-file Accessibility scope note), never a hard gate, and the PO has now explicitly waived it for this story. Escape hatch, stated explicitly per the PO's own framing: revisit sizing only if it becomes a real problem in practice (e.g. real mis-taps reported), not as a pre-emptive re-litigation. Resolved in QUESTIONS.md's Blocking table. **Tester's testability-check landed clean, 2026-09-08 — no gaps, sent straight to QA's per-story gate.** **QA gate landed clean, 2026-09-08 — zero Real/Minor findings, one Nitpick (folded in above).** **Locked, 2026-09-08 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA gate), clear for Developer implementation.** **Cross-reference added, 2026-09-08 (QA finding M14 on S16, additive clarification — not a reopening of this already-Locked story):** S16's new icon-only "edit aisle" affordance (introduced after this story was drafted) is itself a nested per-row icon control, so it is also in scope for this story's ~25% shrink, same as the note-toggle/aisle-toggle/delete/drag-handle controls already listed above. **Cross-reference added, 2026-09-08 (QA finding M17 on S15, additive clarification — not a reopening of this already-Done story):** S15's new dedicated edit-icon (introduced after this story was drafted and implemented, PO-confirmed via `s15-edit-gesture-picker.html`) is likewise a nested per-row icon control and is also in scope for this ~25% shrink — Developer applies it when S15 itself is implemented, same forward-reference pattern as S16's icon above. **Disclosed implementation side-effect, 2026-09-08 (Developer, not hidden — full detail on S5's own row):** S5's Up/Down buttons, not named in this story's control list, turned out to share the same `.icon-btn` CSS class as the controls actually targeted here (this AC was drafted assuming S13 — which removes Up/Down entirely — would land first; the order flipped). Developer let them shrink too rather than splitting a separate class to hold them at the old size for what's expected to be only a few days until S13 ships and deletes them outright. Purely cosmetic, purely temporary, zero functional change — accepted as-is, no AC change needed here beyond this disclosure note. **Done, 2026-09-08 — Tester's independent formal pass: 180/180 assertions passed (74 Sprint-1 + 80 Sprint-2 re-confirmed clean + 26 new S14/S16/S17 checks, TC14.1-14.5 for this story specifically), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/180-total row (script `vopping-tests-tester-s14-s16-s17-formal.js`), current canonical citation. Implementation pushed sha `606285f`.** **Cross-reference, 2026-09-09 (additive, not a reopening of this Done story):** new story **S20** (drafted 2026-09-09) restyles all per-row icon controls — removing the square button outline/chrome and scaling each glyph to fill the button's footprint — the PO's own idea to ease the crowding S19's Up/Down restoration reintroduces. S20 is complementary to, and does NOT undo, this story's ~25% footprint reduction: the tap-target footprint stays exactly whatever this story left it; S20 only removes the surrounding chrome and enlarges the glyph within that same footprint (PO: "the appearance of a bigger button without the size of the button actually changing"). Same `.icon-btn` control set this story targeted. | Done | 3 |
| S15 | As a user, I want to edit an item's own name/text in place, so that I can fix a typo or add a quick detail (e.g. "zucchini" → "2 zucchini") without deleting and re-adding the item and losing its position, note, aisle, or crossed-off state. | **Drafted 2026-09-08, PO decision from the crowded-row review — real functional gap, the app has never supported editing an item's own text, only its note/aisle metadata.** Functional core (locked now): editing an item's own text is supported directly on the row, without delete+recreate; the edited text persists to localStorage immediately, no reload required; the item's position, crossed-off state, note, and aisle fields are all preserved across the edit. Blank/whitespace-only edit reverts to the item's original text unchanged (no accidental deletion via an emptied edit) — same trim-and-revert precedent as S1's/S7's existing empty-input handling (decided directly, no PO input needed). No de-duplication check against other item names — same no-dedup policy already stated project-wide (S1/S4). **Developer sanity-check landed, 2026-09-08 — two real AC gaps found, resolved directly here (no PO input needed, narrow extensions of already-established precedent):** (a) editing an item's text does NOT touch S10's separate frequency counter, in either direction — the counter simply doesn't observe this edit at all, same arm's-length treatment already given to S7/S8's note/aisle edits, extended symmetrically from S12's existing "counter never decrements on removal" rule; this specifically avoids the fragmentation risk Developer flagged (editing "zucchini" to "2 zucchini" must NOT create two separate counter entries). (b) the same live-resort-on-mutation guarantee S9 already requires for adds (QA finding M2) applies here: editing an item's text while a non-Manual sort is active immediately re-renders the item at its newly-correct position within that sort — e.g. editing "zucchini" to "apple" under Alphabetical sort moves the row to reflect "apple"'s new alphabetical position immediately, not only after a reload or sort-mode toggle. **QA finding R11, 2026-09-08, folded in directly (same technical-edge-case category as everything else resolved directly this session, no PO input needed):** this re-sort trigger reads the item's last-committed name, never the live in-progress draft still being typed in the open editor — same precedent S8/S9's own grouping-key logic already established (grouping/sorting reads the committed `item.aisle`, never a live in-progress aisle draft). While the editor is open and actively being typed into, the row does NOT relocate on every keystroke; it snaps to its newly-correct position only once the edit commits (blur/Enter), exactly like every other mutation-triggered re-sort in this project. This closes off the same failure shape as S7/S8's already-twice-fixed focus-loss-on-render bug — self-triggered this time (an open editor's own re-sort re-rendering it out from under its own cursor) rather than triggered by an unrelated action elsewhere. Undo eligibility is an open, non-blocking question (see QUESTIONS.md) — working default: NOT covered by S6's undo buffer, treated the same as S7's note edits and S8's aisle edits, open to override. Sequenced after S13/S14 so the decision-tool mockup reflects the row's final post-shrink, post-drag-and-drop layout, not one about to change again. **Tester's testability-check landed, 2026-09-08 — three real gaps, resolved directly here (no PO input needed):** (1) **Implementation approach stays Developer's choice (reuse S7/S8's shared `editingField`/`openEditor`/`commitEditor` infrastructure, or a separate implementation) — NOT locked here — but regardless of which approach is taken, the name editor must satisfy the same two testable guarantees S7/S8's editors already provide:** (i) an in-progress name-edit draft survives a re-render triggered by an unrelated action (Undo, another row's reorder/drag-pickup, a new add) with its draft text intact; (ii) opening this row's name editor, or a drag-pickup starting elsewhere (S13), or opening a different row's note/aisle editor, correctly commits (never silently discards) whatever edit was already in progress. Tester should write dedicated coverage for both regardless of implementation approach, since a separate implementation could reintroduce either bug class independently of S7/S8's already-fixed code. (2) **Simultaneous editors on one row:** the same mutual-exclusivity rule already established and tested for S7/S8 (only one editor open at a time per row; opening any editor — note, aisle, or now name — commits whatever editor was previously open, same-row or cross-row, same tested "second-editor-commits-first-draft" guarantee) extends to include this story's new name editor as a third editor type in that same set; note/aisle/name editors are never simultaneously open. (3) **Truncation after editing:** committing an edit does not change how the item name subsequently renders — same single-line/ellipsis-truncation behavior the locked row-density spec (S1/S2) already establishes for every item name, edited or not; this story does not introduce multi-line wrapping for item names the way S7's notes wrap. While the editor itself is open, the input shows the in-progress text in full (scrolling within the input as needed, same as any standard text input) — truncation applies to the saved/displayed name, not to the live edit-in-progress input. **PO decision, 2026-09-08 (from `s15-edit-gesture-picker.html`, Developer-built per the playbook's role-boundary lesson):** the edit gesture is a dedicated edit-icon/button (the tool's "Option 3"), NOT double-tap or long-press — PO's reasoning, their own words: consistent with already using a dedicated button for delete rather than introducing a new swipe/tap gesture; also confirmed ~20 characters of row space remain even pre-S14-shrink with 4 buttons present, which they consider sufficient ("longer text is what notes are for"). This resolves the gesture/icon conflict the PO originally flagged (S7's note-toggle already uses a pencil icon) — Option 3 offered 3 icon pairings resolving that conflict, one marked "recommended" (frees the pencil for this story's edit affordance, gives S7's note-toggle a different, non-pencil icon — reflecting the PO's own earlier "maybe a sticky note?" suggestion). **PO explicitly confirmed the recommended pairing, 2026-09-08 (same day, resolving the ambiguity flagged below):** "yes the one i said i like the most is my confirmed choice. i'll be clearer about that language in the future." — closes the ambiguity directly, not an inferred pick. **Locked here:** this story's own dedicated edit-icon/button uses the pencil glyph (✎), freed from S7's note-toggle. **Deliberately NOT locked here (cross-story consistency, no PO input needed for this specific call):** the mockup's specific replacement glyph for S7's note-toggle (a colorful "sticky note" emoji, 🗒️) is not being locked via this story's row — `s7-note-icon-picker.html` is a separate, dedicated, more-targeted decision-tool redo already in flight for that exact glyph, prompted by the PO's own later, more specific stated preference to avoid colorful emoji-style pictographs in favor of plain Unicode symbol/dingbat glyphs (see PLAYBOOK_UPDATES_PENDING.md). Locking "sticky-note emoji" here would risk contradicting that in-flight, purpose-built process for the identical icon; S7's own row/that separate mockup remains the source of truth for what specifically replaces the pencil on notes — this row only locks that the pencil itself moves to become this story's edit-icon, not what S7 gets instead. Whatever S7 ends up with, it must not conflict with the row's existing nested-control precedence (whole-row cross-off tap, note-toggle, aisle-toggle, delete, and S13's drag-pickup gesture) — **and, per QA finding M16, 2026-09-08 (folded in directly, no PO input needed — this cross-reference pointed nowhere until now):** tapping this story's own dedicated edit-icon performs ONLY the edit-open action and must NOT also trigger the row's whole-row cross-off toggle, same nested-control-precedence rule already established for every other per-row control (S2's original rule, restated on S3/S5/S7/S8/S13 for their own controls) — now actually stated for this story's edit-icon too, not left as a dangling reference. **QA finding M17, 2026-09-08, folded in directly (no PO input needed):** this edit-icon is also a nested per-row icon control, so it's in scope for S14's ~25% shrink, same cross-reference treatment S16's icon already got on S14's own row (see S14's row for the mirrored note) — since S14 shipped before this icon existed, Developer applies the shrink to it at S15's own implementation time, not retroactively to S14. Tester's testability-check independently re-flagged the icon-pairing-needs-PO-confirm point before this confirmation landed — not a new issue, already closed. **QA gate finding R10, 2026-09-08 — resolved by PO decision:** this story's own headline example — renaming "zucchini" to "2 zucchini" — can make the OLD name "zucchini" resurface as an S10 "what am I missing?" suggestion chip, even though the user still has that exact item, just renamed. Not a bug in S10 or S15 individually — an interaction between two independently-correct pieces of logic (S10's presence-filter checks the live list by name; the renamed item's old name is no longer literally present under that name). **PO's own words, 2026-09-08: "i like the first option."** Accepted as-is — this is a KNOWN, ACCEPTED, low-stakes behavior, not a bug to fix or flag later: a renamed item's old name can transiently reappear as a suggestion chip; worst case, tapping it re-adds the old name as a harmless duplicate row, itself undo-recoverable via S6. No additional logic required — S10's counter-fragmentation/identity question stays exactly as already scoped (edits still don't touch S10's counter in either direction, per the Developer sanity-check resolution above). Resolved in QUESTIONS.md's Blocking table. **Status, 2026-09-08 (scrum-master's call, same precedent as S13/S14/S16/S17):** Tester's re-check already confirmed the three testability-check fixes above hold up (per the combined S13/S15/S16 re-check pass), and QA's gate already ran (finding R10, now resolved). With the icon-pairing now also explicitly PO-confirmed, the only remaining step before Lock is QA's final Lock-gate re-read — the same one-more-look S13 got after its own last fix landed — to confirm zero new contradictions across R10's resolution and this icon-pairing confirmation; not flipping Status to Locked without that step, consistent with this project's own precedent of never skipping QA's final sign-off. **QA's final re-read landed clean, 2026-09-08 (same day, continued):** R11/M16/M17 all confirmed resolved, no new contradictions found across all 6 rounds of fixes this story went through — no objection to Locking. **Locked, 2026-09-08 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA gate, including this final re-read), clear for Developer implementation, sequenced after S13/S14 per this story's own AC.** **Sequencing + restyle-scope note, 2026-09-09:** Developer is currently mid-implementation on this story and finishes it first; the two new 2026-09-09 stories (S19 revert-to-Up/Down, S20 button restyle) are sequenced AFTER it, so their AC assumes this story's edit-icon already exists in the row. This story's edit-icon is itself in scope for S20's frameless/glyph-fills-footprint restyle, same as it's already in scope for S14's ~25% shrink (M17 above) — Developer applies both treatments to it at this story's implementation time. **Implemented, 2026-09-09 (Developer, pushed sha `10c804e`):** built by reusing S7/S8's shared editor infrastructure (`editingField`/`openEditor`/`commitEditor`) rather than a parallel build — one of the two approaches Tester's testability-check explicitly left to Developer's choice, with both behavioral guarantees required either way (draft survives an unrelated re-render; opening any other editor, or a drag-pickup elsewhere, commits it). Developer self-verified 33/33 new checks + the full 219/219 regression suite re-confirmed clean. **Status → In Review** (from Locked), same convention S7/S8/S9/S10 used for "implemented + self-verified, Tester's formal pass now underway" — reserving Done for Tester's own independent formal-pass citation, per this project's standing rule that self-verification (however thorough) is never itself sufficient for Done; Tester's formal pass is running now. **Always-visible edit-icon crowding effect (Developer-flagged, tracked, not a regression):** because item names are never empty, this edit-icon — unlike the note/aisle toggles, which render only while their field is empty — is always present, adding one icon to every row's control set; this shifts the specific icon counts cited in S7/S8's crowded-row closure evidence (Axis A 1→2, Axis B 3→4). Row height/overflow still pass clean and the PO has accepted the crowding, so it does not reopen that gate or R7 — handled as a point-in-time-count softening on S7/S8's own rows and the top-of-file Gate CLOSED note (dated 2026-09-09 addendum there), not re-counted now since S19/S20 will change the row again. **Done, 2026-09-09 — Tester's independent formal pass: 246/246 assertions passed (219 regression baseline re-confirmed + 27 new S15 checks; arithmetic 219 + 27 = 246), zero defects — Tester's citation of record, superseding this story's Locked/In-Review states, per the playbook's independent-formal-pass-supersedes-self-check rule (Developer's own 33/33 self-verification is thereby no longer the citation for this slot). **Regression-ledger citation, 2026-09-09:** this 246/246 IS REGRESSION_LOG.md's current canonical row (marked "cite this row," inline arithmetic 67+81+26+45+27=246, superseded-chain intact back through 219/216/154/74) — an earlier note here flagged the row as not-yet-added, which was a stale read during work concurrent with Tester's own edits; nothing is owed there, cite REGRESSION_LOG.md's 246 row as the canonical figure.** | Done | 3 |
| S16 | As a user, when I'm sorting my list by aisle, I want the redundant per-row aisle tag hidden, so that rows don't take up an unnecessary second line when the aisle is already shown as the group header. | **Drafted 2026-09-08, PO decision from the crowded-row review, follow-on to S9:** PO's own words — "we suppress the up/down arrows when sorting by anything other than manual, but we don't suppress the aisle tags when sorting by aisle... now that aisle is on a second row per item even if there aren't any notes, we should suppress that to save screen space." When By-Aisle sort (S9) is active, an item's own per-row aisle tag (S8) is suppressed/hidden — the aisle is already conveyed by the group header. In every OTHER sort mode (Manual, Alphabetical) the aisle tag continues to render on the row exactly as S8/S9 already specify — this story's suppression is scoped to By-Aisle sort mode only. **Developer sanity-check finding, 2026-09-08, folded in directly (no PO input needed):** this suppression applies to the passive display tag only — the aisle-edit affordance/editor itself (S8) must continue to work in every sort mode exactly as S9 already guarantees ("check/uncheck, delete, and note/aisle editing remain fully available in every sort mode"); hiding the passive tag's rendering must NOT disable or hide the ability to open/use the aisle editor while sorted By Aisle. **Tester's testability-check landed, 2026-09-08 — a genuine self-contradiction, not just a gap, resolved directly here (no PO input needed):** in the actual implementation, the per-row aisle tag is currently the ONLY tap-to-edit affordance once an aisle is set — there is no separate always-present edit icon the way S7's note field has. Suppressing that tag's rendering therefore removes the sole entry point to reopen its editor, directly contradicting the guarantee just above that the editor keeps working in every sort mode. **Resolution (adopts Tester's candidate fix, scoped narrowly):** while sorted By Aisle specifically, and ONLY in that sort mode, render a minimal icon-only "edit aisle" affordance in place of the full text tag (which stays suppressed) — tapping it opens the same aisle editor, pre-filled with the item's real current value (or empty, if Unassigned), exactly as if the full tag had been tapped in any other sort mode. In every OTHER sort mode (Manual, Alphabetical), the existing full tag-is-the-tap-target behavior is completely unchanged — this fix touches By-Aisle mode only. Being icon-only rather than the full value text, this affordance is also small enough that it should not force the second-line row growth this story is trying to avoid in the first place — actually reinforcing S16's own space-saving goal rather than just patching the contradiction. **QA gate findings, 2026-09-08, both folded in directly (cheap Minors, no PO input needed):** (M13) whether Unassigned items reuse S8's separate empty-state "add aisle" icon or this story's new icon — resolved: they use the SAME new icon-only affordance as populated items, not S8's separate empty-state icon; one consistent icon for the whole column while sorted By Aisle (whether the item is Unassigned or already has a value), rather than two different icons appearing side-by-side within the same view depending on each row's state. (M14) cross-referencing S14's shrink-target control list: this story's new icon-only affordance is itself a nested per-row icon control, so it is also subject to S14's ~25% shrink — added the cross-reference on S14's own row (dated technical note, since S14 is already Locked; this is an additive clarification, not a reopening). A row's note (S7), if present, is unaffected by this story and continues to render/grow the row as already specified. Row height still follows the existing content-driven rule (S7/S8): a row grows only for content it's actually displaying. View-only rendering concern, same category as S9 itself — never touches S6's undo buffer, never mutates the underlying stored item array. **Sequencing decision (scrum-master's call, no PO input needed):** drafted as its own follow-on story rather than folded into S9's already-in-review AC, specifically so Tester's just-started formal pass on S9 can proceed against the implementation that actually exists today without testing a moving target — see S9's own forward-reference note. Developer sanity-check landed, 2026-09-08 — one gap found and folded in directly above; Tester confirmed the self-contradiction fix holds up to deterministic testing. **QA gate landed clean, 2026-09-08 — zero Real findings, two cheap Minors (M13, M14), both folded in directly above.** **Locked, 2026-09-08 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA gate), clear for Developer implementation.** **Done, 2026-09-08 — Tester's independent formal pass: 180/180 assertions passed (74 Sprint-1 + 80 Sprint-2 re-confirmed clean + 26 new S14/S16/S17 checks, TC16.1-16.12 for this story specifically, including confirmation the self-contradiction found at testability-check is genuinely resolved by the shipped icon-only affordance), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/180-total row (script `vopping-tests-tester-s14-s16-s17-formal.js`), current canonical citation. Implementation pushed sha `606285f`. Icon-glyph swap also landed, 2026-09-08 (commit `8724733`): the PO delivered their hand-picked glyph via a Developer-built decision-tool mockup (`s16-aisle-icon-picker.html`) and chose U+2691 (BLACK FLAG) with an accent-color treatment scoped to this icon specifically — no longer a placeholder, this is the PO's real final pick.** **Cross-reference, 2026-09-09 (additive, not a reopening):** this story's icon-only aisle-sort affordance (U+2691) is in scope for new story **S20**'s frameless/glyph-fills-footprint restyle (remove the square outline, scale the glyph to fill), same as every other per-row icon control — its accent-color treatment stays as-is and remains Okabe-Ito-safe; see S20's row. **Cross-story supersession, 2026-09-10 (superseded by S22/M23 — story STAYS Done, S19→S13 convention):** the PO confirmed 2026-09-10 that in By-Aisle compact mode the per-row persistent inline aisle select replaces this story's hand-picked icon-only ⚑ (U+2691) affordance — so the ⚑ glyph, the .aisle-sort-icon color rule, and the .aisle-tag second-line element become DEAD CODE to remove as part of S22 (M19/M20 dead-code-cleanup precedent). This story's original delivery (the ⚑ pick and its accent styling) stays Done as shipped; it is superseded, not reverted. See S22's S16 ripple and the M23 confirmation. **Further superseded (presentation), 2026-09-11 (S26 Locked — cross-story supersession per playbook, S19→S13 convention; S16 STAYS Done):** S26 moves the per-item aisle affordance to an icon-footprint native `<select>` on the PRIMARY line in ALL sort modes INCLUDING By-Aisle — so this story's compact affordance, already once superseded (⚑ → inline select, S22/M23), is now further superseded to that primary-line icon-footprint select. Critically, honoring THIS story's own hard-won lesson (the edit-affordance-must-work-in-every-sort-mode self-contradiction S16 resolved), S26 does NOT suppress the aisle icon in By-Aisle mode — the icon stays present in every mode, preserving the edit-in-every-mode guarantee; the previously-redundant full-select-under-a-group-header just goes away. S16's shipped delivery (the ⚑ pick + its styling) stays Done; only the mechanism moved. See S26. | Done | 3 |
| S17 | As a user, when I'm sorting my list by aisle, I want the aisle group headers to stand out more clearly from item rows, so that I can tell at a glance where one aisle's group ends and the next begins. | **Drafted 2026-09-08, PO decision from the crowded-row review, follow-on to S9:** PO's own words — "it's just hard to see the different aisle headers... maybe more space and/or bold and/or font size and/or different font? i'm not picky." PO explicitly delegated the exact combination of treatment (spacing/font-weight/font-size/font-family, or some mix) — Developer's call, not locked to one specific combination; no decision-tool pass needed for this one (unlike S13/S15), since the PO explicitly said "i'm not picky." If any color-coding is introduced as part of this treatment, it must use a colorblind-safe palette — default to Okabe-Ito, per the project's already-settled top-of-file Accessibility scope note; plain non-color treatment (spacing/weight/size/font alone) also fully satisfies this story, color is not itself required. **Developer sanity-check finding, 2026-09-08, folded in directly (free related fix, no PO input needed):** aisle group headers must NOT inherit the tap-row cursor/active-state affordances (e.g. pointer cursor, tap-highlight/active-state styling) that item rows use for their whole-row cross-off tap target — headers are not clickable/tappable rows themselves, and visually implying otherwise is a latent UX bug worth closing alongside this story's main visual-distinction work. View-only rendering concern, same as S9/S16 — never touches undo, never mutates stored data. Same sequencing rationale as S16: drafted as a follow-on to avoid disrupting Tester's just-started S9 formal pass; independent of S16, can proceed in parallel with it. Developer sanity-check landed, 2026-09-08 — no AC gaps, one free related fix folded in directly above; Tester's testability-check landed clean too, 2026-09-08 — no gaps, sent straight to QA's per-story gate. **QA gate landed clean, 2026-09-08 — zero Real/Minor/Nitpick findings.** **Locked, 2026-09-08 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA gate), clear for Developer implementation.** **Tracked, non-blocking, 2026-09-08:** PO, unprompted, wants to hand-pick the actual glyph for this story's new icon-only "edit aisle" affordance themselves (same as they're doing for S15's edit icon) — their own words: "we probably need to make a change to the aisle button too. i'll try to find an icon to use." NOT a Lock- or implementation-blocker — Developer proceeds now with a placeholder glyph and swaps it once the PO delivers a pick; tracked here so the icon showing as provisional isn't a surprise, and so this commitment doesn't silently evaporate before the swap actually happens. **Tracked note resolved, 2026-09-08 (same day):** the PO delivered their hand-picked glyph via `s16-aisle-icon-picker.html` and Developer swapped it in (commit `8724733`, U+2691 BLACK FLAG) before this session paused — no longer a placeholder; see S16's own row for the full citation. **Done, 2026-09-08 — Tester's independent formal pass: 180/180 assertions passed (74 Sprint-1 + 80 Sprint-2 re-confirmed clean + 26 new S14/S16/S17 checks, TC17.1-17.6 for this story specifically), zero defects, zero regressions — see REGRESSION_LOG.md's 2026-09-08/180-total row (script `vopping-tests-tester-s14-s16-s17-formal.js`), current canonical citation. Implementation pushed sha `606285f`.** | Done | 3 |
| S18 | As a user, I want to collapse/expand an aisle's group when sorted by aisle, so that I can navigate my list faster by hiding aisles I've already finished with. | Not yet scoped in detail — no locked AC. PO's own words, 2026-09-08: "also also collapsing by aisle will [be] necessary — that can be a stretch goal. just helps navigate a little faster." Explicitly PO-designated as lower priority/stretch, same treatment as S11 — parked, not built now. Likely depends on S9 (By-Aisle sort) existing first if/when picked up. **Explicitly out of scope for the current backlog/sprint — do not build.** Revisit only if the PO asks for it after living with S9/S16/S17's aisle-sort view for a while. | Not Started | Unscheduled — parked, low priority, future |
| S19 | As a user, I want to reorder items with Up/Down buttons again instead of drag-and-drop, so that reordering actually works on my phone — where the drag gesture loses to the browser's native scroll. | **Drafted 2026-09-09, PO decision after a SECOND real-device drag failure — reverts S13's interaction mechanism back to S5's Up/Down buttons.** PO's own words: "the drag and drop isn't working anymore... when i try to drag the item, it scrolls the screen instead of moving the item... i think we need to go back to the up/down arrows. i know it will result in the buttons being a bit crowded, but i'll take crowded over not working. from some testing, it looks like i'll still have about 20 characters even with two up/down buttons and the edit button." **Scope:** remove S13's drag-and-drop interaction entirely — the press-and-hold pickup + delay/jitter arming, the live placeholder, the auto-scroll-near-edge mechanic, the Pointer-Events drag machinery, and the drag-only CSS (`touch-action: none`/`.drag-active`/`.dragging`/`.drag-placeholder`); the whole-row cross-off tap (S2) is unaffected and remains the row's tap behavior. **Restore S5's per-row Up and Down buttons exactly as S5 specified:** clicking Up/Down swaps the item with its immediate neighbor; the top row's Up and the bottom row's Down are disabled/no-op; each swap persists the manual array order to localStorage immediately and survives refresh; each single swap is one of S6's four undo-eligible action types and undo restores the exact prior order. **Data model unchanged — this is a UI/interaction reversal only, not a data change:** S13 never altered the persisted manual-order model (it carried S5's forward verbatim), so restoring Up/Down needs no data migration; the reorder/persistence logic is identical to what S5 shipped and S13 preserved. **Nested-control precedence (S2):** tapping Up or Down fires ONLY that swap, never the row's whole-row cross-off toggle — same rule S5 originally had and every other nested control follows. **Non-Manual sort (S9):** Up/Down are hidden/disabled whenever a non-Manual sort is active and reappear in Manual — the same manual-order-only rule S9 always described (it predates S13; S13 had merely swapped in its drag equivalent), now reverting to govern Up/Down again; S9's AC clause re-points to this story on ship (see S9's forward-reference note). **Crowding is an explicit, PO-accepted tradeoff, NOT a reopening of the R7 / Tracked-follow-up-#2 crowded-row gate:** restoring Up/Down alongside S15's new edit button reintroduces row crowding that S13 (removing Up/Down) and S14 (shrinking controls) had reduced — but unlike R7 (where the gate existed because the PO had not yet seen/approved a crowded row), here the PO has directly experienced the alternative, seen the crowding, and explicitly chosen it ("i'll take crowded over not working"), confirming via their own testing that ~20 characters of item-name space remain even with both Up/Down buttons and the edit button present — same PO-owned-accepted-tradeoff handling as S14's sub-24px controls, not a defect to re-flag. S20 (drafted alongside this story) is the PO's own mitigation for this reintroduced crowding. **Auto-scroll tradeoff, subsumed in the PO's decision (not a new open question):** S13's PO-required auto-scroll-during-drag (for reordering across a long list without releasing) goes away with the drag machinery; moving an item a long distance now means repeated Up/Down taps, the same friction that originally motivated S13 — the PO has weighed reliability over that convenience and chosen Up/Down regardless, noted for the record so it isn't rediscovered later as a regression, not routed back as a fresh question. **Sequenced after S15** per the Orchestrator: Developer finishes S15's in-flight edit-button work first; this story's AC assumes S15's edit-icon already exists in the row. **Escape hatch (per Orchestrator):** this revert is the PO's decision UNLESS Developer's separate, parallel root-cause diagnosis of the real-device scroll-collision (likely a `touch-action`/non-passive-listener/`preventDefault` interaction desktop Chromium structurally can't reproduce — S13 passed 219/219 there) proves it trivially fixable AND the PO, shown that, reconsiders — in which case this story would be parked and S13's drag retained. Proceeding with the revert regardless in the meantime; the diagnosis is not a blocker on this story's own doc pipeline. **Escape hatch CLOSED — decision final, 2026-09-09:** Developer's diagnosis came back non-trivial (iOS `touch-action` latched at `touchstart` — the engine commits to native scroll before the pickup-delay timer can claim the gesture; desktop Chromium doesn't, hence S13's green 219/219 there), and the PO — shown the one drag-preserving alternative, a dedicated drag-handle at one icon fewer than restored Up/Down (5 vs 6) — explicitly declined it and chose the proven Up/Down mechanism, having been burned by drag twice. This story's revert therefore proceeds UNCONDITIONALLY — no longer parked-if-trivial; drag-and-drop is out for good. **Pipeline, 2026-09-09:** Developer's sanity-check is done; Tester's testability-check is next (Orchestrator routing it now); this story stays Not Started until Tester + QA both clear it, per this project's standard no-Lock-before-those-two rule. S5's 7 retired test cases (see `test-plans/S5-reorder-buttons.md`) become behaviorally relevant again — Tester's call whether to un-retire or author fresh S19 cases. **Status: Not Started** — needs Developer sanity-check + Tester testability-check + QA gate before Lock, same pipeline every story on this project has gone through (no shortcut for a revert). **Developer sanity-check landed, 2026-09-09 — findings folded in (scrum-master's triage: AC-worthy requirements and verification items locked here; pure implementation-approach choices left to Developer per this project's standing convention):** **(a) Worst-case-row verification requirement (Real):** restoring Up/Down alongside S15's edit-icon brings a both-fields-EMPTY item to SIX primary-line icons (note-toggle + aisle-toggle + S15 edit + Up + Down + delete) — one more than R7's original "5 controls," since S15's edit-icon postdates the PO's crowding experience; Developer's rough math (6 × 19.5px + gaps/padding at 320px) lands near ~19-20 chars of item-name space — plausibly fine but genuinely tight. Tester's formal pass MUST measure the true 6-icon empty-fields row at 320px (control count, row height, no horizontal overflow), not infer it — and this measurement doubles as the deferred definitive re-measurement of the S7/S8 crowded-row closure evidence softened 2026-09-09 (see those rows / the top-of-file Gate CLOSED addendum). A verification requirement, not a blocker; the crowding itself remains the PO-accepted tradeoff already recorded above. **(b) Removal-completeness — remove ALL drag-only artifacts, not just the machinery named earlier, or they orphan as dead code / latent bugs:** also remove `suppressNextClick` + `armClickSuppression()` + the click-listener's suppress guard; the document-level `pointerup`/`pointercancel` M18 fallback listeners; and the `#list-root.sort-manual .items li { cursor: grab }` rule. **(c) `user-select` decision (conscious call, no PO input needed):** the plain `user-select: none` on the row STAYS (it's S1/S2's whole-row-tap selection guard, predates S13, drag-independent); the drag-motivated `-webkit-user-select: none` / `-webkit-touch-callout: none` additions (sha `50c57c8`) are KEPT but re-scoped as general whole-row tap-target selection/callout suppression (a long-press on a tap-only row shouldn't pop iOS text-selection or a callout menu either) rather than drag-collision-specific — dropping them would re-enable that minor quirk for no benefit. Consequence: the iOS-fix's previously-tracked-non-blocking real-device re-test is now MOOT — no drag press-and-hold remains for the iOS long-press gesture to collide with. **(d) Undo shape — Developer's implementation choice, NOT locked here:** the existing `performUndo()` "reorder" handler (`{id, fromIndex, toIndex}`, splice-out/splice-in) can be reused directly for a neighbor swap, and S5's old `{idA, idB}` swap shape has no remaining producer — but that's an implementation-plan choice, same treatment this project gives every Developer technical-approach finding; what's LOCKED is only the behavioral guarantee (each swap is one undo-eligible reorder action; undo restores the exact prior order), satisfiable either way. **(e) Downstream (Tester's call, noted):** S9's TC9.8/TC9.9 (already rewritten once for the drag era) will need rewriting AGAIN to gate on Up/Down button visibility instead of drag-arm probing — a real consequence of this revert, flagged so it's not a surprise at formal pass. **Tester's testability-check now also landed, 2026-09-09 (superseding the "testability-check is next" note above): AC verified fully testable, one clarification folded in — (f):** the PO-accepted crowding does NOT waive the locked no-horizontal-overflow row guardrail (S1/S2's, checked at 320/360/375/390px with flex-wrap permitted) — that guardrail stays the deterministic pass/fail line for the reintroduced crowding, while the PO's "~20 chars" figure remains an accepted-tradeoff observation, not a per-character assertion Tester must verify. **QA per-story gate landed clean, 2026-09-09 — zero Real findings (QA verified against the real `script.js`/`style.css`, not just AC prose).** QA notably confirmed this revert structurally ELIMINATES the C1/R13 stale-DOM-reference bug class rather than reintroducing it — Up/Down is a synchronous click→swap→`render()` with no DOM reference carried across a render — and that the cross-row editor-commit-on-reorder guarantee is inherited automatically via the existing `focusout` path (no gap to build). **M19 folded in directly (removal-completeness, no PO input needed):** removal finding (b) above names deleting the drag-era `#list-root.sort-manual .items li { cursor: grab }` CSS rule, which QA confirmed by project-wide grep is the ONLY consumer of the `sort-manual` class anywhere — so the `sort-manual` class-toggle in `script.js` (~line 1229) becomes orphaned dead code once that rule goes, unless repurposed. Decision named (exact approach is Developer's, but the orphan must not survive): if the restored Up/Down visibility is JS-gated in `renderRow` per sort mode, REMOVE the `sort-manual` toggle entirely as dead code; if Developer instead CSS-gates that visibility, KEEP the toggle and repurpose it to drive the Up/Down show/hide — either way no orphaned class-toggle is left behind by this revert. **Locked, 2026-09-09 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA per-story gate, zero open findings), clear for Developer implementation; sequenced first of the S19/S20 pair per the reorder-then-restyle ordering, though `.icon-btn` being a shared class means the S19/S20 implementation order isn't load-bearing.** **Implemented + Done, 2026-09-09 — Tester's independent formal pass: 222/222 assertions passed, zero defects — citation of record (REGRESSION_LOG.md's new canonical row; the count dropped 246→222 BY DESIGN as the retired drag-era `s1-s13` suite's removed-drag cases were retired and S9's TC9.8/TC9.9 were rewritten back to gate on Up/Down visibility — a planned retrofit, not a regression, the same cross-story-supersession bookkeeping S5→S13 already did in the other direction).** The authoritative 6-icon worst-case row (both fields empty: note-toggle + aisle-toggle + S15 edit + Up + Down + delete) FITS CLEANLY at all four target widths (320/360/375/390px) — zero horizontal overflow, no wrapping — satisfying finding (a)'s verification requirement AND discharging the deferred S7/S8 crowded-row closure re-measurement (that loop is now fully closed against real shipped code, not inference). **QA post-implementation adversarial review considered and deliberately NOT made a Done gate (scrum-master's call):** unlike S13 — a novel, complex drag interaction where the post-impl code trace caught C1 — this is a revert to S5's already-proven synchronous Up/Down mechanism, QA already confirmed pre-lock it structurally eliminates the C1/R13 class, and S14/S15/S16/S17 all reached Done on Tester's formal pass alone; Done is therefore not gated on a QA pass. Separately requested a NON-BLOCKING QA code-hygiene spot-check as a fast follow-up (see SPRINT_LOG) to confirm the removal-completeness clauses (finding (b) / M19 / M20 / N9 — orphaned `sort-manual` toggle, the document-level M18 fallback listeners, dead CSS declarations) were actually honored, since a functional formal pass structurally can't see leftover dead code; any leftover is a cleanup task, not a Done-reversion, unless it proves a genuine latent bug (e.g. the M18 global listeners still firing). **S19 → Done.** | Done | 3 |
| S20 | As a user, I want the row control buttons to look bigger and be easier to tap without the row getting any taller, so that the row stays comfortable even after the Up/Down buttons come back and crowd it. | **Drafted 2026-09-09, PO's own idea to ease the crowding S19's revert reintroduces.** PO's own words: "for all the buttons, instead of having a tiny icon in a square button, make the icon the size of the button and remove the square outline. it will give the appearance of a bigger button without the size of the button actually changing." **Scope:** for every per-row icon control — Up and Down (S5/S19), delete (S3), note-toggle (S7), aisle-toggle (S8), S15's edit-icon, and S16's icon-only aisle-sort affordance (the full `.icon-btn` set) — remove the square button outline/chrome (border, background, box), and scale the glyph up to fill the button's existing footprint. **Net effect (the PO's whole point):** the glyph appears larger and the control reads as a bigger, more tappable target WITHOUT increasing the button footprint or the row height — the footprint stays exactly whatever S14's ~25% shrink left it. **Relationship to S14 (does NOT undo it):** this story keeps S14's footprint reduction intact and only changes the chrome (removed) and the glyph size (enlarged to fill) within that same footprint — complementary to S14, not a reopening; cross-referenced on S14's own row. **Out of scope — the text tags:** the populated-state `.note-display`/`.aisle-tag` affordances are text, not "a tiny icon in a square button," so the PO's instruction doesn't apply to them — same boundary S14 already drew (tag chrome excluded); this story touches the icon-button controls only. **No functional/behavioral change** to any control's action — visual/styling only, same framing as S14. **No decision-tool mockup needed:** unlike S13/S15's gesture/icon picks, this is a direct, unambiguous PO instruction (remove the outline, fill the footprint with the glyph), not an open "I'm open to ideas" subjective call — same treatment as S16's direct instruction and S17's "I'm not picky" delegation. **Accessibility:** removing the square outline removes a visual button boundary — the enlarged glyph must still read clearly as an actionable control, and if any glyph carries a color treatment (e.g. S16's accent-colored U+2691 flag) it stays colorblind-safe/Okabe-Ito per the top-of-file Accessibility scope note; plain non-color glyphs also fully satisfy this story. Tap-target footprint is unchanged (already sub-24px post-S14, a PO-accepted tradeoff) and a larger-appearing glyph can only help effective tappability, so no new tap-target concern beyond S14's already-accepted one. **Sequenced after S15** (and pairs with S19, same as S14 paired with S13): Developer finishes S15's in-flight edit-button work first, so this story's AC assumes S15's edit-icon exists to be restyled. **Status: Not Started** — needs Developer sanity-check + Tester testability-check + QA gate before Lock. **Developer sanity-check landed, 2026-09-09 — findings folded in:** **(a) Uneven-rendering caveat (Real, expectation-setting):** "make the glyph fill the footprint" with ONE uniform font-size across the mixed glyph set (note/aisle/edit/delete + Up/Down arrows) renders UNEVENLY — glyphs have very different intrinsic ink coverage at the same size, so a single font-size does NOT automatically yield visually uniform apparent sizes; apparent uniformity may need per-glyph size tuning (a Developer-level detail, same delegation as S17's typography), and some residual unevenness may be acceptable — the PO's instruction is remove-the-chrome + enlarge-the-glyphs, not a promise of pixel-perfect uniform glyph sizing. **(b) Footprint/row-height verification requirement:** Tester's formal pass MUST confirm the enlarged glyphs stay within the existing fixed footprint (post-S14, ~19.5px) and do NOT grow row height — the PO's whole promise is "bigger appearance without the size actually changing"; pairs with S19's 6-icon-row measurement at 320px. **(c) Removal-completeness — drop dead declarations:** removing the borders makes the `border-color` declarations on `.icon-btn.delete-btn` and `.icon-btn.aisle-sort-icon` dead (their color distinction survives via `color:` alone), so those must be dropped, not orphaned. **(d) Press-feedback + separation (real-device heads-up, PO-requested so proceeding):** frameless removes the current `button:hover` background press-feedback, and adjacent frameless glyphs on the crowded 6-icon row (S19) may read as harder to separate — so this story must preserve SOME press/tap feedback that survives without a square background (e.g. a subtle `:active` glyph treatment, Developer's exact choice), and a real-device look at legibility/separation is the acceptance signal (per this project's real-hardware standard, not narrow-viewport emulation); a heads-up and a preserve-feedback requirement, not an objection to the PO's chosen direction. **Tester's testability-check now also landed, 2026-09-09 — AC verified fully testable, one clarification folded in — (e):** the soft phrase "scale the glyph to fill the footprint" is pinned to a deterministic pass/fail line — computed font-size materially larger than pre-S20 — with the exact fill ratio a Developer tuning detail (not PO-locked, same as S14's ~25% or S13's constants); and "remove the box" means border + background + box-shadow removed, NOT shrinking the tap target — the footprint-preservation clause holds the control's width/height/min-width at the existing ~19.5px. **QA per-story gate landed clean, 2026-09-09 — zero Real findings (QA verified against the real `style.css`).** **M20 folded in directly (removal-completeness, no PO input needed — and it corrects finding (d)'s premise):** finding (d) said "frameless removes the current `button:hover` background" — QA found that's slightly off: the inherited base `button:hover { background }` rule (`style.css` ~lines 69-71) still matches `.icon-btn` after S20 restyles the `.items .icon-btn` rule, so the hover background is NOT auto-removed and would reappear as a box-like grey highlight on hover, partially contradicting "remove the square outline." S20 must therefore EXPLICITLY remove/override `button:hover`'s background for `.icon-btn`. Low stakes (hover is desktop-only; the PO's phone has none), but a real removal-completeness item — and finding (d)'s actual touch press-feedback concern still stands, served by the `:active`/tap affordance rather than hover. **N9 folded in (dead-declaration tidy, same category as finding (c)):** once the border and background are removed, `.icon-btn`'s `border-radius: 4px` is vestigial — drop it too. **(N10 consciously skipped — confirmed non-issue:** S20 lists Up/Down among its restyle targets while sequencing "after S15"; harmless because `.icon-btn` is a shared class, so implementation order between S19 and S20 isn't load-bearing — whichever ships first, the other's controls inherit the restyle via the shared class; the existing "pairs with S19" language already conveys this.) **Locked, 2026-09-09 — full doc pipeline cleared (scrum-master sanity-check → Developer sanity-check → Tester testability-check → QA per-story gate, zero open findings), clear for Developer implementation, sequenced after S15 and pairing with S19.** **Implemented + Done, 2026-09-09 — Tester's independent formal pass: 222/222 assertions passed, zero defects — citation of record (same new canonical REGRESSION_LOG.md row as S19; the 246→222 drop is the by-design drag-suite retirement, not a regression). Tester verified the enlarged glyphs stay within the existing ~19.5px footprint with no row-height growth (finding (b)) and the frameless restyle holds at all four target widths (320/360/375/390px) as part of the 6-icon worst-case row measurement on S19's row above. The M20/N9 hover-override and dead-declaration removal items ride along with S19's non-blocking QA code-hygiene spot-check (see SPRINT_LOG); any leftover is a cleanup task, not a Done-reversion. **S20 → Done.** **Refined by S27, 2026-09-11 (S27 Locked — cross-story supersession per playbook; S20 STAYS Done):** this story's Developer-finding (a) and its explicitly-accepted residual glyph-unevenness ("some residual unevenness may be acceptable... not a promise of pixel-perfect uniform glyph sizing") is now REFINED by S27, which equalizes the non-note glyphs' rendered HEIGHT toward the note glyph via per-glyph font-size tuning within S20's SAME fixed ~19.5px frameless boxes (glyph-only, no box change — S20's footprint/row-height guarantee is preserved, not reopened). S20's frameless restyle stays Done as shipped; S27 is the follow-on evenness pass the PO asked for after living with it. See S27. | Done | 3 |
| S21 | As a user, I want a settings menu I can open from the top of the app, so that less-frequent actions and future features have a home without cluttering the main shopping screen. | **Pipeline COMPLETE — LOCKED 2026-09-10.** Cleared Scrum-Master + Developer sanity-checks, Tester testability-check, and QA's per-story gate plus a clean Lock-gate re-read (zero Real findings); AC frozen, clear for Developer implementation. Captures the PO's independent, forward-looking endorsement (verbatim: a settings menu is probably a good idea in general, gives us real estate for new features) — drafted as reusable infrastructure in its own right. **Scope:** add a settings entry point to the existing top header (index.html today has a header element with the h1 title and a global-controls group holding Undo / Clear-crossed-off; there is NO settings menu yet) plus the panel or menu shell it opens and closes, able to host feature controls. The panel's first real content is S23 (aisle management); this story delivers the frame, the open/close behavior, and a place to mount controls, not the aisle CRUD itself. **Close mechanism (NB-5):** the panel closes via an explicit close (X) control AND by tapping the scrim / outside the panel; Escape-to-close is optional only (keyboard-only navigation is out of scope per the project accessibility scope), so the formal pass drives close deterministically via the X control or a scrim/outside tap. **Dependency:** S23 mounts inside this shell — S21 should land with or just ahead of S23; independent of S22. **Constraints (locked, project-wide):** fully offline, file://-loadable, plain HTML/CSS/JS with localStorage, phone-first — the entry point and panel must open/close and be usable at the mobile viewport widths the row-layout work already targeted (320/360/375/390px); if the entry point needs a glyph it is a plain monochrome Unicode symbol, never a colored emoji, treated as a PO glyph pick (placeholder now, PO confirms later, same pattern as S16/S7). **iOS focus-zoom guard (carried from the root motivation of this rework, see S22):** any text input this shell introduces must use font-size >= 16px so it never triggers iOS focus-zoom. **Done, 2026-09-10 — Tester's independent formal pass: 268/269 assertions, this story's checks clean; new canonical regression baseline in REGRESSION_LOG.md. The 222→209 total-count delta is BY-DESIGN net-supersession (S8/S9/S16's free-text-input / datalist / ⚑ checks retrofit to the new select / persisted-set mechanism, NOT lost coverage; same convention as S13→S5, S19→S13). The single 269th assertion is Minor defect D1, scoped to S23 only (see S23's row) — it does not affect this story.** | Done | 4 |
| S22 | As a user shopping on my phone, I want to pick an item's aisle from a native dropdown instead of a text field, so that the app stops zooming in (and staying zoomed) every time I set an aisle, and so my aisles are a deliberate, reusable set rather than ad-hoc typed text. | **Pipeline COMPLETE — LOCKED 2026-09-10.** Cleared Scrum-Master + Developer sanity-checks, Tester testability-check, and QA's per-story gate plus a clean Lock-gate re-read (zero Real findings); AC frozen, clear for Developer implementation. **ROOT MOTIVATION (PO):** the current per-item aisle field is an open text input, so mobile iOS zooms to fit it on focus and stays zoomed; a native select fixes this (a native picker does not trigger focus-zoom). **Mechanism change (functionality #2):** the per-item aisle field becomes a persistent per-row native select picking from a defined list; the free-text aisle input, its datalist (index.html aisle-options), and getAislePool()'s live-derived pool are superseded. getAislePool() collapses to return state.aisles; renderAisleDatalist() and the datalist element become dead code to remove. **Option order (NB-1):** the select renders options in state.aisles insertion order (starters-then-first-seen, mirroring the old getAislePool order), NOT alpha-sorted; the no-aisle option (labeled state.unassignedLabel, i.e. Other by default) sits FIRST as the default/unset choice, the real aisles follow in that insertion order, and S24's + Add new aisle… sentinel is ALWAYS LAST (it is an action, not an aisle). This per-item-select order is intentionally DISTINCT from S9's by-aisle GROUP sort, where the Other bucket sorts last. **Persisted aisle set (functionality #3) — Developer data model:** store the list inside the EXISTING state object as state.aisles; parseStoredState() already preserves unknown keys, so NO new localStorage key and it is backward-compatible. **Migration on first load (map-then-seed ORDER matters):** if state.aisles is not a valid array, FIRST remap any item whose aisle normalizes to the literal default label constant Other (compare against the hardcoded Other constant, NOT state.unassignedLabel — that label state does not exist yet at first migration) to '' — this MUST run BEFORE the seed union, else the union would pull those Other-normalizing values right back in — THEN seed state.aisles = the real starter aisles (Produce, Dairy, Meat/Seafood, Bakery, Frozen, Pantry, Beverages, Household — Other excluded, see N11 below) UNION every distinct remaining item.aisle value, deduped case-insensitively via S8's normalize (trim + toLowerCase; starter casing wins on collision, else first-seen item casing). Migration adds state.aisles and MAY narrowly rewrite items (ONLY the Other→'' remap) — it never rewrites nextId or any other item field. **Persist choice (N13):** persisting the seeded state.aisles immediately after migration via one saveState is a CONSCIOUS, stated choice (the set is durable from first load); idempotency holds either way, because the seed is deterministic from the same inputs — a re-derivation on a later load would produce the same set. **Idempotency (NB-7):** the testable assertion is the RESULT — a reload after migration does NOT re-mutate state.aisles or items (saveState call-count is not cleanly observable black-box, so assert on end-state stability, not call count); once seeded the set is AUTHORITATIVE, never re-merged with the starter list or re-scanned from items on later loads (a later-deleted starter must not resurrect). **Dangling-value guard (the real breadth fix):** the per-item select matches an item's current aisle to its option by NORMALIZED-KEY (normalize(optionValue) === normalize(item.aisle)), NOT exact string — mirroring S9 grouping and getAisleDisplayMap — so pre-existing case/whitespace variants from free-text S8 (e.g. an item that stored produce, or a trailing-space Bakery) resolve to the seeded canonical option instead of a dangling select; the change-handler then writes the chosen option's canonical display string back to item.aisle, canonicalizing on first edit. With normalized selection, ONLY Other-normalizing values need the explicit migration rewrite above; all real-aisle case/whitespace variants resolve with no item-data change (exact-string selection was rejected — it would force canonicalizing every item.aisle at migration, a far broader rewrite). Items store their aisle as a STRING/by-value (not an id), so S23 rename cascades over items. **Other / no-aisle state (functionality #4):** aisle==='' stays the canonical no-aisle value; Other is only the LABEL for that intrinsic bucket, held in state.unassignedLabel (default Other). **CONDITIONAL exclusion (N11):** Other is excluded from the assignable set ONLY while it is serving as the no-aisle bucket label — this is NOT absolute: per S23, once the user deletes the Other label (bucket falls back to Unassigned) they MAY re-create a real, assignable Other aisle. The intrinsic no-aisle STATE cannot be deleted; the select always offers it as the FIRST option, labeled state.unassignedLabel (Other by default, or the built-in Unassigned fallback once the label has been deleted per S23). **Group-header label invariant (R15):** the By-Aisle group-header label lookup flips from items-derived (can't miss) to state.aisles-derived (can miss) — add an explicit cross-story invariant that the header label ALWAYS resolves, with a defensive fallback to the raw item.aisle string (same posture as R1 / parseStoredState) so a missing key can never render the literal string undefined. **iOS guard:** the native select is the fix; also set the select font-size >= 16px belt-and-suspenders, and every text input in this rework uses >= 16px (S21/S23/S24/S25). **Row placement + overflow re-check (M24):** the per-row aisle select is now ALWAYS present (unlike the old affordance that showed only when empty, or the tag only when set), so a placement for it in the row must be specified (Developer finalizes exact position within the row's control/meta region) AND a worst-case-row overflow re-check is REQUIRED at 320/360/375/390px — the S19/S20 layout gate closed BEFORE this always-present control existed, so this re-check follows the R7/M14/M17/S19 precedent rather than assuming the old measurement still holds. **Real-device vs desktop-pass line (NB-6):** BOTH the on-device iOS zoom-elimination AND the native picker overlay opening on tap of the aisle select are tracked as non-blocking REAL-DEVICE confirmation signals, NOT desktop formal-pass gates (same discipline S13's iOS issue taught) — the desktop formal pass verifies only the structural proxies: the field is a select, computed font-size >= 16px on every new text input and on the select, the option set/order is correct, the worst-case row does not overflow at the four widths, and (per the resolved M23 decision) the By-Aisle compact affordance renders correctly. **Ripples (cross-story supersession per playbook — old stories stay Done, reciprocal dated notes added to those rows when this Locks):** S8 = input→select is a COMMIT-MODEL change (a select commits on change, immediately — not on blur/Enter), so the aisle path moves OUT of the shared text-editor machinery into a new change handler; handler-wiring cleanups (S22 scope): remove aisle-input from the input / keydown-Enter / focusout text branches, and drop the aisle-toggle→openEditor(id,'aisle') click branch; the persistent select with a data-role re-focuses cleanly via the existing focus-restore (setSelectionRange is already guarded to INPUT/TEXTAREA, so no special-casing). Unthreading the aisle editingField type is the LARGEST structural ripple — scope deliberately, leave note/name on the text-editor infra. S9 = the by-aisle group label becomes state.unassignedLabel (Unassigned shows only after the label is deleted). S16 = the icon-only edit-aisle affordance can no longer open a text editor. **CONFIRMED (M23, PO confirmed 2026-09-10):** By-Aisle COMPACT mode shows the SAME persistent inline aisle select as the other sort modes, SUPERSEDING S16's hand-picked icon-only ⚑ flag — the PO confirmed 2026-09-10 that the per-row inline select replaces the ⚑ in By-Aisle mode; the ⚑ / .aisle-sort-icon / .aisle-tag accordingly become dead code (cleanup tracked in S16's supersession note). Rejected alternative (on record): an icon affordance calling select.showPicker() with a select.focus() fallback — showPicker() is fragile on file:// (our deployment) and the focus() fallback does not actually open a picker, so the inline select is preferred. Either way the aisleSortCompact branch AND the second-line aisle-tag suppression currently key off isEditingAisle, which disappears once aisle leaves editingField, so that rendering logic must be reworked, not just an element swap. **R14 — cross-editor commit safety (folds in the Developer's mitigation as testable AC + deterministic tests):** gate the DEFERRED focusout-commit so it SKIPS commit/render when document.activeElement is an aisle select inside listRoot (a picker is open); the aisle select's change handler captures id+value FIRST, then commits any open editor (mirrors openEditor's commit-first guard around script.js :524-526), then calls saveAisle (safe — change fires only after the picker closes); ALSO add the aisle-select role to the focusout scheduling branch so a dismiss-without-choosing still flushes a pending note/name editor (guarded by editingField === pending). REJECTED alternative and why: committing synchronously on the select's pointerdown/mousedown would render() away the very select being tapped — the mirror image of the 2026-09-08 focusout bug. **Deterministic first-interaction test (add to S22 AC):** open item B's note editor and type milk (leave it open) → selectOption Dairy on item A's aisle select → assert ON THE FIRST INTERACTION that A.aisle normalizes to Dairy AND B's milk note is committed/persisted; companion case: dismiss A's picker via Escape without choosing → assert A unchanged AND B's milk still committed on blur. Automation caveat: Playwright selectOption drives the change path deterministically (the durable regression guard); add ONE scripted real-device step holding a native picker open ~2s to exercise the activeElement gate against a genuinely long-lived picker. **QA non-blocking test note (residual, NOT a defect):** the narrow abandoned-note-draft case — open a note editor → tap another row's aisle select → dismiss the picker WITHOUT choosing → abandon the session — leaves the note draft uncommitted until the next interaction. This is NOT new committed-data loss (note drafts are already in-memory-only/transient and lost on refresh today) and any single subsequent tap recovers/commits it; recorded so Tester does not misread it as a defect. **Undo:** aisle selection stays OUTSIDE S6's undo buffer, unchanged from S8. **Done, 2026-09-10 — Tester's independent formal pass: 268/269 assertions, this story's checks clean; new canonical regression baseline in REGRESSION_LOG.md. The 222→209 total-count delta is BY-DESIGN net-supersession (S8/S9/S16's free-text-input / datalist / ⚑ checks retrofit to the new select / persisted-set mechanism, NOT lost coverage; same convention as S13→S5, S19→S13). The single 269th assertion is Minor defect D1, scoped to S23 only (see S23's row) — it does not affect this story.** **Forward-reference / PRESENTATION superseded, 2026-09-11 (S26 drafted — cross-story supersession per playbook, S19→S13 convention; NOT a reopening, this story STAYS Done):** PO real-device feedback on the shipped build is that the always-present per-row `<select>` forcing a SECOND line onto EVERY item (including aisle-less ones) halves item density — the exact M24 / "every row is now two lines" trade flagged at implementation, now confirmed unacceptable on-device (NB-6 doing its job). New story S26 changes ONLY this story's PRESENTATION — the always-present-select-on-a-second-line becomes an icon-gated reveal on the primary line, so aisle-less items stay a single line. This story's FUNCTIONALITY is unchanged and stays Done: the persisted `state.aisles` set + migration, the native `<select>` editor + commit-on-`change` model, S24's add-flow, the iOS focus-zoom fix, normalized-key matching / R15 guard, and the `Other`/no-aisle bucket are all KEPT. S26 LOCKED 2026-09-11 — this reciprocal supersession note now STANDS: S26 supersedes ONLY this story's PRESENTATION (all the FUNCTIONALITY listed just above is KEPT), this story STAYS Done, and reciprocal ripple notes are added to the Done S16 and S9 rows. See S26 and its paired icon-sizing story S27 (also Locked 2026-09-11). | Done | 4 |
| S23 | As a user, I want to create, rename, and delete aisles from the settings menu, so that my aisle list matches the store I'm actually shopping in. | **Pipeline COMPLETE — LOCKED 2026-09-10.** Cleared Scrum-Master + Developer sanity-checks, Tester testability-check, and QA's per-story gate plus a clean Lock-gate re-read (zero Real findings); AC frozen, clear for Developer implementation. **Scope (functionality #5):** manage the persisted state.aisles set (S22) inside the S21 settings panel — create, rename, delete. Depends on S21 (the panel) and S22 (the set + state.unassignedLabel). **Other is NOT special — it is renamable/deletable like any aisle, and the starters are just seeds with no protected status. Behaviors (PO-locked), Developer-recommended mechanism:** (a) RENAME a real aisle = cascade the new string over every item using it + update the state.aisles entry; RENAME Other = update state.unassignedLabel only (relabels the no-aisle bucket everywhere, no item data changes). (b) DELETE an in-use aisle = set matching items' aisle to '' (revert to the no-aisle bucket) + remove the state.aisles entry — it does NOT block until reassignment. (c) DELETE the Other entry = drop the custom label so the intrinsic no-aisle bucket falls back to a built-in Unassigned label (the no-aisle STATE still cannot be deleted, only its label); the user may re-create an Other aisle afterward (per S22 N11, Other becomes assignable again once it is no longer the bucket label). **Normalized-key matching (NB-2, has teeth):** both the rename-cascade and the delete-in-use revert MUST select affected items by NORMALIZED key (normalize(item.aisle) === normalize(target)), consistent with S22's normalized-key selection — because S22's migration deliberately leaves non-canonical item values untouched (e.g. produce, trailing-space Bakery), an exact-string cascade would ORPHAN those items when their canonical option is renamed or deleted; normalized-key match keeps them in sync. **Create AND rename validation (working defaults, open to refinement before lock):** reject empty/whitespace-only names; using S8's normalize() consistently, reject any proposed name that case-insensitively duplicates EITHER an existing state.aisles entry OR the current bucket label state.unassignedLabel (default Other); the RENAME collision check MUST EXCLUDE the entry being renamed (M21) so a no-op or pure-recasing rename (e.g. Dairy → dairy) is not spuriously rejected; also reject a name whose normalized form equals S24's reserved add-new sentinel label (belt-and-suspenders with S24 N12). This prevents minting a real Other that collides with the bucket label and reintroducing the two-Others ambiguity S22 exists to prevent. **Rejection signal (NB-3) + blur reconciliation (M22):** on an EXPLICIT commit of an invalid/colliding name the field STAYS OPEN, the create/rename is a no-op (state.aisles unchanged), and a brief inline message is shown (exact wording/styling delegated to the Developer, same precedent as S3's toast copy); but if the field is instead dismissed by BLUR without an explicit commit, revert/cancel WINS — the create/rename simply didn't happen (consistent with S24's revert-on-blur rule) — resolving the S23-stay-open vs S24-revert fork in favor of revert on blur. Testable outcomes: explicit invalid commit → field open + state unchanged; blur dismissal → editor closed + state unchanged. **iOS focus-zoom guard (PO, testable AC):** the Create-aisle field and any rename field in Settings MUST use font-size >= 16px, or this rework simply relocates the same zoom into Settings. **Undo / safety (OPEN PO question, QUESTIONS.md, non-blocking; Tester NB-4 confirmed the default is testable and the PO already has it on the console — leaving as the logged question, no reform):** aisle create/rename/delete are a new mutation class; working default is NOT undo-eligible (consistent with S8 aisle edits being outside S6's buffer), but delete-in-use silently reverting multiple items is the one genuinely destructive new action — asking the PO whether delete (esp. in-use) should get a brief confirm or a Deleted-Undo toast mirroring S3. **OUT of scope this round (PO):** reordering aisles / controlling by-aisle sort order — noted as a possible future companion so by-aisle sort can match a store's physical walk order; grouping stays alpha-ordered with the no-aisle bucket last for now. **Glyphs:** any control glyph is a plain monochrome Unicode symbol, never a colored emoji; PO glyph pick, placeholder now. **Formal pass complete, 2026-09-10 — 17/18 for this story (part of the 268/269 canonical pass); NOT Done. Minor defect D1 OPEN, PARKED to next session (not fixed now): an explicit invalid/colliding RENAME commit in the Settings aisle manager reverts+closes instead of staying open with an inline error — the rename is still correctly REJECTED (no bad rename applied, no data-loss risk); only the stay-open+inline-error affordance (NB-3/M22) is missing, it behaves like a blur-dismiss instead. Done BLOCKED pending the known one-line fix: add the activeElement guard the row-editor's focusout already has (~script.js:1608 vs the row-editor guard ~1363-1365) + a TC23.10 re-verify. Flagged as the FIRST task next session in QUESTIONS.md and SPRINT_LOG.md.** **D1 RESOLVED — Done, 2026-09-11:** the known one-line fix landed (the `activeElement` guard the row-editor's `focusout` already had, mirrored into the Settings rename-commit path) and Tester's independent re-verify passed TC23.10 — an explicit invalid/colliding RENAME commit now STAYS OPEN, shows the inline error, and leaves `state.aisles` unchanged (NB-3 / M22 satisfied), with a blur dismissal still reverting/cancelling; full suite re-run 269/269, zero open defects. `test-plans/S23-aisle-crud-settings.md` updated to 18/18 DONE and REGRESSION_LOG.md to the 2026-09-11 269/269 canonical row (citation of record). **S23 → Done** — the entire backlog is now Done except the two parked stretch goals (S11, S18) and the newly-drafted S26/S27. | Done | 4 |
| S24 | As a user, when I'm choosing an item's aisle and the aisle I need isn't in the list yet, I want to add it without leaving the item, so that I can categorize the item by its real store section on the spot. | **Pipeline COMPLETE — LOCKED 2026-09-10.** Cleared Scrum-Master + Developer sanity-checks, Tester testability-check, and QA's per-story gate plus a clean Lock-gate re-read (zero Real findings); AC frozen, clear for Developer implementation. **Scope (functionality #6):** from the per-item aisle select (S22), let the user add a brand-new aisle not yet in the list and have it applied to the item being edited. Depends on S22 (the select) and S23 (create/persist + validation + the state.aisles set). **UX fork — RESOLVED by the Developer's spike (drafted this way; a native select cannot host a text field in its OS overlay, and a custom dropdown would reintroduce the very zoom this rework removes):** PRIMARY (chosen) = a sentinel option + Add new aisle… positioned ALWAYS LAST in the option list (per S22 NB-1) that carries a RESERVED non-aisle value distinct from any real aisle name so it can never collide with a user aisle (N12; S23 additionally rejects creating an aisle whose normalized name equals this sentinel label); on change it reveals a name-entry input styled at font-size >= 16px; on commit → create + persist (state.aisles via S23, same validation) + assign + re-select the item; caveat carried in the AC: inherently TWO-STEP on iOS (pick the sentinel, the native picker closes, then the input appears) — the closest to the PO's inline preference a native, non-zooming control allows. DOCUMENTED ALTERNATIVE (on the record, NOT chosen): the add action opens Create-aisle in the S23 Settings flow and auto-assigns the new aisle back onto the item after save. **Mechanism precision (sanity-check fix):** the new-aisle-name field must NOT reuse S22's removed aisle editingField path (S22 deletes that type) — it must be its OWN mechanism (a dedicated new-aisle editingField type or dedicated handlers), named explicitly so the implementer does not wire it back onto the removed aisle path. **Revert-on-cancel + blur reconciliation (sanity-check fix + M22):** capture the item's PRIOR aisle BEFORE the sentinel changes select.value; the revert to that captured value must fire on empty commit, explicit cancel, AND blur dismissal of the reveal input (not only an explicit cancel), because the select momentarily shows + Add new aisle… as its own value. If the reveal input is dismissed by BLUR — including when the typed name is empty OR collides with an existing aisle/label — REVERT wins: the add simply didn't happen and the select returns to the captured prior value; only an EXPLICIT commit of a colliding/invalid name surfaces S23 NB-3's stay-open + inline message. This resolves the S23-vs-S24 dismissal fork in favor of revert-on-blur. Whichever path, a successful add ends with the new aisle both persisted in state.aisles AND selected on the originating item. **Constraints:** the reveal input and the select are >= 16px (iOS guard); any add-affordance glyph is a plain monochrome Unicode symbol, never a colored emoji (PO glyph pick, placeholder now). **QA implementation requirements (non-blocking, folded so they can't be lost — NOT Lock conditions):** (a) IMPORTANT, same C1 bug class — commitEditor() MUST gain an explicit new-aisle branch, else a new-aisle draft falls through the else to saveName() and corrupts the item name (the exact C1-class fall-through); (b) the change-handler's commit-first step (R14) MUST also apply to the sentinel-reveal branch, so opening the + Add new aisle… reveal first flushes any other open editor, same as a normal aisle change. **Note:** closest successor to the PO's original narrow ask (buy chocolate, no candy aisle in the dropdown, need to add one), now on the persisted-set model rather than free-text. **Done, 2026-09-10 — Tester's independent formal pass: 268/269 assertions, this story's checks clean; new canonical regression baseline in REGRESSION_LOG.md. The 222→209 total-count delta is BY-DESIGN net-supersession (S8/S9/S16's free-text-input / datalist / ⚑ checks retrofit to the new select / persisted-set mechanism, NOT lost coverage; same convention as S13→S5, S19→S13). The single 269th assertion is Minor defect D1, scoped to S23 only (see S23's row) — it does not affect this story.** | Done | 4 |
| S25 | As a user, I want the per-item note field to stop making the app zoom in on my phone, so that adding or editing a note is as smooth as the aisle picker now is. | **Drafted 2026-09-10 (Scrum Master), PO green-lit — ENTERS the normal doc pipeline (Scrum-Master sanity-check next); a stage behind S21–S24.** **Root cause (same as S22's aisle-field zoom):** the per-item NOTE editor's input (.row-meta-input) is styled at 0.82rem (~13.12px), below iOS's 16px focus-zoom threshold, so focusing it zooms the page and stays zoomed. **Fix:** bump that input to font-size >= 16px — the same cross-cutting >= 16px rule this whole rework applies to every text input. The NOTE field STAYS a free-text input (it is free-form text, NOT a closed set like aisles — no select conversion, no data-model change, no commit-model change; only the font-size). **Scope guard:** touches only the note editor's input styling; note behavior, the shared text-editor infra (updateDraft/commitEditor + input/focusout/keydown branches), and undo-eligibility (notes stay OUTSIDE S6's buffer, unchanged) are all untouched. **Testability:** the desktop pass asserts the note input's computed font-size >= 16px; on-device zoom-elimination is a non-blocking REAL-DEVICE confirmation signal, NOT a desktop gate (same NB-6 discipline as S22). Trivial, but runs the normal pipeline (sanity-check → testability-check → QA gate → Lock) for the process touch. **Done, 2026-09-10 — cleared its (trivial) pipeline touches and shipped alongside the aisle rework; Tester's independent formal pass: 268/269 (new canonical baseline in REGRESSION_LOG.md), this story's check (note input computed font-size >= 16px) clean. On-device zoom-elimination is the non-blocking real-device signal per NB-6; the note field stayed free-text as specified. D1 (the 269th) is S23-only and unrelated.** | Done | 4 |
| S26 | As a user shopping on my phone, I want the per-item aisle affordance hidden behind an icon on the item's primary line (in line with the note/edit/up/down/delete icons) instead of an always-visible dropdown, so that an item I don't set an aisle on stays a SINGLE line and I stop losing roughly half my screen's item density to an empty second row on every item. | **Drafted 2026-09-11 (Scrum Master), from PO real-device feedback on the shipped S21–S25 build — a PRESENTATION change ONLY, NOT a functionality change (cross-story supersession per playbook, S19→S13 / S22→S8 convention; S22 STAYS Done). Draft only — needs the full doc pipeline before Lock.** PO's own words (verbatim): "the functionality of the dropdown is good but i'd like it to be hidden behind an icon in line with the other icons. currently it's creating a second row for each item whether i want it to have an aisle or not, effectively reducing screen space for items by half." **Root problem (presentation only):** S22 made the per-item aisle a native `<select>` that is ALWAYS present on a second line (`.row-meta`, `renderRow` ~script.js:1047-1066 / `renderAisleSelect` ~1086) of EVERY item row — so even an aisle-less item renders two lines, halving on-device item density. This is exactly the density trade the team flagged at S22 implementation (M24 + the Developer's "every row is now two lines" disclosure), now confirmed unacceptable on the PO's actual phone — the NB-6 real-device signal doing its job. **KEPT unchanged — all of S22's FUNCTIONALITY (this story does NOT reopen any of it):** the persisted `state.aisles` set + first-load map-then-seed migration; the native `<select>` editor and its commit-on-`change` model (the delegated `change` handler ~script.js:1372, `saveAisle` ~683); S24's `+ Add new aisle…` sentinel/inline-create flow; the iOS focus-zoom fix (native picker + font-size >= 16px); normalized-key option matching + the R15 dangling-value guard; and the intrinsic no-aisle/`Other` bucket (`state.unassignedLabel`). Only the affordance's PRESENTATION and reveal-gating change. **Core AC (the "what"):** the per-item aisle affordance becomes an ICON on the row's PRIMARY line, in line with the existing per-row icons (note-toggle, S15 edit, S19 Up/Down, delete); an item with NO aisle set renders as a SINGLE line — the aisle `<select>` is NO LONGER forced onto an always-present second line, so aisle-less items no longer each cost a second row; the aisle `<select>` (retaining its full S22/S24 option set, NB-1 order, and behavior) appears only when the user engages that icon, and dismissing it returns an aisle-less item to a single line. **Testable outcome:** an item with no note and no aisle set renders exactly one line (same height as S1/S2's locked single-line spec); engaging/setting an aisle never permanently forces a second line onto an unrelated aisle-less item. **Developer sanity-check landed, 2026-09-11 — FEASIBLE; findings folded in below (scrum-master triage: the chosen R14-safe mechanism and the testable invariants are recorded here so the Tester can write deterministic tests; the set-aisle-presentation + glyph picks are deferred to a single post-Lock PO mockup per the Developer's recommendation — NO PO input needed now).** **R14-SAFE MECHANISM — CHOSEN (Option C):** keep the native `<select>` ALWAYS-PRESENT and always-openable — S22's EXACT R14-safe model, so the delegated `change`-commit handler (~script.js:1372) AND the deferred-`focusout` `activeElement` guard (~1363-1365) stay BYTE-FOR-BYTE UNTOUCHED (zero JS commit/guard changes) — but move it OFF the `.row-meta` second line onto the PRIMARY line and collapse it to an `.icon-btn`-sized footprint purely via CSS (`appearance:none`, fixed icon width, clipped/transparent option text, the PO's glyph overlaid as a `pointer-events:none` span so a tap falls THROUGH to the `<select>` and opens the native picker directly — one tap, no `showPicker()`). Key insight the Developer identified: the density problem was the select being FULL-WIDTH ON A SECOND LINE, NOT its being always-present — so keeping it always-present inherits ALL of S22's R14 safety while shrink-and-relocate alone fixes density. An aisle-less AND note-less item then has no `.row-meta` content → renders a SINGLE line (the density fix). **Implementation requirement (N14, QA nitpick):** the Developer must NOT emit the `.row-meta` element when it would be empty (or must add `.row-meta:empty { display:none }`) — else its `flex-basis:100%` + `margin-top` would still add height and reintroduce the second line; this is exactly what Tester's C2 height-equality assertion guards. **R14 invariant (testable AC):** no `render()` fires while a native picker is open, and the `<select>` is NEVER destroyed/recreated to reveal it (it was there all along) — the mirror-image hazard of the S13 C1/R13 and 2026-09-08 focusout seams is structurally avoided, not merely guarded. **Documented fallback (Option A), if C's select-as-icon styling proves unworkable on real iOS:** an icon button + transient reveal-render (two taps, commit-first) — recorded as the alternative, not the plan. **By-Aisle — RESOLVED (Developer):** the aisle icon replaces the second-line select on the PRIMARY line in ALL sort modes INCLUDING By-Aisle; per S16's own hard-won lesson, do NOT suppress the aisle icon in By-Aisle mode — hiding the only edit affordance would break S9's edit-in-every-sort-mode guarantee (the exact self-contradiction S16 caught) — so the icon stays present in every mode and the previously-redundant full-select-under-a-group-header simply goes away for free. S9/S16 reciprocal notes land at Lock. **Set-aisle presentation + aisle glyph — DEFERRED to a single post-Lock PO mockup (Developer's recommendation — folds BOTH picks into ONE decision-tool page to minimize PO round-trips; building it before the AC settles risks the S16 moving-target trap since tag-vs-inline is coupled to the mechanism):** tag-vs-inline for a SET aisle, and the icon's glyph (plain monochrome Unicode, never a colored emoji), are BOTH post-Lock PO picks — NOT resolved now (N15, QA nitpick: the glyph-overlay's accessibility treatment — an accessible label so the icon/select is identifiable — rides on this SAME deferred glyph mockup, decided alongside the glyph). **The testable line for THIS story:** only AISLE-LESS items MUST stay single-line; a row that HAS an aisle set MAY be two lines (e.g. a tag). Logged non-blocking in QUESTIONS.md (updated to the post-Lock single-mockup path). **Overflow re-check (M24/R7) — MEASURE, do NOT assume icon-btn parity (M25, QA Minor):** this moves the worst-case primary-line control count 5→6, BUT the 6th control is a `<select>`, NOT an `.icon-btn` — it does not inherit `.icon-btn` sizing, and a native `<select>` under `appearance:none` does not reliably collapse to a declared width across engines — so the deferred ONE combined re-check at 320/360/375/390px (after the S26→S27 pair lands) MUST MEASURE the collapsed select's REAL rendered footprint at each width, NOT assume `.icon-btn` parity with S19's 6-icon measurement (S19's clean 222/222 fit is a reassuring prior, not a substitute for measuring this select; still holds only if S27 keeps S20's fixed ~19.5px boxes). **Undo:** aisle selection stays OUTSIDE S6's undo buffer, unchanged from S8/S22. **Paired with S27** (equalize row-icon glyph sizes toward the note icon) — sequence S26 then S27 so sizing finalizes ONCE against this story's final primary-line layout (S13→S14 precedent) and the single combined overflow re-check covers the pair. **Tester testability-check landed, 2026-09-11 — TESTABLE, zero blocking criteria; 3 non-blocking clarifications folded in:** **(C1, NB-6 labeling)** the "a tap falls THROUGH the `pointer-events:none` glyph overlay → opens the native picker directly, one tap, no `showPicker()`" behavior is a NON-BLOCKING REAL-DEVICE confirmation signal (same NB-6 discipline as S22's picker-overlay-on-tap sentence), NOT a desktop formal-pass gate — the desktop pass verifies only the structural proxies: the `<select>` is present on the PRIMARY line at the ~19.5px icon footprint, the glyph overlay is `pointer-events:none`, the `change`-commit path works, NO reveal-render occurs, and the `<select>` persists across renders. **(C2, canonical density signal)** the "single line / no forced second line" guarantee is pinned to ONE deterministic assertion: an aisle-less AND note-less row's height EQUALS the locked single-line reference height AND no second-line content contributes height (robust whether or not an empty `.row-meta` div is emitted) — this height-equality is the canonical test signal for the density fix. **(C3, fallback contingency — recorded for the Lock note)** the test plan is authored against Option C (the plan); the documented Option A fallback (icon button + transient reveal-render, two taps) would INTRODUCE a reveal-render and thus RE-OPEN the R14 / 2026-09-08-focusout test design (a reveal-render would need its own guard), so a real-device signal forcing A would require RE-AUTHORING the test plan — a conscious contingency, non-blocking, not an objection. **QA per-story gate landed clean, 2026-09-11 — zero Real findings; the R14/C1 hazard hunt came up GENUINELY CLEAN (Option C truly inherits S22's R14 safety with ZERO JS changes, confirmed against the real `change`-commit handler + `focusout` `activeElement` guard). Cheap folds (all above): M25 (overflow note now says MEASURE the select's real footprint, not assume `.icon-btn` parity), N14 (do not emit an empty `.row-meta`), N15 (glyph-overlay a11y rides the deferred mockup).** **LOCKED 2026-09-11 — full doc pipeline cleared (scrum-master → Developer sanity-check → Tester testability-check → QA per-story gate, zero Real findings); AC frozen, clear for Developer implementation, sequenced BEFORE S27. No QA re-read needed (clean gate, one-line folds only, no Real findings — the S14/S17 clean-gate-Lock precedent, not the S13/S15 re-read case). Reciprocal supersession notes added to the Done rows at Lock: S22 (PRESENTATION superseded — stays Done), S16 (compact-affordance mechanism further superseded — stays Done), S9 (by-aisle aisle-affordance rendering adjusted — stays Done).** **Implemented + Done, 2026-09-14 — Tester's FINAL combined formal pass: 308/308 per REGRESSION_LOG.md's 2026-09-14 row, script `vopping-tests-tester-s1-s27-formal.js` (sha `afcd725`), 0 defects; S26 = 11/11 (Part 12), S27 = 7/7 (G1-G4, Part 13). Shipped as the Locked Option C mechanism (always-present native `<select>` moved to the PRIMARY line and CSS-collapsed to a MEASURED 19.5×19.5px icon footprint with a `pointer-events:none` glyph overlay; the `change`-commit handler + `focusout` `activeElement` guard stayed byte-for-byte untouched — R14-safe). The PO's single combined post-Lock decision-tool mockup resolved BOTH deferred picks: set-aisle presentation = TAG-WHEN-SET (Option 1 — a compact S8-style aisle tag on a second line ONLY when an aisle is set, inert/display-only, shown in Manual/Alpha and suppressed in By-Aisle; editing stays via the primary-line select) and aisle glyph = ⌖ (U+2316, a plain monochrome Unicode symbol, no emoji). Density fix confirmed: an aisle-less + note-less row renders a SINGLE line again (~36px, C2), and the 6-control worst-case row fits with zero horizontal overflow at 320/360/375/390px per the separate targeted closure `vopping-worst-case-row-s26-s27-closure.js` (9/9). On-device picker-open + glyph "match" are PO-confirmed (NB-6). S26 → Done.** | Done | 4 |
| S27 | As a user, I want all the per-row icons made a bit bigger — matching the note icon's size, which already looks right — so that every row control is comfortably tappable and visually consistent. | **Drafted 2026-09-11 (Scrum Master), PO feedback arriving alongside S26 — kept as a SEPARATE companion sizing story rather than folded into S26, mirroring the S13-mechanism / S14-sizing split so icon size is finalized ONCE against S26's final primary-line layout. Draft only — needs the full doc pipeline before Lock.** PO's own words (verbatim): "also let's make the icons a bit bigger. the size of the notes icon is good, make the other icons match it." **Core AC (the "what") — RESTATED after the Developer's sanity-check, which found the naive reading is a no-op:** the NOTE icon's current on-screen APPEARANCE is the PO's target; equalize every OTHER per-row glyph's RENDERED size toward the note glyph's (edit/pencil, S26's aisle icon, S19's Up and Down, delete). **Developer sanity-check landed, 2026-09-11 — FEASIBLE; the sharp finding that redefines the AC:** every row icon ALREADY renders at the identical CSS box size (`.icon-btn`, `1.15rem`, ~19.5px — there is NO note-specific size rule anywhere); the note only LOOKS bigger because its glyph (U+1F5CB) has heavier/fuller metrics than the thin line glyphs (edit/delete/up/down). Consequences folded into the AC: **(a)** a uniform font-size bump CANNOT achieve "match" — it scales every glyph proportionally, so the metrics gap persists; matching REQUIRES PER-GLYPH font-size tuning to equalize each glyph's rendered size toward the note glyph's (this is the concrete meaning of the PO's intent — resolve it this way, NOT a uniform bump). **(b)** enlarge GLYPH font-size ONLY, keeping the fixed ~19.5px `.icon-btn` boxes (S20's box-fixed / glyph-fills model): box stays fixed → row footprint unchanged → the overflow fit is preserved; box-enlargement is the overflow risk to AVOID. **(c)** this is a legitimate refinement that SUPERSEDES S20's explicitly-accepted "renders slightly unevenly — accepted" note (NOT a no-op — S20 accepted the unevenness, S27 evens it); reciprocal note to S20 at Lock. **(d)** device-dependence: U+1F5CB is the exact glyph S7 flagged as rendering differently on the PO's Windows machine, so "match" is verified on the PO's PHONE (the target device), NOT asserted pixel-identical on desktop — the desktop pass asserts the structural proxy (per-glyph font-sizes applied, boxes unchanged), on-device visual match is the non-blocking real-device signal (NB-6 discipline). **Overflow re-check (M24/R7):** with fixed boxes + glyph-only enlargement, the S26 6-icon worst-case row still fits (it is the same 6-icon layout S19 measured clean at 320/360/375/390px, 222/222); require ONE combined re-check at the four widths AFTER the S26→S27 pair lands — S27's fixed-box constraint is what keeps that fit. **No data-model change** — purely visual/CSS glyph sizing; never touches S6's undo buffer or stored data. **PO-CONFIRMED via screenshot, 2026-09-11 — "match" definition VALIDATED, story UN-HELD:** the PO's screenshot of the current primary-line icons (note, edit ✎, up ▲, down ▼, delete ✕) with their own emphasis lines marking the glyph heights VALIDATED the Developer's glyph-metrics finding — the note glyph renders visibly TALLER than the thin edit/up/down/delete glyphs at the SAME button box, confirming the phenomenon is heavier/taller glyph metrics, not a box-size difference (we are on the same page). So "match the note icon" is now confirmed to mean: per-glyph enlargement of the OTHER glyphs (edit ✎, up ▲, down ▼, delete ✕, and S26's new aisle glyph) to equalize their rendered HEIGHT with the note glyph (U+1F5CB), GLYPH-ONLY within the fixed ~19.5px `.icon-btn` boxes — the Developer tunes the exact per-glyph values and the PO confirms the final look on their phone (device-dependent per the S7-glyph caveat: verified on-device, not asserted pixel-identical on desktop). **Tester testability-check landed, 2026-09-11 — TESTABLE, zero blocking criteria; 3 non-blocking clarifications folded in:** **(C1) desktop pass/fail pinned to G1–G4 (not left to inference):** **G1** — the tuned `.icon-btn` boxes stay S20's ~19.5px in BOTH width AND height (±0.5px); glyph-only enlargement, NO box resize — this is the load-bearing overflow-safety constraint (scope corrected per M26 below: this covers the 4 real tuned `.icon-btn`s, not the aisle overlay). **G2** — per-glyph tuning is actually applied, asserted as "the non-note glyph font-sizes are NO LONGER ALL EQUAL to the pre-S27 uniform 1.15rem value" (NOT phrased as "distinct per glyph" — Up/Down may legitimately share a size). **G3** — each tuned non-note glyph's computed font-size is >= the 1.15rem baseline (enlargement UP, never down). **G4** — no row-height growth, and no data-model/undo interaction. **Rendered-glyph-HEIGHT parity is LOOSE corroboration ONLY, NEVER the gate** (each non-note glyph's rendered height merely NEAR the note glyph's — ~±20–25%, or "the gap to the note glyph shrinks") — because the reference note glyph U+1F5CB is itself device-unstable (the S7 finding), a tight desktop height-parity gate would BOTH false-fail a correctly phone-tuned build AND false-pass one that looks wrong on-device; on-device "match" stays the NB-6 real-device signal. **(C3) S26 dependency + glyph-agnostic coverage:** S27's aisle-glyph tuning depends on S26 having shipped the primary-line aisle glyph (hence the correct after-S26 sequencing), and S27's testable line is GLYPH-AGNOSTIC — it tunes WHATEVER glyph S26's deferred post-Lock PO pick lands on to match the note height, so S27's coverage does NOT depend on that unresolved glyph pick (parallels S26's own glyph deferral). **QA per-story gate landed clean, 2026-09-11 — zero Real findings; 2 cheap folds:** **(M26, box-framing scope corrected)** the fixed-~19.5px-`.icon-btn`-box constraint (G1) applies to the 4 REAL tuned `.icon-btn`s — edit ✎, Up ▲, Down ▼, delete ✕ (the note-toggle `.icon-btn` is the unchanged HEIGHT REFERENCE) — but S26's aisle glyph is NOT an `.icon-btn`: it lives on a `pointer-events:none` OVERLAY span over the collapsed `<select>`, and its glyph is itself deferred to S26's post-Lock mockup, so the fixed-box framing is literally INAPPLICABLE to it — its height-matching is scoped to that OVERLAY element explicitly (Developer-tuned, or folded into S26's deferred glyph mockup), NOT to an `.icon-btn` box. **(N16, base fallback)** retain a base `.icon-btn` glyph font-size fallback when the uniform 1.15rem value is replaced by per-glyph rules, so any glyph without an explicit per-glyph rule still renders at a sane size. **LOCKED 2026-09-11 — full doc pipeline cleared (scrum-master → Developer sanity-check → PO "match"-definition confirmation → Tester testability-check → QA per-story gate, zero Real findings); AC frozen, clear for Developer implementation, sequenced AFTER S26. No QA re-read needed (clean gate, one-line folds only, no Real findings — S14/S17 clean-gate-Lock precedent). Reciprocal note added to the Done row at Lock: S20 (its explicitly-accepted glyph-unevenness is refined by this story's per-glyph height equalization — S20 stays Done). The one combined M24/R7 overflow re-check at 320/360/375/390px runs after the S26→S27 pair ships.** **Implemented + Done, 2026-09-14 — Tester's FINAL combined formal pass: 308/308 per REGRESSION_LOG.md's 2026-09-14 row, script `vopping-tests-tester-s1-s27-formal.js` (sha `afcd725`), 0 defects; S27 = 7/7 (G1-G4, Part 13), S26 = 11/11 (Part 12). Per-glyph height equalization shipped GLYPH-ONLY within S20's fixed ~19.5px `.icon-btn` boxes (shipped values: note-toggle 1.15rem HEIGHT REFERENCE unchanged; edit + delete 1.35rem; Up + Down 1.28rem shared; S26's aisle overlay glyph 1.35rem, M26) — no box resize (G1) and no row-height growth (G4), base `.icon-btn` font-size fallback retained (N16). The desktop pass verified the G1-G4 structural proxies; the on-device "match the note icon" look is PO-confirmed (NB-6, device-dependent per the S7 U+1F5CB caveat). S27 → Done.** | Done | 4 |
| S28 | As a user, I want the app's aisle list expanded to the full 18-aisle store-walk taxonomy the auto-aisle dictionary is built around, so that auto-assigned aisles land in real, reusable aisles instead of dangling one-off labels. | **Drafted 2026-09-14 (Scrum Master), from `AUTO_AISLE_SPEC.md` §1 + §6-A — first story of the Auto-Aisle feature. AC finalized 2026-09-14 — PO Q3 answered (rename-migration) and the per-story gate is CLEAR (Lock-ready; see the gate fold-in + Status below).** **Scope:** expand the seeded taxonomy to the spec's 18 store-walk aisles — the 17 real aisles (Produce · Meat · Seafood · Deli · Bakery · Dairy & Eggs · Frozen · Cereal & Breakfast · Canned & Jarred · Pasta, Rice & Grains · Baking & Spices · Condiments & Sauces · Snacks & Candy · Beverages · Household & Cleaning · Personal Care & Health · Pantry) plus `Other`, which stays the LABEL of the intrinsic no-aisle bucket and is excluded from the assignable set (S22 N11 unchanged). **THE BLOCKER (Dev+QA+SM converged):** `migrateAisles()` short-circuits on `Array.isArray(st.aisles)` (script.js ~54), so for the already-seeded PO device simply editing `AISLE_STARTER_LIST` is a SILENT NO-OP — the new aisles would never appear. Required: a VERSIONED one-time re-seed — persist a `seedVersion` (or equivalent) in `state`; on load, when the stored version is behind current, UNION any MISSING new starters into the existing `state.aisles` EXACTLY ONCE, idempotently, then bump the stored version so a later reload at current version does NOT re-mutate (extends S22's NB-7 idempotency to the new starters). **Never resurrect user-deleted starters:** the versioned union runs once per version and is gated by the version bump, so a starter the user deleted via S23 after that bump stays deleted (extends S22 N13's durability guarantee to the new starters) — Developer's mechanism, the locked guarantee is "each version's union runs exactly once." **Item re-map — narrow, one-time, PO-approved (Q3 = rename-migration):** migration otherwise only ADDs to `state.aisles`, WITH exactly two deliberate one-time `item.aisle` re-tags carried by the rename step below (`Dairy`→`Dairy & Eggs`, `Household`→`Household & Cleaning`) plus the pre-existing S22 `Other`→'' remap — NO other item value is ever rewritten (in particular, `Meat/Seafood` items are NOT re-tagged or split). **Single source of truth (Dev+QA):** seeded starter names MUST be byte-identical after `normalize()` to the dictionary's aisle keys, so the S29 matcher's output always resolves to a seeded `<select>` option — derive the starter list from `Object.keys(window.AISLE_DICTIONARY)` minus `Other`, OR keep the literal `AISLE_STARTER_LIST` and assert set-equality (post-`normalize`) against the dictionary keys at load (fail-fast in dev); if the dictionary is absent at load, fall back to a hardcoded literal 17 + `Other` so the app never crashes (co-sequenced with S29's dictionary load-order). **Old↔new name drift — RESOLVED (PO Q3 = RENAME-MIGRATION, 2026-09-14; the PO chose option (B), NOT the (A) pure-union default):** as part of the same one-time versioned migration: (1) RENAME the two clean 1:1 old→new starters — `Dairy`→`Dairy & Eggs` and `Household`→`Household & Cleaning` — updating the `state.aisles` entry AND re-tagging every existing item whose aisle NORMALIZES to the old name to the new name (matched by normalized key like an S23 rename cascade, so non-canonical variants come along); (2) LEAVE the old `Meat/Seafood` aisle AS-IS — preserved as a custom user aisle — and ADD `Meat` + `Seafood` as NEW starters; do NOT auto-split or re-tag any `Meat/Seafood` item; (3) all other old starters already match the new names (`Produce`/`Bakery`/`Frozen`/`Pantry`/`Beverages`/`Other`) — unchanged; (4) preserve all genuinely custom user aisles untouched (union). **Data-safety (QA to scrutinize this two-aisle item re-tag especially):** the re-tag matches by normalized key, runs EXACTLY ONCE via the version gate (idempotent), and if the target name already exists in `state.aisles` (e.g. the user had already typed `Dairy & Eggs`) it MERGES into that entry rather than creating a duplicate. The N13 no-resurrect guarantee still holds for the genuinely new starters (`Meat`, `Seafood`, and the rest of the expanded 17). **Undo:** migration is not a user action and never touches S6's buffer. **Depends on** S29's dictionary load-order for the single-source-of-truth check (co-sequence); prerequisite for S29 output resolving and for S31/S32. **Pre-Lock gate fold-in, 2026-09-14 (Developer sanity-check + Tester testability-check + QA per-story gate — CLEAR, Lock-ready):** **(M27, QA — step-order + resulting option-order):** the versioned re-seed runs in this order — (i) RENAME the two 1:1 starters (`state.aisles` entry + normalized-key item re-tag, merge-not-duplicate), (ii) UNION-APPEND any missing new starters to the END of `state.aisles`, (iii) bump the stored version. Consequence (low stakes, consistent with S22 insertion-order): an EXISTING user's dropdown shows their prior set THEN the appended new starters — NOT full store-walk order; a FRESH install seeds all 17 in store-walk order. Tester asserts THIS order (not store-walk) for the migrated case. **(Dev — seed source / S29 sequencing):** S28 ships BEFORE S29 loads `window.AISLE_DICTIONARY`, so the RUNTIME seed uses a HARDCODED literal (17 + `Other`); the `Object.keys(window.AISLE_DICTIONARY)`-derived list + set-equality assert is a dev/test guard GUARDED on dictionary presence (a no-op until S29 lands). Literal ≡ dict keys verified — shipping S28-first is safe. **(Dev — build placement):** implement the versioned re-seed as a step AFTER `var state = loadState()` (script.js:138), NOT inside `migrateAisles()` (which runs pre-state-assignment and can't reuse the S23 rename/delete cascade helpers ~801-830); dedupe the union-add + the rename by NORMALIZED key so they converge regardless of order. **(Tester):** the persisted version field is NAMED `state.seedVersion` (build 'behind'/'at-current' fixtures against it); NB-7 idempotency re-points to 'runs EXACTLY ONCE', asserted on end-state stability across a reload. **Locked, 2026-09-14 — full doc pipeline cleared (scrum-master + Developer sanity-check → Tester testability-check → QA per-story gate, zero open findings; folds above), clear for Developer implementation; sequenced FIRST (foundation) of the Auto-Aisle set.** **Implemented + Done, 2026-09-14 — Tester's independent formal pass: 345/345 assertions, 0 defects (sha `e0a0f77`; new canonical baseline in REGRESSION_LOG.md). Shipped the VERSIONED one-time re-seed (a `seedVersion`-gated union that runs EXACTLY ONCE and never resurrects user-deleted starters) expanding the seeded taxonomy to the 18 store-walk aisles, plus the PO Q3 RENAME-MIGRATION: `Dairy`→`Dairy & Eggs` and `Household`→`Household & Cleaning` renamed with a one-time normalized-key item re-tag (merge-not-duplicate), old `Meat/Seafood` KEPT as a custom aisle with `Meat`+`Seafood` added as new starters (no item split), all other starters unchanged and custom aisles preserved (union); the migrated-user dropdown order (prior set then appended new starters, M27) is verified. S28 → Done.** | Done | 5 |
| S29 | As a user, I want the app to carry a built-in item→aisle dictionary and a matcher that maps an item name to its best aisle, so that the auto-aisle triggers have an accurate, fully-offline engine to call. | **Drafted 2026-09-14 (Scrum Master), from spec §2 + §3.4 + §6-B. AC finalized 2026-09-14 — per-story gate CLEAR: QA's R16 re-read came back clean (see the gate fold-in + Status below).** **Ship the dictionary:** `AISLE_DICTIONARY.draft.js` graduates from the reviewed draft into the committed app data file (~1,286 items across the 17 real aisles) — kept READABLE/hand-editable (NOT minified or generated-only; the PO edits it directly). Load it via a `<script>` in index.html BEFORE `script.js` (index.html currently loads only `script.js` ~line 87) so `window.AISLE_DICTIONARY` is available at init. **Matcher precedence — first hit wins (spec §3.4):** tier 1 = personal override (added by S30 — reserve the slot), tier 2 = dictionary canonical exact, tier 3 = dictionary alias exact, tier 4 = whole-token match, tier 5 = no match (leave unaisled/no-aisle bucket, counts as "couldn't be matched"). **TWO separate maps built once at load — `canonicalMap` + `aliasMap`** (normalized name/alias → aisle) run as ordered passes (tier 2 hits canonicalMap, tier 3 aliasMap, tier 4 token scan), inserted in store-walk aisle order with FIRST-WRITE-WINS — this is what resolves the ~68 alias↔canonical cross-aisle collisions in the sensible direction (bare `broccoli`→Produce not Frozen; `shrimp`→Seafood not Frozen). FIRM: canonical-exact (t2) beats alias-exact (t3) beats token (t4). **Tier 4 is WHOLE-TOKEN, never raw substring** — must NOT let `ham`→hamburger, `corn`→popcorn, `ice`→rice/juice, `pepper`→peppercorns, `oat`→goat. Pin the algorithm: tokenize on whitespace/punctuation; strip a leading quantity/unit token set (bare numbers, `lb`/`lbs`/`oz`/`g`/`kg`/`ct`/`pack`/`dozen`/… — this token set is FROZEN); handle trailing descriptors/parentheticals (e.g. `(organic)`); match a canonical/alias only as a WHOLE token or contiguous multi-word run; prefer the LONGEST / most-specific match, canonical beating alias on ties. **Enumerate a fixed worked-example table for Tester:** ≥2 longest-match cases (e.g. `"frozen broccoli"`→Frozen beats the contained `broccoli`→Produce; `"2 lbs organic bananas"`→banana/Produce after strip) + 1 NAMED alias-vs-alias cross-aisle tie: bare `drumstick` is an alias of `chicken drumstick` (Meat) AND of `ice cream cone` (Frozen), canonical in neither → resolves to **Meat** (Meat precedes Frozen in store-walk order; first-write-wins) — each example carries its expected aisle. **Fix the stale spec example** `"chicken breast → chicken"`: there is no bare `chicken` canonical — the real match is the `chicken breast` canonical (t2); use a corrected example. **Runtime = DEGRADE GRACEFULLY, never throw before render (R16, QA Real — the S29 HOLD):** the runtime dictionary-load / lookup-build path must NOT throw — first-write-wins on a canonical-vs-canonical collision (as specified above) and `console.warn`+skip on a malformed entry (the `parseStoredState` R1 posture) — so a PO typo in the hand-edited ~1,286-entry file can NEVER crash the IIFE into a blank offline page. **HARD fail-fast lives ONLY in the standing dev/test data-integrity gate, never at runtime.** That dev/test gate asserts: valid structure `{ [aisle]: [{ name, aliases: [] }] }`, all names/aliases strings, within-aisle canonical uniqueness, and NO canonical-vs-canonical cross-aisle collision; the ~1,286 total count is a SOFT lower-bound LOG (not exact-equality — the PO hand-edits the file, so exact-count would be a false tripwire); and it WHITELISTS the intentional easter-egg alias (`lemon` is the CANONICAL, tier 2; `"difficult"` its ALIAS, tier 3 — see the KEEP note below), never flagging or stripping it. **Degrade gracefully:** if `window.AISLE_DICTIONARY` is absent/empty the matcher must not crash (everything → no-match). **Test hook (NAMED contract):** expose the pure matcher + lookup-builder on `window` under a namespace, e.g. `window.__voppingAutoAisle = { matchAisle, buildLookup }`, with signatures `matchAisle(name, { dictionary, overrides }) → { aisle, tier, matchedText }` and `buildLookup(dictionary) → { canonicalMap, aliasMap }` — the `dictionary`/`overrides` params INJECTABLE so an empty/absent dictionary (fallback, no-crash) and the S30 tier-1 override path are testable DOM-free under file://. **Intentional easter egg — KEEP (PO confirmed 2026-09-14):** the alias `lemon`→`"difficult"` (`AISLE_DICTIONARY.draft.js` ~line 359) is a DELIBERATE easter egg for the PO's partner, NOT a data slip — do NOT flag, remove, or "fix" it; the S29 data-integrity test must WHITELIST it (it resolves `lemon`→Produce via tier 3, harmless). **Reuse** existing `normalize()`; no UI in this story (pure engine + data). **Undo:** N/A. **Depends on** S28 (matcher output must equal a seeded canonical aisle); prerequisite for S30/S31/S32. **Pre-Lock gate — R16 (QA Real, 2026-09-14) folded + RE-READ CLEAN; the runtime-degrade fix is folded above and QA's R16 re-read came back clean, closing the last hold. Additional Tester+Dev+QA folds:** **(Tester, TOP — tested contract):** the matcher returns `{ aisle, tier, matchedText }`, NOT just the aisle — so a precedence bug (tier-2-beats-3, longest-match-wins) is CAUGHT even when two paths happen to yield the SAME aisle (the C1 silent-gap class); assertions check `tier`/`matchedText`, not only the resulting aisle. **(Degrade seam, QA/Dev — testable):** tier 1 (S30 overrides) must resolve with NO dictionary present (dictionary-absent → dictionary tiers no-op, override/no-match still work, no crash) — reachable via the injectable hook above. **(Perf, non-blocking):** a matcher perf assertion (dozens of items against the ~1,286-entry lookup stays fast on a phone) is in Tester's scope. **(Track — organic dictionary curation, QA FYI, NON-BLOCKING, not a mechanism defect):** bare `chicken` currently resolves to Canned & Jarred via the `canned chicken` alias (there is no bare `chicken` canonical) — logged as an organic-refinement item, NOT a Lock blocker (the PO opted to refine the dictionary organically, and the S30 learning layer self-heals it on the first hand-correction). **Locked, 2026-09-14 — full doc pipeline cleared (scrum-master + Developer sanity-check → Tester testability-check → QA per-story gate); QA's R16 re-read came back CLEAN — the runtime degrade-gracefully fix folded above (dictionary-load/lookup-build never throws) satisfies it, closing the last open finding. Zero open findings, clear for Developer implementation; co-sequenced with S28 (dictionary load-order + single-source-of-truth check).** **Implemented + Done, 2026-09-14 — Tester's independent formal pass: 345/345 assertions, 0 defects (sha `e0a0f77`; new canonical baseline in REGRESSION_LOG.md). `AISLE_DICTIONARY.draft.js` GRADUATED into the committed, hand-editable app data file, loaded via a `<script>` in index.html BEFORE `script.js` so `window.AISLE_DICTIONARY` is available at init. Shipped the first-hit-wins MATCHER (t1 override slot reserved for S30, t2 canonical-exact, t3 alias-exact, t4 whole-token, t5 no-match) over the two load-time maps (`canonicalMap`+`aliasMap`, store-walk order, first-write-wins), the whole-token tier with the frozen quantity/unit strip, and the `{aisle,tier,matchedText}` return contract. R16 SATISFIED — the runtime dictionary-load/lookup-build path DEGRADES GRACEFULLY (console.warn+skip on a malformed entry, first-write-wins on a canonical collision, absent/empty dictionary → all no-match) and never throws before render; HARD fail-fast stays in the dev/test data-integrity gate (which WHITELISTS the intentional lemon easter-egg alias). The named `window.__voppingAutoAisle = { matchAisle, buildLookup }` test hook ships with injectable dictionary/overrides. S29 → Done.** | Done | 5 |
| S30 | As a user, I want the app to learn the aisle I pick for an item by hand, so that a wrong auto-guess is never repeated and my own custom aisles get auto-assigned too. | **Drafted 2026-09-14 (Scrum Master), from spec §3.3 + §3.4 tier 1 + §6-C. AC finalized 2026-09-14 — PO Q2 answered (learn non-empty picks only) and the per-story gate is CLEAR (Lock-ready; see the gate fold-in + Status below).** **Scope:** a persisted personal-override map in its OWN localStorage key (alongside state + the S10 frequency record), shape `{ normalize(name): aisle }`. Parse it with the SAME defensive guard as `parseStoredState`/`parseFrequencyState` (QA R1): parse failure, `null`, non-object, or wrong entry shape → empty map, never throws before render (a NEW R1 surface). **Record ONLY on the user's HAND pick** — via `saveAisle` (a native-`<select>` `change` to a real aisle) and S24's commit-new-aisle path. The S31 list-run and the S32 per-item Auto-detect MUST set the aisle WITHOUT recording an override, or they poison tier 1 and defeat future re-runs (implementation: a record-suppressing flag on the shared write, or a separate matcher-write path — Developer's choice; the locked guarantee is auto-writes never learn). **Matcher tier 1:** an exact `normalize(name)` hit in the override map wins ahead of ALL dictionary tiers (S29) — this is how custom/user aisles (which the built-in dictionary doesn't know) get auto-assigned. **Managed-option guarantee (R15 posture):** any aisle the matcher assigns (dictionary OR override) must be present in `state.aisles` — if absent, ADD it, so it renders as a managed reusable `<select>` option, never a dangling label (dictionary aisles are already seeded by S28; this covers override targets that are custom aisles). **S23 cascade coverage (M28, pinned):** S23 rename/delete cascades over `state.aisles` + `item.aisle` (~804-820) but NOT the override map today, which would leave dangling override targets — AC: extend S23 rename/delete to cascade INTO the override map — on RENAME, UPDATE the stored target aisle (matched by normalized key, mirroring the item cascade); on DELETE, DROP the override entry ENTIRELY (NEVER blank it to `''` — a `''`-override would contradict PO Q2 and permanently block a later fill-empty auto-run from re-filling that item). The cascade-ON-RENAME approach is PINNED over validate-and-drop-at-match-time (which would silently lose a learned override on any rename). The override map lives in its OWN localStorage key, NAMED `vopping-overrides-v1`. Test on the OBSERVABLE end-state: after deleting an override's target aisle, a later auto-run does NOT assign/recreate the dead aisle (not internal timing). **Clear-to-no-aisle — RESOLVED (PO Q2 = NO, 2026-09-14, accepted default):** a manual CLEAR to no-aisle is NOT recorded as an override — ONLY NON-EMPTY hand picks are learned. `saveAisle('')` records nothing to the override map, so a later auto-run (S31) can still fill a cleared item (consistent with S31's fill-empty-only rule); likewise an S23 delete/rename that blanks a target does not create a `''` override. **Undo:** recording is a side effect of an aisle pick; aisle picks stay outside S6's buffer (unchanged from S8/S22). **Depends on** S29 (adds tier 1 to the matcher); prerequisite for S31/S32. **Pre-Lock gate fold-in, 2026-09-14 (Tester+Dev+QA — CLEAR, Lock-ready):** **(Dev):** co-design S30's record-suppress flag WITH S32's `saveAisle`-shape reuse so BOTH the S31 bulk run and the S32 per-item Auto-detect set an aisle WITHOUT learning (one shared suppress mechanism). **(Tester/QA, non-blocking coverage):** the guarded-write path (auto-writes never learn) and the degrade seam (tier 1 resolves with NO dictionary present) are in Tester's scope. **Locked, 2026-09-14 — full doc pipeline cleared (scrum-master + Developer sanity-check → Tester testability-check → QA per-story gate, zero open findings; PO Q2 answered — learn non-empty picks only — and folds above), clear for Developer implementation; sequenced after S29 (adds matcher tier 1).** **Implemented + Done, 2026-09-15 — Tester's independent formal pass: 379/379 assertions, 0 defects (sha `fbc046e`; new canonical baseline in REGRESSION_LOG.md; QA's implementation re-check also CLEAR). Shipped the persisted personal-override map in its OWN localStorage key `vopping-overrides-v1` with the R1-defensive parse (parse-failure / `null` / non-object / wrong entry shape → empty map, never throws before render); learning is recorded ONLY on a user HAND pick, via the shared `saveAisle` choke point — a `{viaAuto:true}` record-suppress flag lets the S31 bulk run and the S32 per-item Auto-detect set an aisle WITHOUT recording an override, so matcher tier 1 is never poisoned by an auto-guess. The map feeds matcher TIER 1 (an exact `normalize(name)` hit wins ahead of ALL S29 dictionary tiers; degrade-seam safe — resolves with no dictionary present). S23 rename cascades UPDATE the override's target aisle by normalized key, and S23 delete DROPS the matching override entry entirely (never blanks it to `''`, per M28). R15 `ensureAisleExists` keeps any matcher-assigned aisle (dictionary OR override) present in `state.aisles` as a real managed `<select>` option, never a dangling label. PO Q2 honored — a hand clear-to-no-aisle records nothing and keeps any prior override. S30 → Done.** | Done | 5 |
| S31 | As a user, I want a one-tap "Auto-aisle" button that fills in aisles for every item that doesn't have one yet, so that I rarely have to set aisles by hand. | **Drafted 2026-09-14 (Scrum Master), from spec §3.1 + §6-D. AC finalized 2026-09-14 — PO Q1 answered (bulk run is one undoable action) and the per-story gate is CLEAR (Lock-ready; see the gate fold-in + Status below).** **Placement:** inside `.sort-controls`, next to the sort `<select>` (index.html ~49-56) — NOT the `.global-controls` region; an isolated new element + its own click listener, introducing no per-row / R14 surface. **Presentation:** glyph ⭍ (U+2B4D) + a short "Auto-aisle" text label; fallback ↯ (U+21AF) if ⭍ tofus on iOS. Plain monochrome Unicode (no emoji). **Action — fill-empty-ONLY (M30 — eligibility pinned to the intrinsic empty value):** eligibility is `item.aisle === ''` (the intrinsic no-aisle value), NOT "normalizes to the bucket LABEL" — so if the user has re-created a real, assignable `Other` aisle (S22 N11), an item tagged with that real `Other` is NOT treated as empty and is left alone. For every eligible (empty) item, run the matcher (S29/S30) and assign the best aisle; NEVER touch an item that already has an aisle (manual OR previously auto-set). Auto-writes do NOT record an override (S30). **Toast summary — FROZEN string + when-shown (Tester+Dev):** `showToast()` renders `{message} — Undo` (it appends ` — Undo` + the real `[data-role="toast-undo"]` button, per the `afcd725` fix), so the `{message}` passed carries NO trailing period. Compose `{message}` by counts — n = items sorted (matched + assigned), m = eligible-but-unmatched: **n≥1, m=0** → `Sorted {n}`; **n≥1, m≥1** → `Sorted {n} · {m} couldn't be matched`; **n=0, m≥1** → `{m} couldn't be matched`. FROZEN: `·` = U+00B7 with one space each side; "couldn't be matched" is INVARIANT (does not pluralize with m); "Sorted {n}" for all n≥1 (n=1 → `Sorted 1`, no "item"). **Undo affordance:** only a run that actually changed ≥1 item (n≥1) arms the bulk-undo and shows a working Undo; the n=0/m≥1 feedback toast does NOT arm an undo and does NOT touch the single slot — it must show WITHOUT a live Undo affordance (Dev: a no-undo `showToast` variant, or omit the arming so its Undo is a dismiss-only no-op — resolve at build, but it must NEVER undo an unrelated prior action). Unmatched items stay unaisled. **When-shown / no-op (UX per Orchestrator + M11):** the fully-silent no-op (NO toast, NO undo entry, NO slot change) is reserved for ZERO-ELIGIBLE runs only (no item had an empty aisle). A run WITH eligible items ALWAYS shows the toast above — including the n=0/m≥1 "nothing matched" case — so the user gets feedback their tap acted. **Bulk-undo — RESOLVED (PO Q1 = YES, 2026-09-14, accepted default):** the whole run is ONE undoable action via a NEW undo action type snapshotting `{ id, prevAisle }` for each TOUCHED (empty-only) item; undo restores each to its prior aisle (all were empty, so undo re-clears them). It occupies/clobbers the single S6 last-action slot like any other action and is the FIRST undo-eligible aisle mutation (per-item Auto-detect, S32, stays NON-undoable). Only an n≥1 run (≥1 item actually changed) arms the bulk-undo and clobbers the single slot; a ZERO-ELIGIBLE run changes nothing and touches nothing (no slot change), an eligible-but-unmatched run (n=0/m≥1) shows the feedback toast but arms no undo (see the toast clause). **M30 slot-clobber pin (resolves M1 in the mixed model):** a subsequent NON-undoable aisle action (S32 per-item Auto-detect, or a manual per-item aisle pick) NEITHER creates NOR clears the pending bulk-undo target — same rule M1 set for note/aisle edits. A stale bulk-toast Undo tapped after a newer action clobbered the slot is a safe dismiss-only no-op (existing TOAST.8 behavior). **NB-6:** on-device ⭍/↯ glyph rendering is a non-blocking real-device signal (desktop verifies structure). **Depends on** S29 + S30. **Pre-Lock gate fold-in, 2026-09-14 (Tester+Dev+QA — CLEAR, Lock-ready):** **(M29, QA):** add a `.sort-controls` worst-case overflow re-check at 320/360/375/390px for the new button (R7/M24/M25 precedent) — it's a new element in that region. **(Dev build):** call `setLastAction({ type: 'aisle-bulk', entries: [{ id, prevAisle }, … ] })` BEFORE `showToast()` so the toast's Undo binds to the bulk action; `performUndo` gains an `'aisle-bulk'` branch restoring each `prevAisle`. **Locked, 2026-09-14 — full doc pipeline cleared (scrum-master + Developer sanity-check → Tester testability-check → QA per-story gate, zero open findings; PO Q1 answered — bulk run is one undoable action — and folds above), clear for Developer implementation; sequenced after S29 + S30.** **Implemented + Tester formal pass 404/404 + function confirmed on the PO's phone, 2026-09-14 (sha `3ac58fb`; the 404/404 REGRESSION_LOG row + the Tester's S31/S32 test-plan docs are uncommitted, riding the eventual auto-aisle Done-flip after S34).** **Glyph-rendering disposition, 2026-09-16 (SM, slicing the Settings icon picker — SUPERSEDES the inline U+2B4D→U+21AF blind-swap fallback above; do NOT blind-swap):** the ⭍/U+2B4D iOS tofu is now owned by the Settings icon picker (S33 registry + S34 picker UI), which lets the PO pick a *rendering* glyph for the `auto-aisle-run` icon on-device — a real PO-chosen fix, not a guess. Per NB-6 the on-device glyph is a NON-blocking real-device signal (desktop verifies structure) and the PO has ALREADY confirmed this button's FUNCTION on-device — BUT the ⭍ glyph renders as a visible TOFU BOX on the PO's iOS Safari, so the Orchestrator is deliberately NOT flipping this Done ("won't mark Done what the PO sees as broken"). **Done is GATED on the S34 icon-picker glyph fix:** once S34 ships and the PO picks a rendering glyph for the `auto-aisle-run` icon on-device, this story's auto-aisle Done-flip proceeds (alongside the uncommitted 404/404 REGRESSION_LOG row + S31 test-plan doc riding along); the auto-aisle glyph's device-rendering acceptance TRANSFERS to S34. **DONE 2026-09-16 — the S34 glyph fix is delivered: the Auto-aisle button now renders ↯ U+21AF (the PO's on-device pick, replacing the boxing ⭍ U+2B4D); prior Tester formal pass 404/404 @`3ac58fb` + function confirmed on-device — gate discharged.** | Done | 5 |
| S32 | As a user, when I'm setting one item's aisle, I want an "Auto-detect" option right in the aisle picker, so that I can let the app guess that single item without running the whole list. | **Drafted 2026-09-14 (Scrum Master), from spec §3.2 + §6-E. AC finalized 2026-09-14 — per-story gate CLEAR (Lock-ready; see the gate fold-in + Status below).** **Trigger lives INSIDE the existing per-item aisle `<select>`** (the ⌖ picker, S26) as a TOP option (e.g. `⭍ Auto-detect`), NOT a new row icon — protects the S26/S27 row density (spec is explicit: not a new row icon). **Detect STRUCTURALLY** via a reserved option value + a data attribute (e.g. `data-autodetect="1"`), mirroring S24's `data-sentinel` — NEVER by label (N12), so a user aisle literally named "Auto-detect" can't collide; S23 create/rename validation additionally rejects a name whose normalized form equals the Auto-detect label (belt-and-suspenders, same treatment the S24 sentinel gets). **Action:** run the matcher (S29/S30) for that one item and set its aisle via the EXISTING `change` → (matcher) → `saveAisle`-shape → `render()` path — inheriting the round-4 focus-restore skip (`renderList` deliberately does not re-focus `data-role="aisle-select"`, ~1306-1348) and the R14 deferred-`focusout` `activeElement` guard. Introduce NO new render/commit seam; carry NO DOM ref across a render (the C1/R13 stale-DOM hazard class). Because it's an explicit per-item request it MAY overwrite an existing aisle — but it must NOT record a learning override (S30) (an auto-guess is not a hand pick). **RESOLVED at sanity-check (Dev, 2026-09-14):** Auto-detect is the TOP option (index 0), ABOVE the no-aisle option; the `+ Add new aisle…` sentinel stays LAST. NO dedicated ghost-tap / re-entry guard is needed (it commits via `saveAisle`→`render()` with no keyboard/row-growth, and a ghost re-fire merely re-runs an idempotent match) — confirm on device (NB-6). **N17 (QA FYI, not a reopening):** it is a conscious asymmetry that per-item Auto-detect (overwrite-capable) is NON-undoable while the non-overwriting bulk run (S31) IS undoable — per PO Q1. **NB-6 real-device gate:** the ⭍ glyph inside an `<option>` may render differently in the iOS native picker wheel — give S32 the same non-blocking real-device acceptance gate S26/S27 had (desktop verifies structure: the option exists, carries the reserved value + `data-autodetect`, selecting it runs the matcher and sets the aisle via the existing path with no new seam; on-device wheel glyph rendering is the NB-6 signal). **Undo:** per-item aisle set stays outside S6's buffer (unchanged from S8/S22). **Depends on** S29 + S30. **Locked, 2026-09-14 — full doc pipeline cleared (scrum-master + Developer sanity-check → Tester testability-check → QA per-story gate, zero open findings; folds above), clear for Developer implementation; sequenced after S29 + S30.** **Implemented + Tester formal pass 404/404 + function confirmed on the PO's phone, 2026-09-14 (sha `3ac58fb`; the 404/404 REGRESSION_LOG row + the Tester's S31/S32 test-plan docs are uncommitted, riding the eventual auto-aisle Done-flip after S34).** **Glyph-rendering disposition, 2026-09-16 (SM, slicing the Settings icon picker — SUPERSEDES the inline U+2B4D→U+21AF blind-swap fallback above; do NOT blind-swap):** the ⭍/U+2B4D iOS tofu on the `auto-detect-option` glyph — rendered INSIDE a native `<option>` (iOS picker wheel), a DIFFERENT render context from S31's button, see Q4 — is now owned by the Settings icon picker (S33 + S34). Per NB-6 the on-device glyph is a NON-blocking real-device signal (desktop verifies structure) and the PO has ALREADY confirmed the per-item Auto-detect FUNCTION on-device — BUT the ⭍/U+2B4D glyph boxes on iOS, so the Orchestrator is deliberately NOT flipping this Done ("won't mark Done what the PO sees as broken"). **Done is GATED on the S34 icon-picker glyph fix:** once S34 ships and the PO picks a rendering glyph for the `auto-detect-option` icon on-device (its OWN registry entry, per Q4 = A), this story's auto-aisle Done-flip proceeds (alongside the uncommitted 404/404 REGRESSION_LOG row + S32 test-plan doc riding along); the option-glyph's device-rendering acceptance TRANSFERS to S34. **DONE 2026-09-16 — the S34 glyph fix is delivered (the Auto-aisle/Auto-detect ⭍ U+2B4D tofu resolved via the picker; the PO picked ↯ U+21AF); prior Tester formal pass 404/404 @`3ac58fb` + function confirmed on-device — gate discharged.** | Done | 5 |
| S33 | As a user, I want the app's icons to all come from one place, so that when a glyph shows as an empty box on my phone the fix applies everywhere that icon appears instead of one hard-coded spot at a time. | **LOCKED 2026-09-16 (Scrum Master), from `ICON_PICKER_SPEC.md` §1–§8 — FOUNDATION of the Settings icon-picker feature (Sprint 6); supersedes the old pre-agreed U+2B4D→U+21AF blind-swap fallback (S31/S32). Pre-Lock review CLEAR (Dev BUILDABLE / Tester TESTABLE / QA CLEAR); QA's re-read confirmed R17a/R17b + R18 closed at f6e08c3. The folded acceptance criteria (spec §8 ledger) are authoritative; this row is synced to them.** **Central registry (ONE source of truth):** one in-code object maps each stable `action-id` → `{ default glyph, human label, [candidate set] }`, enumerating ALL glyph-bearing icons found in the code (spec §1): `settings-gear` ⚙ U+2699, `settings-close` ✕ U+2715, `auto-aisle-run` ⭍ U+2B4D, `note-toggle` 🗋 U+1F5CB, `name-edit` ✎ U+270E, `move-up` ▲ U+25B2, `move-down` ▼ U+25BC, `delete-item` ✕ U+2715, `aisle-marker` ⌖ U+2316, `auto-detect-option` ⭍ U+2B4D. **The `default` values are EXACTLY today's glyphs, so with no override stored the app renders byte-identically to today (the non-regression contract).** Text-only controls (Undo, Clear crossed off, Add/Add all/Save/Rename/Delete, the sort `<select>`) carry no glyph and are explicitly OUT of scope. **Single accessor:** `iconFor(actionId)` returns the EFFECTIVE glyph = the stored override if present + non-empty, else the registry default; EVERY draw-site pulls from it so a pick applies everywhere consistently and no glyph literal survives outside the registry — this replaces the `NOTE_TOGGLE_ICON_GLYPH` / `NAME_EDIT_ICON_GLYPH` / `AISLE_ICON_GLYPH` / `AUTODETECT_ICON_GLYPH` constants AND the inline `▲`/`▼`/`✕` literals in `renderList` AND the static `⚙`/`✕`/`⭍` in `index.html`. **Static-HTML glyphs routed too — `applyStaticIcons()`:** the three static `index.html` glyphs (`settings-gear` `#settings-btn`, `settings-close` `.settings-close` in the header, `auto-aisle-run` the NESTED `<span aria-hidden>` inside `#auto-aisle-btn`) are set from `iconFor()` by a single `applyStaticIcons()` run at init AND on every override write — neither `render()` nor `renderSettings()` touches them (`renderSettings()` rebuilds only `#settings-body`, so header close/gear aren't covered by it). **N18/Dev-1:** the `auto-aisle-run` write targets that nested span via a new selector hook, NEVER `#auto-aisle-btn.textContent` (which holds the `' Auto-aisle'` label). **N19:** the setter uses `.textContent` (inherently safe, no escaping). **N20:** accessible names stay glyph-independent (the glyph is `aria-hidden`/decorative). **Persistence:** a NEW localStorage key `vopping-icons-v1`, a SPARSE object `{ "<action-id>": "<glyph>" }` holding only actions the PO has actively changed (absent id ⇒ default). **R1-defensive parse (never throw before render, the degrade posture used across this app — S1/S10/S30):** parse-failure / `null` / non-object / **a top-level array (M31 — `Array.isArray()`-reject, matching the state/frequency/S30 parsers)** → empty override map (⇒ all defaults); an entry failing the **R17b CONTENT constraint (string / non-empty-trimmed / length-capped ≤8 UTF-16 units / no ASCII-control or markup chars `< > & " '`)** drops to default (the rest of the map still loads); unknown keys and glyphs for unknown ids dropped silently; an override that is empty/whitespace is treated as "no override" (fall back to default, NEVER render a blank control). **M32:** the map is parsed ONCE at init into an in-memory object (`renderList` is a hot path — no per-icon/per-render `localStorage`+`JSON.parse`). **NO visible change in this story** — defaults render identically to today; this is the regression-safety foundation. **Q4 working default:** ship `auto-aisle-run` and `auto-detect-option` as TWO registry entries sharing one default glyph + one candidate set (safe superset — collapses to a single shared pick in S34's UI if the PO answers Q4 = shared). **Do NOT regress** S26/S27 row density + icon layout, the R14 render-timing/focus fixes, or the S21/S23 settings machinery. **R17a escape-at-draw-site:** every `iconFor()` site that builds innerHTML wraps the glyph in `escapeHtml()` (note ~L1723, name ~L1752, up/down ~L1769–70, delete ~L1812, aisle-glyph ~L1863; the auto-detect `<option>` ~L1844 already does) — pairs with R17b so a corrupt hand-edited value can neither inject markup nor be shown as escaped gibberish (the `.textContent` static setters need no escaping). **`iconFor('unknown-id')` → a defined `FALLBACK_GLYPH` `□` U+25A1, never `undefined`/throw (R-NOTHROW).** **Test hook lands WITH S33:** `window.__voppingIcons` (`iconFor`/`getRegistry`/`getOverrides`/`ICONS_KEY`/`setIcon`/`resetIcon`/`resetAll`/`applyIcons`), mirroring `window.__voppingAutoAisle`, so S33 is verifiable in its own formal pass before S34's UI exists; named assertions **R-DEFAULT / R-OVERRIDE / R-EVERYWHERE / R-ISOLATION / R-NOTHROW**. **Depends on** nothing new; touches many draw-sites so sequenced FIRST; prerequisite for S34. **DONE 2026-09-16 — Tester formal pass 483/483 @`8322b59`, 0 defects; registry / persistence / apply-everywhere all desktop-verified.** | Done | 6 |
| S34 | As a user, I want a Settings panel that lists every app icon and lets me swap any that shows as an empty box on my phone for one that renders, so that I can fix a broken-looking icon myself without waiting for a code change. | **LOCKED 2026-09-16 (Scrum Master), from `ICON_PICKER_SPEC.md` §4–§8 — the PO-facing UI of the Settings icon-picker feature (Sprint 6). Pre-Lock review CLEAR (Dev BUILDABLE / Tester TESTABLE / QA CLEAR); QA's re-read confirmed R18 closed at f6e08c3. The folded acceptance criteria (spec §4/§5/§8) are authoritative; this row is synced to them.** **Panel:** a new `settings-section` in `renderSettings()` (e.g. "Icons"), below the existing "Aisles" section, listing EVERY registry action (S33) with its human label + its current EFFECTIVE glyph + a "Change" affordance; reuse the S21/S23 settings machinery and its focus-preservation pattern; introduce NO per-row render seam and NO new R14-class hazard. **Change → palette:** tapping "Change" reveals a candidate-glyph palette for that action (inline expander or sub-panel, Developer's call); tapping a candidate WRITES the override to `vopping-icons-v1` (S33), applies EVERYWHERE immediately (re-render the list + re-set the static glyphs), persists, no reload. **Candidate palette — curated PLAIN Unicode symbol/dingbat glyphs, NO emoji (the standing PO preference). CRITICAL: it MUST include several glyphs known to render broadly on iOS system fonts so the picker gives the PO WORKING choices — a palette of all-tofu candidates is useless.** Evidence-based curation (spec §4, the strongest signal available since desktop cannot verify iOS rendering): anchor candidate sets on THIS project's own proven-on-device glyphs (⚑ U+2691 shipped S16, ⌖ U+2316 PO-picked on-device S26, ✎ U+270E S15, ▲/▼ U+25B2/25BC S19, ✕ U+2715) and their Unicode blocks (Dingbats U+2700s, Geometric Shapes U+25xx, Misc Symbols U+2600s, Arrows U+2190s, Misc Technical U+2300s); AVOID the Misc Symbols & Arrows block U+2B00–2BFF (where the tofu'd U+2B4D lives) and astral-plane pictographs U+1F300+ (the S7 `note-toggle` U+1F5CB collapsed here). Team proposes the palette; the **PO picks per-action ON-DEVICE (NB-6)** — the whole point is the PO can SEE tofu vs. real glyph in the actual picker on their phone, so the shipped picker itself doubles as the on-device decision tool (`density-picker.html` / `s16-aisle-icon-picker.html` / `s26-aisle-presentation-picker.html` precedent; a separate mockup page is optional). The candidate palette is now **FROZEN** (spec §4 per-action sets; the PO still picks AMONG them on-device, but the SET no longer changes), with **⭍ U+2B4D explicitly WHITELISTED** in the guard (the deliberate tofu-confirm candidate, same carve-out as the auto-aisle lemon→'difficult' whitelist). **N23 (Tester impl note):** the non-emoji guard MUST key on EMOJI-PRESENTATION-DEFAULT (`Emoji_Presentation=Yes` / a VS16 U+FE0F selector), NOT mere RGI-emoji / `Emoji=Yes` membership — else text-default frozen candidates (✳ U+2733, ✷ U+2737, ✦ U+2726, ✧ U+2727, ★ U+2605) would false-fail the guard meant to bless them. **M34:** each candidate is previewed at its target draw-site's font/size (a glyph fitting the header may overflow the ~19.5px row box). A universal shared palette (same candidates for every action) is an acceptable simpler alternative to per-action sets — a cheap build-time call. **Reset — PINNED to BOTH:** a per-action "reset to default" AND a single global "reset all icons to default" (clears the key) — a guaranteed way back to a known-rendering baseline. **Resolves the U+2B4D tofu:** once shipped, the PO picks a rendering glyph for `auto-aisle-run` (and `auto-detect-option` per Q4), discharging the auto-aisle glyph's NB-6 device-rendering acceptance transferred here from S31/S32. **NB-6 real-device gate:** on-device glyph rendering is the acceptance signal; desktop verifies STRUCTURE — the section lists all actions, "Change" opens the palette, a pick writes the override + applies everywhere + persists + survives reload, and reset restores the default. **R18 — palette survives a candidate tap:** a tap must NOT collapse the open palette or lose scroll position (persist an open-palette action-id as render state + preserve scroll, OR update the glyph node in place — `renderSettings()`'s full-`innerHTML` rebuild today preserves only the two aisle inputs); the picker IS the on-device decision tool. **M33:** chosen glyphs get NO per-glyph S27 height tuning and NO `overflow:hidden` on the icon boxes (would hide the tofu signal). **Dev-3/NB-6:** after any row-icon or `aisle-marker` glyph change, require an on-device density re-confirm (`note-toggle` is the S27 height reference). **Do NOT regress** S26/S27, R14, or S23 aisle-CRUD. **Depends on** S33. **DONE 2026-09-16 — Tester 483/483 @`8322b59`, 0 defects; NB-6 met: the PO picked ↯ U+21AF (Auto-aisle) + ☰ U+2630 (note-toggle) via the on-device picker, both PROMOTED to registry defaults (retiring ⭍ U+2B4D + astral 🗋 U+1F5CB).** | Done | 6 |
| S35 | As a user, I want common staples I type by their bare name (cheese, rice, chicken, beef, fish…) to auto-assign to the right aisle, so that the Auto-aisle run and per-item Auto-detect stop dropping them into no-aisle. | **Drafted 2026-09-16 (Scrum Master), from the PO's on-device use + QA's dictionary audit (Sprint 7). DATA-ONLY correctness pass on `aisle-dictionary.js` — no `script.js` / matcher / UI change; low risk. Awaiting the standard pre-Lock review (Developer feasibility → Tester testability → QA hazard) before Lock.** **Scope — add MISSING bare head-word staples** as canonical dictionary entries in the named aisle so a bare item name resolves via the S29 matcher's tier-2 (canonical-exact) path: `cheese` → Dairy & Eggs; `rice`, `pasta`, `noodles` → Pasta, Rice & Grains; `beef`, `steak`, `sausage` → Meat; `fish` → Seafood; `vinegar` → Condiments & Sauces; `batteries`/`battery` → Household & Cleaning; and lower-frequency: `pork` → Meat, `oil` → Condiments & Sauces, `cream` → Dairy & Eggs, `juice` → Beverages, `vitamins` → Personal Care & Health. Add each as a canonical head-word with sensible plural/variant aliases folded in where natural (e.g. one `battery` entry aliasing `batteries`); the exact alias roster is hand-editable (the file is the PO's to edit). **RE-HOME `chicken`:** it currently resolves to Canned & Jarred because its ONLY occurrence is the alias `chicken` under the canonical `canned chicken` (Canned & Jarred, ~line 4556). Add a NEW canonical `chicken` in **Meat**, so bare "chicken" resolves to Meat via **canonical-beats-alias** (matcher tier-2 canonical wins over tier-3 alias). LEAVE `canned chicken` (and its `chicken` alias) in Canned & Jarred untouched — "canned chicken" still resolves to Canned via its own canonical. **Do NOT touch the deliberate judgment-calls** (peas, tuna, and the colby / condensed-milk / broth-stock labeling quirks) — the PO did not ask to change them; out of scope. **File hygiene:** keep the readable, hand-editable format (do NOT minify/generate-only); no duplicate canonicals, no new cross-aisle canonical collision (a canonical name may live in exactly one aisle). **Testability:** each added/changed term is asserted via the named `window.__voppingAutoAisle` matcher hook to resolve to its intended aisle (`cheese`→Dairy & Eggs, `chicken`→Meat, `canned chicken`→Canned & Jarred, `rice`→Pasta Rice & Grains, `vinegar`→Condiments & Sauces, `batteries`→Household & Cleaning, …); plus a regression assertion that pre-existing terms still resolve as before (esp. that the new bare canonicals `chicken`/`beef`/`fish`/`oil`/`cream` did not break existing multi-word entries like `chicken breast`, `ground beef`, `olive oil`, `heavy cream`, `canned chicken`), and `window.__voppingAutoAisle.validateDictionary()` still passes clean. **No migration, no localStorage, no UI** — ships as a pure data edit. **Depends on** S29 (dictionary + matcher, Done); sequenced BEFORE S36 (both edit the same data file). Pre-Lock review CLEAR (Dev/Tester/QA); tier-4 side-effect + compound-token regression guards folded 2026-09-16 (see Sprint 7 detail). **DONE 2026-09-16 — Tester formal pass 441/441 @e549ac2, 0 defects; fully desktop-verifiable (no NB-6 device gate).** | Done | 7 |
| S36 | As a user, I want an International aisle — with my tortillas, kosher staples, and other international foods — added to my store layout, so that those items auto-group where I actually find them in the store. | **Drafted 2026-09-16 (Scrum Master), from the PO's request (Sprint 7). Two parts: (A) taxonomy 18→19 + a versioned re-seed migration; (B) dictionary population. Awaiting the standard pre-Lock review (Developer feasibility → Tester testability → QA hazard) before Lock — QA's data-safety focus is part (A)'s migration.** **(A) Taxonomy + migration:** add `International` to `AISLE_STARTER_LIST` (script.js ~38) at a sensible store-walk position — SM's call: between `Condiments & Sauces` and `Snacks & Candy` (the center-store canned/pasta/sauces cluster; adjustable at build). This takes the taxonomy from 18 (17 real + `Other`) to 19 (18 real + `Other`). Bump `AISLE_SEED_VERSION` 2→3. Add a NEW once-only version-gated migration step (mirroring `migrateAislesV2`, run after `loadState()` in the existing post-load migration sequence) that UNIONS `International` into `state.aisles`: **runs EXACTLY once** per device (a device below v3 runs it; a reload at v3 is a pure no-op); **N13 no-resurrect** — if the user later deletes `International` via S23 after the v3 stamp, it stays deleted; **merge-not-duplicate** — if the user already hand-created an `International` aisle, don't add a second (union by normalized key); **never drops user aisles; never re-maps any `item.aisle`** (International is a pure additive union — NO rename/re-tag step, unlike v2's Dairy/Household renames); stamp `seedVersion = 3` after; R1-defensive (runs at init, must never throw/blank the page, same guard as `migrateAislesV2`). **DATA-SAFETY-CRITICAL — QA hazard focus, same class as the S28 short-circuit blocker:** `migrateAislesV2` today is gated on AND stamps the GLOBAL `AISLE_SEED_VERSION` (script.js ~161 gate + ~179 stamp). Naively bumping that constant to 3 while leaving V2 keyed to it would (a) RE-OPEN V2's gate on an existing v2 device (`2 >= 3` is false), re-running V2's union of `AISLE_V2_NEW_STARTERS` — and if the user had DELETED one of those v2 starters (e.g. `Deli`), it would be RESURRECTED, a direct N13 no-resurrect violation; and (b) re-stamp `seedVersion = 3`, causing the new V3 step to be skipped. **Required fix:** pin each version step to its OWN literal target — V2 gated `>= 2` / stamps `= 2`; V3 gated `>= 3` / stamps `= 3` — run V2 then V3 in ascending order after `loadState()`; `AISLE_SEED_VERSION` becomes only the highest/current marker (3) used for fresh-state final stamping and any debug readout. (Fresh install: `migrateAisles` seeds all 19 incl. `International` directly from `AISLE_STARTER_LIST`; V2 then V3 both no-op-union and stamp up to v3.) Developer's exact mechanism, but the locked guarantee is: **V2's union NEVER re-runs, no user-deleted v2 starter is ever resurrected, and V3 runs exactly once.** **(B) Dictionary population** (`aisle-dictionary.js`): add an `International` key (in the store-walk position matching the starter list) populated with — (1) **tortillas MOVED OUT of Bakery:** relocate the `tortilla`, `corn tortilla`, `flour tortilla` entries (currently Bakery, ~lines 2726/2393/2491) with their aliases into `International`, and REMOVE them from Bakery so a canonical lives in exactly one aisle (after the move, bare `tortilla`/`corn tortilla`/`flour tortilla` resolve to International, NOT Bakery); (2) `taco shells`, `tostadas`; (3) kosher staples: `matzah`/`matzo`, `gefilte fish`, `Manischewitz` (as a brand alias/canonical), plus a MODEST set of clearly-international staples (hand-editable/PO-refinable, same as the rest of the dictionary). **Naan / pita / flatbread STAY in Bakery** (they're breads) — explicitly NOT moved. **Do NOT aggressively re-home currently well-placed items** (soy sauce stays in Condiments & Sauces, etc.); candidate items that COULD arguably move (salsa/salsa verde/soy sauce/sriracha/hoisin/tahini in Condiments, coconut milk/refried beans in Canned, couscous/ramen/rice noodles in Pasta, hummus in Deli) are LEFT AS-IS and FLAGGED to the PO in QUESTIONS.md (non-blocking) rather than moved. **File hygiene / integrity:** readable/hand-editable format preserved; `validateDictionary()` passes after the move (esp. no leftover duplicate `tortilla` canonical in Bakery). **Testability:** after migration + edits — `International` is present in `state.aisles` for a FRESH install (via `AISLE_STARTER_LIST`, at the intended position) AND for an existing v2 device (via the v3 union, exactly once — asserted on end-state stability across a reload); the v3 migration is data-safe (no-resurrect: a user delete of `International` after v3 stays deleted; merge-not-duplicate: a pre-existing hand-made `International` isn't duplicated; NO `item.aisle` re-mapped; NO user aisle dropped; V2's union does not re-run / no deleted v2 starter resurrected); and the matcher resolves `tortilla`/`corn tortilla`/`flour tortilla` → International (not Bakery), `taco shells`/`tostadas`/`matzah`/`matzo`/`gefilte fish`/`Manischewitz` → International, while `naan`/`pita`/`flatbread` still → Bakery. **Depends on** S28 (versioned re-seed machinery, Done) + S29 (dictionary + matcher, Done); sequenced AFTER S35 (both edit `aisle-dictionary.js`). PO scope expansion (International = full multi-cuisine aisle) + V2/V3 literal-version-gate fix + parity/N21/N22 gates folded 2026-09-16 (see Sprint 7 detail). **DONE 2026-09-16 — shipped the versioned V2/V3 migration (International added on the PO's next app-open, N13-safe) + multi-cuisine dictionary population; Tester formal pass 441/441 @e549ac2, 0 defects; fully desktop-verifiable (International is a data string, not a glyph — no NB-6 device gate).** | Done | 7 |
| S37 | As a user, I want the app to remember which sort view I chose (Manual / Alphabetical / By Aisle) so that reopening or refreshing my list keeps my chosen view instead of snapping back to Manual every time. | On every sort change, persist the selected mode to its OWN localStorage key **`vopping-sort-v1`** — NOT inside `vopping-list-state-v1` — matching the one-key-per-feature pattern of `vopping-frequency-v1` / `vopping-overrides-v1` / `vopping-icons-v1`, keeping view state orthogonal to item data. **Wrap BOTH the get and the set in `try/catch`, mirroring `loadFrequency`/`saveFrequency` (script.js ~L376-388)** — a read/write failure (private browsing) degrades gracefully, never throws. **Whitelist-validate the stored value to EXACTLY one of `manual` | `alpha` | `aisle`; anything else — unknown string, `null`, non-string, or corrupt — falls back to `manual`** (today's default). **Parse-THEN-assign** so `#sort-select` never ends up with no selected option. **On load, set BOTH the in-memory `sortMode` var (script.js ~L1901) AND `sortSelect.value` BEFORE the sole bootstrap `render()` (~L2974), immediately after the `sortSelect` capture (~L1977)** — so the control and the rendered view agree from the first paint (this replaces the current S9 comment at ~L2671, "resets to Manual on reload… nothing to restore here on load"). **STRICTLY view-only (S9's core guarantee — do NOT regress):** persisting the mode must never touch `state.items` order — `getSortedItems()` (PARAMETERLESS; it reads the closure `sortMode`, script.js ~L1913) still returns a `.slice()`d copy per mode; the stored item order is untouched; S19 Up/Down still operate on the true `state.items` order (only meaningful/visible in Manual, JS-gated as today). No migration (absent key == Manual == today's behavior). **Tester baseline supersession (fold):** S9's TC9.7 ('reload returns to Manual') is INVERTED — rewrite it to assert the chosen mode PERSISTS across reload — and S9's 'sort selection need not persist' AC line is RETIRED (same supersession pattern TC9.8/9.9 applied for S13/S19). SMALL; no hard dependency (sits on S9, Done). **Sprint 8 rank 1 (build first — quick win). Pre-Lock review CLEAR (Dev feasibility / Tester testability / QA hazard — no hold); sharpenings folded 2026-09-17. LOCKED 2026-09-17. DONE 2026-09-17 — Tester formal pass 516/516 @3ed4100, 0 defects; QA don't-regress CLEAR; PO confirmed on-device (S38 unified add box + S39 aisle-header reorder — the NB-6 gates — working; S37 sort-persistence desktop-complete).** | Done | 8 |
| S38 | As a user, I want ONE add box that handles both a single typed item and a multi-line paste, so that I don't have to choose between two separate add controls for what is really the same action. | Consolidate the two current add controls into ONE always-visible field. **[DECIDED] The unified control is a small `<textarea>`** (NOT an `<input>` + `paste`-event interceptor) — its FULL value always routes through S4's EXISTING `parsePasteLines()` (script.js ~L595) → `addPastedItems()` (~L615) for BOTH the single-line and multi-line cases, so there is ONE S4 code path and NO `ClipboardEvent` dependency (iOS-robust). A single typed line yields a one-item batch (same `{type:'add', ids:[…]}` snapshot shape); a multi-line value yields a many-item batch. **Multi-line MUST route through `addPastedItems` as ONE `{type:'add', ids:[…]}` batch — NOT a loop of `addItem()`** (a loop would regress S6 to single-item undo; the whole add is ONE undo action, same as S4's paste-batch today). **Explicit Enter-submit keydown (a `<textarea>` does NOT auto-submit a form):** plain **Enter → submit** the field; **[DECIDED] Shift+Enter → insert a literal newline** (so the user can hand-compose a multi-item batch in the box). **R-S38-1 (Real — would blank the page if botched): delete the paste-panel ATOMICALLY with its JS references.** Remove the `<details class="paste-panel">` block (index.html ~L35-41) TOGETHER WITH the `pasteForm` / `pasteInput` var declarations (script.js ~L1969-1970) AND the `pasteForm` submit handler (~L2639-2644). Leaving the handler while removing the HTML makes `pasteForm` `null` → `addEventListener` throws at ~L2639 → aborts ALL later wiring (sort / auto-aisle / suggestions / settings + the bootstrap render) → dead page. KEEP `parsePasteLines` (~L595) and `addPastedItems` (~L615) — they are now the single ingest. The retained control is the primary always-visible add field (`#add-form` / `#add-input` → now a `<textarea>`, index.html ~L27-30); **update its placeholder/label to signal one-or-many** (e.g. "Add an item, or paste a list…"). **Undo (S6) unchanged;** empty/whitespace-only input stays a no-op (no blank row); Frequency + "What am I missing?" suggestions (S10) still increment on BOTH paths (via `createItem()` → `incrementFrequency`). **[DECIDED, FLAG-TO-PO] side effect (logged non-blocking in QUESTIONS.md):** because a typed single line now goes through `parsePasteLines`, a typed leading list-marker is stripped ("- milk" → "milk", "1. milk" → "milk"), which the old single-add `addItem()` did NOT do — ACCEPTED as consistent with the paste path; the PO may veto pre-build. Do NOT regress S1's add/persist, S4's marker-strip/blank-skip parsing, or S6's single-slot undo. MEDIUM; sits on S1/S4/S6/S10 (all Done). **Sprint 8 rank 2. Pre-Lock review folded 2026-09-17 (1 Real, R-S38-1). LOCKED 2026-09-17. DONE 2026-09-17 — Tester formal pass 516/516 @3ed4100, 0 defects; QA don't-regress CLEAR; PO confirmed on-device (S38 unified add box + S39 aisle-header reorder — the NB-6 gates — working; S37 sort-persistence desktop-complete).** | Done | 8 |
| S39 | As a user shopping, I want up/down arrows on each aisle group header (in the By-Aisle view) to move that whole aisle up or down, so that both my list's aisle order AND the aisle picker follow the order I actually walk my store — without opening Settings mid-shop. | In the By-Aisle sorted view (S9), each aisle-group header (`<li class="aisle-group-header">`, script.js ~L2244; styled by S17, tag-suppressed by S16) gains up/down controls that move that whole aisle within the PERSISTED aisle order. **ONE persisted order drives BOTH** (a) the by-aisle grouping sequence in the list AND (b) the per-item aisle `<select>` option order (S22/S26, script.js ~L2163 via `getAislePool()`). **MIGRATION — PINNED (data-safety, do NOT deviate):** a reorder MUTATES `state.aisles` IN PLACE (array splice) then `saveState()` + `render()` — **NO new localStorage key, NO parallel/second order representation, and NO `AISLE_SEED_VERSION` bump.** `state.aisles` is already persisted (`saveState` ~L312-314) and already authoritative for the dropdown (`getAislePool` = `return state.aisles`, ~L1571-1573), and `migrateAisles()` short-circuits on a seeded array (~L124-126). A separate order key would RE-INTRODUCE the S28 dual-source-of-truth hazard — it is FORBIDDEN. **(b) FT3's fix is therefore AUTOMATIC — the dropdown already reads `state.aisles` order, so no dropdown code change.** **GROUPING change:** the By-Aisle comparator (`getSortedItems`' aisle branch, script.js ~L1917-1928 — note `getSortedItems()` is PARAMETERLESS, reading the closure `sortMode`) switches from ALPHABETICAL to `state.aisles` INDEX order. **R-S39-2 (Real):** on an index tie (e.g. multiple distinct dangling keys not present in `state.aisles`), secondary-sort by the aisle KEY so distinct keys don't compare equal and interleave/split headers, THEN the existing within-group name tie-break. The no-aisle bucket ('') stays pinned LAST (~L1923-1925). **R-S39-1 (Real — delegation mis-route):** add the header-button branch at the TOP of the list click handler, BEFORE `var li = e.target.closest('li[data-id]'); if (!li) return;` (script.js ~L2398-2399); match `data-role` **`aisle-up` / `aisle-down`** (distinct from the row `up`/`down` at ~L2407-2408 — no `moveItem` collision), read `data-aisle-key` off the header `<li>`, and call a NEW `moveAisle(key, ±1)`. **DROP the earlier keydown-extension idea** — a native `<button>`'s Enter/Space already routes through `click`, and a keydown clause would collide with the existing early-return that stops header keys from toggling a row. **R-S39-3 (Real — focus loss):** extend the render focus capture/restore (currently keyed to `active.closest('li[data-id]')` ~L2204-2213 / restore ~L2285-2298) to ALSO recognize header buttons keyed by `data-aisle-key` + role, so repeated taps keep focus on the arrow (restoring onto a now-disabled end button is a silent no-op, same as row Up/Down — acceptable). **[DECIDED, FLAG-TO-PO] move-semantics past EMPTY aisles (logged non-blocking in QUESTIONS.md):** reposition the moved aisle relative to its nearest ITEM-BEARING (visible) neighbor in that direction, so EVERY tap VISIBLY moves the group in the list (matches the PO's "follow it as I walk" intent) — NOT a blind ±1 splice in the full array (which looks like a no-op when an empty, headerless aisle sits between two visible groups). First/last-disable is defined on the SAME visible-group basis (Up disabled on the first VISIBLE group, Down on the last). `state.aisles` is still permuted + persisted; empty aisles keep their relative slots. **KNOWN LIMITATION (logged to QUESTIONS.md):** an empty aisle renders no header, so it has no in-context arrow — arranging the full store walk INCLUDING currently-empty aisles is out of S39's scope. **Glyphs:** reuse `iconFor('move-up')` U+25B2 / `iconFor('move-down')` U+25BC (S19 + S33), `escapeHtml`-wrapped at the draw-site (as at ~L2086-2087) — NO new glyphs. **Header layout (minor):** `.aisle-group-header` (~L2244) is text-only today — add a small flex layout to seat the two `.icon-btn`s without regressing S17's header distinction or the "headers aren't tap rows" styling. **Newly-created aisle** (S23 settings / S24 inline → `addAisle()` `state.aisles.push`, ~L1396) appends at the END of the order (user then reorders it); a set aisle already lands in `state.aisles` via `ensureAisleExists()` (~L453). **Do NOT regress:** S16 (row aisle tag stays suppressed in aisle view, ~L2109), S26 (per-item picker mechanics — only its option ORDER now follows the persisted order), S28-S36 (the versioned re-seed/migration APPENDS new starters at the tail — compatible with a user-reordered `state.aisles`; N13 no-resurrect + merge-not-duplicate preserved; a reorder is a pure permutation, never re-maps `item.aisle`), the R15 dangling-value fallback (~L2243, never render `undefined`), the S29 iOS aisle-picker re-open carve-out (unaffected — fires only for `focusedRole === 'aisle-select'`, ~L2282), and R14 render-timing (no new native-`<select>` timing surface). **Tester baseline supersession (fold):** S9's TC9.3 ('groups in A-Z order') is INVERTED — rewrite to assert `state.aisles` INDEX order; the within-group alphabetical tie-break and TC9.17 (Unassigned last) are PRESERVED. **FT3 seed-order (logged to QUESTIONS.md, non-blocking):** keep the current store-walk seed (`AISLE_STARTER_LIST`) as the initial order (user-fixable now via S39); SM recommendation adopted as the working default. LARGEST of Sprint 8; sits on S9/S16/S17/S22/S26/S28 (all Done). **Sprint 8 rank 3 (build last). Pre-Lock review folded 2026-09-17 (3 Real: R-S39-1/-2/-3 + the pinned migration). LOCKED 2026-09-17. DONE 2026-09-17 — Tester formal pass 516/516 @3ed4100, 0 defects; QA don't-regress CLEAR; PO confirmed on-device (S38 unified add box + S39 aisle-header reorder — the NB-6 gates — working; S37 sort-persistence desktop-complete).** | Done | 8 |
