# Questions for Product Owner

Log anything needing real Product Owner input here. Blocking questions are also
sent to "main" (Orchestrator) immediately; non-blocking ones are batched by the
Orchestrator.

## Blocking

| Question | Raised By | Status | Answer |
|----------|-----------|--------|--------|
| Cross-story tension flagged by QA (R2, 2026-09-04): S3 has no delete confirmation, and S6's undo is a single slot clobbered by literally any next mutating action — combined, an accidental delete can become permanent with zero warning ever shown, at any point (e.g. delete the wrong item, don't notice, check off something else before reaching for Undo — gone for good). Three concrete resolution options, so this isn't an open-ended ask: **(A, scrum-master's recommendation)** keep S3's no-blocking-confirm policy exactly as-is, but add a brief "Deleted '<item>' — Undo" toast/snackbar at the moment of deletion (in addition to the existing always-visible Undo control), so the recovery prompt appears right when it matters most instead of relying on the user remembering a static button elsewhere — lowest added friction, doesn't fully close the "did something else first" gap but meaningfully narrows it. **(B)** give delete a longer/dedicated undo window instead of sharing S6's flat single-slot rule with the other 3 action types — e.g. a delete's undo eligibility survives one additional lower-stakes action, or a short time-based window, before being clobbered — directly targets the fragility QA flagged, but breaks the current "one uniform buffer covers all 4 types identically" simplicity and adds a real special case to build/test. **(C)** add a minimal one-tap confirm specifically to individual delete only (e.g. a second tap arms-then-fires, or a small inline row affirmation) — not a blocking modal `window.confirm()` — closes the zero-warning gap completely for the highest-risk action, but partially reverses S3's original no-confirmation-on-delete decision and adds friction to the single most common destructive action in a fast in-store workflow. Blocks final AC lock on S3/S6 only — does not block Developer's sanity-check or Tester's testability-check from proceeding against today's spec in the meantime. | scrum-master (relaying QA finding R2, per Orchestrator's request for concrete options rather than an open question) | Answered | PO picked Option A, 2026-09-04, relayed via Orchestrator: a brief "Deleted '<item>' — Undo" toast at the moment of deletion, no blocking confirm — S3's no-confirm policy stays exactly as originally specified, S6's undo-buffer mechanics are unchanged; the toast is purely a UI surfacing of the existing Undo control at the moment it matters most. Folded into BACKLOG.md's S3 and S6 rows directly. |
| S13 (drag-and-drop reorder, Developer's sanity-check finding, 2026-09-08): should dragging an item to an off-screen position near the top/bottom edge of a scrolled list auto-scroll the list? Real scope call, not something to guess at. Routed directly to the PO via console by the Orchestrator. Blocks S13's final AC lock only — does not block Developer's/Tester's other work in the meantime. | Orchestrator (relaying Developer's sanity-check finding directly; scrum-master tracking here for the BACKLOG.md paper trail) | Answered | PO, 2026-09-08: yes, required, not optional — "the grocery lists can sometimes be quite long, so that level of complexity is unfortunately important." Folded into S13's AC directly as a real requirement (auto-scroll while dragging near a viewport edge, continues while held near that edge; exact trigger-zone/speed are Developer-level tuning details). |
| S14 (25% row-control button shrink, Developer's sanity-check finding, 2026-09-08): the literal math guarantees a 25% reduction breaches this story's own 24px tap-target-floor guardrail — not a maybe (e.g. 26px × 0.75 ≈ 19.5px). Is the PO fine with sub-24px controls given they said the current size isn't a problem, or do they want a smaller shrink instead? Routed directly to the PO via console by the Orchestrator. Blocks S14's final AC lock only. | Orchestrator (relaying Developer's sanity-check finding directly; scrum-master tracking here for the BACKLOG.md paper trail) | Answered | PO, 2026-09-08: fine with sub-24px — "if it's a problem, we can adjust again later or figure out a different strategy." Folded into S14's AC directly as an accepted tradeoff (not a violation to keep re-flagging), with an explicit "revisit only if it becomes a real problem in practice" escape hatch per the PO's own framing. |
| S15 (in-place item editing, QA gate finding R10, 2026-09-08): this story's own headline example — renaming "zucchini" to "2 zucchini" — can make the OLD name "zucchini" resurface as an S10 "what am I missing?" suggestion chip, even though the user still has that exact item, just renamed. Not a bug in S10 or S15 individually, an interaction between two independently-correct pieces of logic. Two concrete options: **(A, scrum-master's lean, given the cost/benefit)** accept it as a low-stakes, self-correcting quirk — worst case, tapping the stale suggestion re-adds "zucchini" as a harmless duplicate row, itself undo-recoverable via S6; no implementation change, ships now. **(B)** fix it properly — make S10 recognize a same-session rename as "still the same item" so the old name stops qualifying as a suggestion; this genuinely reopens the counter-identity/fragmentation question already deliberately kept out of S15's scope (this story's AC currently states edits don't touch S10's counter in either direction specifically to avoid this exact complexity) and would need its own small design pass. How much this quirk matters in real in-store use is a real judgment call, not derivable from internal consistency — same category as S2's auto-move-on-check or S10's original signal/threshold question. Blocks S15's final AC lock only — does not block resolving S15's other open items (icon pairing) or any other Sprint 3 work in the meantime. | scrum-master (relaying QA finding R10, giving concrete options rather than an open-ended ask, per this project's established pattern for this class of question) | Answered | PO, 2026-09-08: Option A — "i like the first option." Accepted as-is, no extra logic needed. Folded into S15's AC directly as a known, low-stakes accepted behavior (so it isn't mistaken for a bug later, not left as a silent gap). |

## Non-blocking

| Question | Raised By | Status | Answer |
|----------|-----------|--------|--------|
| Retroactive paper trail for a decision already made in this draft (QA finding M4, 2026-09-04): S1's AC states a full "clear site data" wipe is an accepted, non-blocking exception to "list persists through refreshes." Unlike this draft's other two PO-attributed working defaults (S2's auto-move-on-check default; S10's frequency signal/threshold default), this one had no corresponding QUESTIONS.md entry, even though it's the same category of decision — QA correctly flagged the missing trail, not the decision itself. Closing the gap: this was NOT an inline justification invented during drafting — it's a real, explicit resolved decision the Orchestrator relayed directly from the PO in this project's original task brief ("PO has accepted that localStorage survives normal refreshes but not a full 'clear site data' wipe — no story needed for this, it's just an accepted constraint, not a bug to design around"), predating this backlog draft entirely. | scrum-master (closing QA finding M4's paper-trail gap) | Answered | Confirmed sourced from the PO via the Orchestrator's original task brief, 2026-09-04 — not an inline drafting assumption. No change to S1's AC; this row exists purely to close the missing-paper-trail gap QA correctly flagged. |
| PO decision (2026-09-04, relayed via Orchestrator): approved QA finding R3's proposed missing 12th story — a bulk "clear all checked items" action, since S10's entire "items must actually leave the list between trips" premise depended on some removal mechanism beyond one-at-a-time delete. | scrum-master (drafting the resulting S12 story) | Answered | New story S12 added to BACKLOG.md, sequenced into Sprint 1 (after S2/S6) — see S12's row for full AC, including the undo-eligibility call (treated as one atomic action, same precedent as S4's paste-batch) made directly per Orchestrator's delegated framing, not escalated further. |
| PO decision (2026-09-04, relayed via Orchestrator) on QA finding R5: S4's "added verbatim" paste rule would otherwise capture literal bullet/number prefixes from real recipe-site copy-paste (e.g. "- Milk", "1. Eggs") — resolved to strip leading list-marker glyphs only (a fixed small set: "-", "*", "•", or a leading number+"."/")"), nothing more — formatting-noise cleanup, not real ingredient/quantity parsing, staying consistent with S4's "simple" paste decision and S11's parked scope. | scrum-master (folding into S4's AC) | Answered | Folded into BACKLOG.md's S4 row directly — see S4's row for the exact stripped-pattern list and worked examples. |
| Row density/row-height pick for the item list: `density-picker.html` (root of this project) has 4 side-by-side interactive options built by the Orchestrator, addressing real PO feedback that vacking's row density (44px checkboxes, no row margin, ~52px/row) caused excessive scrolling and risked missing items mid-shop. | scrum-master (drafting S1/S2/S5, all of which depend on final row layout) | Answered | PO's final pick, 2026-09-04: whole-row tap target (Option C) + no checkbox/dot glyph at all (Option D) — crossed-off state shown via strikethrough + dimmed text only. Locked into S1/S2/S5's AC directly, implemented, and Tester-verified (74/74 assertions, zero defects — see REGRESSION_LOG.md's `vopping-tests-tester-s1-s2-s5-density-formal.js` row). S1/S2/S5 are now Done; this question is fully closed. |
| S2 (check/uncheck): should checking an item off auto-move it to the bottom of the list (common in many grocery apps), or should checked items stay exactly where they are until the user manually reorders or deletes them? Decided directly for the initial draft — no auto-move, since an automatic reorder triggered by a check action would be exactly the kind of "surprise mutation" the PO's own "list only changes when items are added/removed" requirement warns against, and reordering is otherwise explicitly user-controlled only (S5). Flagging anyway because this is a real, opinionated in-store UX call (not just an implementation detail) that affects the app's core use case, and a different working interpretation would be cheap to swap in before S2 is built, expensive after. | scrum-master (S2 sanity-check) | Answered (working default; open to override before S2 implements) | Proceeding on: no auto-move-to-bottom. Checked items stay in place; strikethrough/dim only. Revisit if PO disagrees. |
| Terminology propagation, re-surfaced 2026-09-08 (independently flagged once already by scrum-master on 2026-09-04, now again by Tester): the PO's "checked" → "crossed off" rename was deliberately scoped to S12/S3's "clear checked"-adjacent language only, not a full-project rename — S2's own story title ("As a user, I want to check an item off...") and S6/S7/S8/S9/S10's internal check/uncheck vocabulary (undo-eligible-type names, sort-mode availability lists, etc.) were left untouched on purpose. Re-flagging since it's now been noticed twice independently: does the PO want the rename propagated fully now, or does the original scoping still stand? Purely cosmetic/prose, zero AC or implementation impact either way — cheap to action whenever answered. | scrum-master (re-flagging a 2026-09-04 scoping decision, per Tester independently re-surfacing it 2026-09-08) | Open (non-blocking; working default is the original scope holds — no rename made pending an answer) | — |
| S15 (new, drafted 2026-09-08, in-place item text editing): should editing an item's own name/text be covered by S6's undo buffer? S6's undo scope was locked to exactly four mutation types (add, check/uncheck, delete, reorder) before item-text editing existed as a concept; the working default drafted into S15's AC treats it like S7/S8's note/aisle edits — NOT undo-eligible. An accidental text edit arguably feels more consequential than a note/aisle metadata tweak (it changes the item's own identity, e.g. "milk" → "milk 2%"), so flagging this as a real, cheap-to-swap-before-S15-locks call rather than assuming the working default is obviously right. | scrum-master (S15 drafting) | Open (non-blocking; working default is NOT undo-eligible, same as S7/S8) | — |
| S21 (new, drafted 2026-09-09, add-a-new-aisle): should an aisle the user deliberately adds PERSIST in the aisle dropdown/suggestion list even when NO current item uses it? Today (S8's model) the suggestion pool is derived live from the current list's aisle values plus the static starter set, so a custom aisle drops out of the dropdown once the last item using it is gone — there is no separate persisted store of custom aisles. Two readings: **(A, working default)** keep the live-derived model — "adding" an aisle means typing/assigning it at the point of use (the PO's own chocolate-in-the-candy-aisle example), no new storage; smallest change, and mostly a discoverability/affordance fix since free-text already works today. **(B)** treat an added aisle as a durable entry that stays offered in the dropdown regardless of whether any item currently uses it — needs a new persisted custom-aisle list. This is entangled with S21's UX-location fork (preferred in-dropdown vs fallback settings-menu): a settings-menu add inherently creates an aisle before any item uses it, which only reading B supports; an in-dropdown, at-assignment add works under reading A. Flagging as the real product fork that changes S21's size/data-model — not resolving it. Non-blocking: Developer sanity-check and Tester testability-check can proceed against the working default in the meantime. | scrum-master (S21 drafting, per PO's 2026-09-09 add-aisle clarification) | Open (non-blocking; working default = A, live-derived model, no new persistence) | PO, 2026-09-10 (relayed via Orchestrator, as part of the confirmed full aisle rework): resolved as Reading B — aisles become a PERSISTED set in localStorage (a deliberate, reusable list), NOT the live-derived model; migration seeds the set once as the starter list UNION every distinct aisle currently on an item. Now covered by S22 (persisted set + native select) and S23 (create/rename/delete); the narrow add-from-dropdown ask carries over to S24. Closed — the free-text discoverability framing this question assumed is itself superseded, since the per-item field becomes a native select rather than free text. |
| S23 (new, drafted 2026-09-10, aisle management create/rename/delete): the PO locked delete-in-use to REVERT affected items to the no-aisle bucket (not block-until-reassign), but specified no safety net for it — deleting an in-use aisle silently strips the aisle from potentially many items at once, a destructive, easy-to-miss action, the same class of risk QA's R2 flagged for item-delete (resolved there via a Deleted-Undo toast, Option A). Aisle create/rename/delete are also a NEW mutation class not covered by S6's undo buffer (working default: NOT undo-eligible, consistent with S8 aisle edits). Question: should aisle delete — at least the in-use case — get a lightweight safeguard (a brief confirm, or a Deleted-Undo toast mirroring S3), or is silent revert acceptable given it's a deliberate Settings action? Non-blocking: S23 can proceed on the working default and add a safeguard before lock if the PO wants one. | scrum-master (S23 drafting, per PO's 2026-09-10 aisle-rework spec) | Open (non-blocking; working default = silent revert, not undo-eligible — revisit before S23 locks) | — |
| S9 (mechanism ripple from the aisle rework, drafted 2026-09-10): once the no-aisle bucket becomes the labeled Other bucket (S22), should that bucket still sort LAST in by-aisle view, or fall into alphabetical position by its label? Working default preserves today's behavior — the no-aisle/Other bucket always sorts last regardless of its label — since it is the catch-all and belongs at the end of a store walk. Cheap-to-swap view call. Non-blocking. | Developer (spike finding) / scrum-master (logging) | Open (non-blocking; working default = Other bucket sorts last, unchanged from S9's current Unassigned-last behavior) | — |
| Note editor iOS zoom (raised by Developer during the aisle-rework spike, 2026-09-10): the per-item NOTE editor uses the same .row-meta-input at 0.82rem (~13.12px), so it has the SAME iOS focus-zoom problem the aisle rework is fixing — but the aisle rework does not touch the note field. Cheap fix: bump that input to font-size >= 16px. Should the PO fix the note field's zoom in this same round? Developer is relaying this to the PO directly. Story-set impact deliberately held pending the answer (per Orchestrator): if yes, wire it in as either a one-line AC addition to this rework or a tiny standalone story (slot reserved, id S25 if standalone); if no, leave the note field as-is. | Developer (spike finding, relaying to PO) / scrum-master (logging + holding the slot) | Answered (PO green-lit 2026-09-10) | PO, 2026-09-10 (relayed via Orchestrator): yes, fix the note field's zoom in this same round. Drafted as standalone story S25 (note-field iOS focus-zoom fix — font-size >= 16px; the note stays a free-text input, no data-model change) — riding along with the aisle rework and running the normal doc pipeline. |
| M23 / S16 compact appearance (raised by Developer 2026-09-10 during the pre-Lock fold, relayed to PO by Orchestrator): the aisle rework makes the per-item aisle control a persistent inline select. The Developer recommends using that SAME inline select in the By-Aisle COMPACT view too, rather than select.showPicker() (fragile on file:// — our deployment — with a focus() fallback that does not actually open a picker). This SUPERSEDES S16's icon-only ⚑ flag affordance — a visual the PO hand-picked from s16-aisle-icon-picker.html — so the by-aisle grouped rows would show a per-row dropdown instead of the flag. Per the S15 do-not-guess-twice-on-a-visual-pick rule, asking the PO to confirm the by-aisle compact appearance before it is final. Drafted in S22 as the WORKING DEFAULT (inline select) but marked provisional; the rest of the set proceeds to QA's Lock-gate re-read regardless. | Developer (recommendation) / scrum-master (logging), relayed to PO by Orchestrator | Answered (PO confirmed 2026-09-10) | PO, 2026-09-10 (relayed via Orchestrator): confirmed — the per-row inline select supersedes S16's ⚑ icon in By-Aisle compact mode. The M23 clause in S22 is flipped provisional → CONFIRMED and Locked; S16's Done row carries the reciprocal supersession + ⚑ / .aisle-sort-icon / .aisle-tag dead-code cleanup note. |
| NEXT-SESSION FIRST TASK — parked defect D1 on S23, 2026-09-10 (NOT a PO question; tracked here per Orchestrator so it can't be lost): S23's formal pass is 17/18 — the one fail is a Minor UX defect. An explicit invalid/colliding RENAME commit in the Settings aisle manager reverts+closes instead of staying open with an inline error (violates NB-3/M22). The rename is still correctly REJECTED — no bad data, no data-loss risk; only the stay-open+inline-error affordance is missing (it behaves like a blur-dismiss). Known one-line fix: add the activeElement guard the row-editor's focusout already has (~script.js:1608 vs ~1363-1365), then Tester re-verifies TC23.10, then S23 → Done. S23 is held at In Review until then; S21/S22/S24/S25 are Done. | Tester (D1, formal pass) / scrum-master (logging) | RESOLVED 2026-09-11 | Fixed 2026-09-11 (the one-line `activeElement` guard, mirrored from the row-editor's `focusout` into the Settings rename-commit path) and Tester independently re-verified — TC23.10 now passes (explicit invalid/colliding rename STAYS OPEN + shows inline error + `state.aisles` unchanged; blur still reverts), full suite 269/269, zero open defects; `test-plans/S23-aisle-crud-settings.md` → 18/18 DONE, REGRESSION_LOG.md → 269/269 canonical row. **S23 → Done.** Backlog now all Done except parked S11/S18 and newly-drafted S26/S27. |
| S10 ("What am I missing?"): the requirement text says "frequently *added* items," so the initial draft's history counter increments on every **add** event (S1 single-add or an S4 paste-ingest line), not on checked-off/purchased events. An alternative reading — count only items that got checked off (i.e., actually bought), not just added-then-possibly-deleted-unbought — would track "what I actually buy" more precisely than "what I type in," but the literal wording supports the add-count reading, so that's what's drafted. Also flagging the starting suggestion threshold (2 = "added at least twice before") as an arbitrary, easily-tunable constant, not a locked value. | scrum-master (S10 sanity-check) | Answered (working default; open to override before S10 implements) | Proceeding on: add-count signal (not purchase/checked-off count), threshold = 2. Revisit if PO wants purchase-count instead, or a different threshold, once there's real usage to look at. |
| S26 (new, drafted 2026-09-11, aisle-affordance presentation rework): once the per-item aisle affordance moves from S22's always-present second-line `<select>` to an icon-gated reveal on the primary line, how should an item that HAS an aisle set display that aisle — a compact S8-style aisle TAG on a second line only when set (tap-to-edit), or inline on the primary line? A subjective visual pick, not derivable from internal consistency — same category as the original density pick and S16's ⚑ glyph pick, squarely under S15's "don't guess twice on a visual pick" rule. Resolution path (updated 2026-09-11 after the Developer's sanity-check): the mechanism is now confirmed FEASIBLE and R14-safe (Option C — an always-present native `<select>` collapsed to an icon-sized footprint on the primary line, zero JS commit/guard changes), so the visual pick is unblocked but DELIBERATELY DEFERRED to AFTER S26 Locks — a single Developer-built decision-tool mockup (density-picker / s16-aisle-icon-picker precedent) that folds BOTH the tag-vs-inline set-aisle presentation AND the aisle ICON's glyph (plain monochrome Unicode symbol, never a colored emoji) into ONE page, to minimize PO round-trips; building it before the AC settles risks the S16 moving-target trap (tag-vs-inline is coupled to the mechanism). The aisle-LESS-item single-line win — the whole point of S26 — holds regardless of this pick; the PO objected only to aisle-less items being forced onto a second line, and the testable AC only requires aisle-less items to stay single-line (a SET aisle MAY be two lines). S26 LOCKED 2026-09-11 without it (as planned); this is now the SOLE active post-Lock PO item for S26 — the Developer builds the one combined mockup, the PO picks, then it folds into the implementation. | scrum-master (S26 drafting, per PO real-device feedback) | Open (non-blocking; ACTIVE post-Lock item — S26 Locked 2026-09-11, R14 feasibility resolved = Option C; resolution path = ONE Developer-built mockup folding tag-vs-inline + glyph → PO pick) | — |
