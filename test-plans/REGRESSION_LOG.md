# Regression Log — canonical running count

Owned by: **Tester**, this file only. This is the single source of truth
for "what's the current cumulative regression count" — cite this file,
not a memory of a number, not a single story's own Results table. vacking
had real citation-drift incidents (149 vs 160, 41 vs 40/42, 283 vs
282/300/351 — see its `QA_FINDINGS.md` R10) every time a number got
restated from memory instead of from a ledger like this one.

**Rule:** once an independent Tester formal-pass script exists for a
story, it becomes the citation of record for that story's test count —
supersedes any Developer self-verification script for that slot. Every
row below names the exact script file and the count it produced. If a
script is re-run later and a number changes, add a new row (dated) —
don't overwrite the old one silently.

## Ledger

| Date | Story/Scope | Script | Result | Cumulative total | Notes |
|------|-------------|--------|--------|-------------------|-------|
| — | — | — | — | 0 | No stories implemented yet; backlog not drafted. Ledger opened 2026-09-04 in advance of any formal passes. |
| 2026-09-04 | S1, S2, S3, S4, S5, S6, S12 (Sprint 1, first formal pass) | `c:\tmp\pw-test\vopping-tests-tester-s1-s6-s12-formal.js` (Tester-independent — supersedes Developer's self-check `vopping-selfcheck.js` as citation of record per playbook) | 67/67 assertions passed, single script run, no prior baseline to re-run against (first formal pass of the project) | 67 | No arithmetic required — this is the script's own single printed total ("67/67 passed"), quoted directly, not summed from sub-parts. Zero console/page errors, zero confirm/alert dialogs, zero external network requests (only the app's own local file:// asset loads). One MINOR non-blocking defect found and disclosed separately (does not affect this pass/fail count, which reflects assertions-as-written): a paste line consisting of only a marker glyph + trailing whitespace creates a spurious junk item — see `S4-paste-ingest.md` Findings section. Full raw transcript archived in `S6-undo.md` (canonical copy, cross-referenced by all other Sprint-1 files rather than duplicated). SUPERSEDED by the row below, same day — do not cite this row going forward. |
| 2026-09-04 | S1-S6, S12 (Sprint 1, re-verification after Developer's S4 marker-regex fix) | Same script, `c:\tmp\pw-test\vopping-tests-tester-s1-s6-s12-formal.js` (updated in place with 2 new fix-verification checks for TC4.3, then re-run in full) | 69/69 assertions passed (67 original + 2 new checks added for this re-run; zero regressions in the other 67) | 69 | Again the script's own single printed total ("69/69 passed"), quoted directly. The TC4.3 defect from the prior row is now fixed and closed — see `S4-paste-ingest.md`'s "Re-verification, 2026-09-04" section. SUPERSEDED by the row below, same day — do not cite this row going forward. |
| 2026-09-04 | S1, S2, S5 (density-picker.html CSS/markup follow-up pass, closes BACKLOG.md's Tracked follow-up gate / QA finding M3) + full S1-S6/S12 regression re-run | New script, `c:\tmp\pw-test\vopping-tests-tester-s1-s2-s5-density-formal.js` (independent rewrite, not a copy — the toggle mechanic fundamentally changed: checkbox removed, whole `<li>` is now the tap/keyboard target with `role="checkbox"`/`aria-checked`/`aria-label`) | 74/74 assertions passed | 74 | Script's own single printed total ("74/74 passed"), quoted directly. Covers: ARIA correctness, zero checkbox glyph, strikethrough+dim computed-style verification, nested-control precedence for both click and keyboard, a repeated-keyboard-toggle test targeting Developer's self-caught focus-restoration fix, terminology rename ("Clear crossed off"), S12 toast text/pluralization (newly verified, not new behavior), tap-target-size (>=24px) and mobile-overflow re-checks now that the row spec is locked rather than a placeholder, plus a full functional regression re-run (add/delete/paste/reorder/undo/clear-checked) — zero regressions anywhere. Per PO scoping decision (2026-09-04, relayed by Orchestrator): the ARIA/keyboard-specific checks in this count are informational sanity-check coverage, not a required blocking gate going forward — see `test-plans/README.md`'s "Accessibility scope" note. Full transcript: `S5-reorder-buttons.md`'s Commands section (canonical copy for this script). SUPERSEDED by the row below for cumulative-total purposes (S1-S6/S12 are re-confirmed clean again there) — do not cite this row's total going forward, cite the row below. |
| 2026-09-08 | S7 (notes), S8 (aisle designation), S9 (sort view), S10 (frequency suggestions) — Sprint 2 first formal pass, run after Developer's own independent S7-S10 re-verification pass landed (pushed sha `f9f097d`, 2 real bugs fixed in the shared note/aisle editor: an open-second-editor draft-discard bug, and a synchronous-focusout-commit race that could swallow a tap on a different nested control) + full S1-S6/S12 regression re-run | New script, `c:\tmp\pw-test\vopping-tests-tester-s7-s10-formal.js` (Tester-independent — supersedes Developer's own self-check `vopping-s7-s10-verify.js` as citation of record per playbook; Part 1 of this script ports the prior row's 74-check Sprint-1 suite verbatim, not a re-derived subset, specifically to keep this row's arithmetic checkable) | 154/154 assertions passed (74 Sprint-1 re-confirmed + 80 new S7-S10 checks; zero regressions in the 74) | 154 | Arithmetic shown inline per this file's own convention: 74 (prior row, re-run verbatim and re-confirmed clean) + 80 (new: 23 for S7, 18 for S8, 18 for S9, 21 for S10 — sub-checks per named test case, e.g. TC10.12's 5-value storage-corruption battery, same convention as S1's TC1.8) = 154, matching the script's own single printed total ("154/154 passed") exactly. Covers all of S7-notes.md (TC7.1-TC7.17), S8-aisle-designation.md (TC8.1-TC8.18), S9-sort-view.md (TC9.1-TC9.17), S10-frequency-suggestions.md (TC10.1-TC10.13) — including TC7.15-17/TC8.16-18, three test cases added specifically to close out Developer's two just-fixed bugs with dedicated regression coverage (editor auto-focus, second-editor-commits-first-draft, tap-on-other-control-fires-on-first-tap). Also closes S12-clear-checked-items.md's deferred forward-reference to S10 (TC10.5: bulk clear-crossed-off does not change historical count, before/after compared directly, not hardcoded). Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `S7-notes.md`'s Commands section (canonical copy for this script, cross-referenced by S8/S9/S10's own files rather than duplicated, same convention as Sprint 1's `S6-undo.md`/`S5-reorder-buttons.md` arrangement). SUPERSEDED by the row below for cumulative-total purposes (S1-S10 are re-confirmed clean again there) — do not cite this row's total going forward, cite the row below. |
| 2026-09-08 | S14 (button shrink), S16 (aisle-sort compact icon), S17 (aisle-header distinction) — Sprint 3's first 3 shipped stories, run after Developer's own self-verification landed (pushed sha `606285f`, 15/15 new checks, 0 console errors, both prior regression suites re-confirmed clean since shared `renderRow()`/`.icon-btn` code was touched) + full S1-S10 regression re-run | New script, `c:\tmp\pw-test\vopping-tests-tester-s14-s16-s17-formal.js` (Tester-independent — supersedes Developer's own self-check as citation of record per playbook; Parts 1 and 2 of this script port the prior row's 74-check Sprint-1 and 80-check Sprint-2 suites verbatim, not re-derived subsets, to keep this row's arithmetic checkable) | 180/180 assertions passed (74 Sprint-1 + 80 Sprint-2 re-confirmed + 26 new S14/S16/S17 checks; zero regressions in the 154) | 180 | Arithmetic shown inline: 74 + 80 + 26 = 180, matching the script's own single printed total ("180/180 passed") exactly. Covers all of S14-shrink-buttons.md (TC14.1-14.5), S16-aisle-sort-compact-icon.md (TC16.1-16.12), S17-aisle-header-distinction.md (TC17.1-17.6) — including TC16's own confirmation that the self-contradiction found during S16's testability-check (suppressing the only edit-entry-point for an already-set aisle) is genuinely resolved by the shipped icon-only affordance. S16's icon-only affordance was tested by `data-role`/class/behavior only, never by its literal glyph, per the Orchestrator's heads-up that it's a deliberate placeholder pending the PO's own pick. Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `S14-shrink-buttons.md`'s Commands section (canonical copy for this script, cross-referenced by S16/S17's own files rather than duplicated, same convention as prior rows). SUPERSEDED by the row below for cumulative-total purposes (this script now hangs Playwright if re-run as-is — it probes `[data-role="up"/"down"]` elements S13 physically removed; a stale artifact of a superseded UI mechanism, not a defect found in this pass at the time) — do not cite this row's total going forward, cite the row below. |
| 2026-09-09 | S13 (drag-and-drop reorder, supersedes S5's Up/Down buttons) — Sprint 3's 4th shipped story, run after Developer's own self-verification landed (pushed sha `33deddf`, part of `635f906`, 27/27 new checks, zero unexpected regressions) + a required regression-suite RETROFIT: retired 7 checks from the old Sprint-1 74-check suite that directly probed S5's now-removed `[data-role="up"/"down"]` buttons (74→67 — see `S5-reorder-buttons.md`'s dated RETIRED note), and rewrote (not skipped) S9's TC9.8/TC9.9 to assert the real S13-era equivalent — drag-availability gated by sort mode via a real pointerdown-then-wait-past-450ms probe — instead of button visibility (80→81, one check split into two; see `S9-sort-view.md`'s dated update note) | New script, `c:\tmp\pw-test\vopping-tests-tester-s1-s13-formal.js` (Tester-independent, freshly authored — not a copy of Developer's own self-check `vopping-s13-drag-verify.js`, which it supersedes as citation of record for S13's own slot per the playbook; also supersedes the three prior canonical scripts — `…s1-s2-s5-density-formal.js`, `…s7-s10-formal.js`, `…s14-s16-s17-formal.js` — as citation of record for the regression-baseline checks they used to own, since all three now hang Playwright if run as-is. Drag gestures are driven by dispatched `PointerEvent` sequences directly, per the locked AC's own requirement that this be built on real Pointer Events specifically so it's automatable this way — `pointercancel` in particular has no Playwright-mouse-API equivalent) | 216/216 assertions passed (67 Sprint-1 retrofitted-and-reconfirmed + 81 Sprint-2 retrofitted-and-reconfirmed + 26 Sprint-3 S14/S16/S17 reconfirmed unchanged + 42 new S13-specific checks; zero regressions in the 174-check retrofitted baseline) | 216 | Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 67 (Part 1) + 81 (Part 2) + 26 (Part 3) + 42 (Part 4) = 216, matching the script's own single printed total ("216/216 passed") exactly. Covers all of S13-drag-drop-reorder.md (TC13.1-13.26, 42 concrete assertions — a few draft cases split into multiple sub-checks once actually scripted, e.g. TC13.1/TC13.9/TC13.14/TC13.18, none dropped): pickup-delay/jitter gating (TC13.1-13.4), the drop-position midpoint rule and out-of-bounds clamp (TC13.5-13.8), `pointercancel` abort per QA finding R9 (TC13.9), raw-Pointer-Events confirmation (TC13.10), cross-row draft-commit-on-pickup (TC13.11), sort-mode gating (TC13.12-13.13), undo-eligibility (TC13.14), QA findings M9 open-editor-blocks-pickup (TC13.15-13.16), M10 surface-agnostic delay (TC13.17), M11 same-position no-op (TC13.18), M15's no-op-wins tie-break against the boundary-commit rule (TC13.19), M12 scroll suppression (TC13.20-13.21), the PO-required auto-scroll near both viewport edges (TC13.22-13.23), persistence (TC13.24), nested-control precedence (TC13.25), and confirmed-N/A accessibility scope (TC13.26). One script-authoring bug was found and fixed while writing this pass (an earlier check's own cleanup gap, not an app defect) — see `S13-drag-drop-reorder.md`'s Results section for the full disclosure. Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `S13-drag-drop-reorder.md`'s Commands section (canonical copy for this script; `S5-reorder-buttons.md`'s RETIRED note and `S9-sort-view.md`'s dated update note both cross-reference this section rather than duplicating it, same convention as prior rows). SUPERSEDED by the row below, same day — this row's 216/216 pass had a real coverage gap: TC13.11 asserted the cross-row draft committed but never checked WHICH element received the `dragging` class, so it kept passing even against the pre-fix buggy code per QA finding C1 (a silent stale-clone failure, not a thrown exception) — do not cite this row going forward, cite the row below. |
| 2026-09-09 | S13 (drag-and-drop reorder) — QA finding C1 re-verification: stale DOM reference in `beginDrag()` fixed (sha `90983e3`), Developer self-check 32/32 passing zero regressions, then Tester's own independent formal re-pass strengthening the exact coverage gap that let C1 slip through the row above, plus a full S1-S13 regression re-run | Same script, `c:\tmp\pw-test\vopping-tests-tester-s1-s13-formal.js`, updated in place (TC13.11 strengthened to assert the LIVE row — not a stale detached clone — receives the `dragging` class; new TC13.27 added as the aisle-editor variant of the same fix) — not a new file, same citation of record as the row above | 219/219 assertions passed (67 Sprint-1 + 81 Sprint-2 + 26 Sprint-3 regression re-confirmed clean + 45 S13-specific checks; zero regressions in the 174-check baseline) | 219 | Arithmetic shown inline: 67 (Part 1) + 81 (Part 2) + 26 (Part 3) + 45 (Part 4) = 219, matching the script's own single printed total ("219/219 passed") exactly (45 = 42 original Part-4 subtotal, unchanged + 1 new TC13.11 sub-check + 2 new TC13.27 checks). Supersedes the row above (same day) per that row's own disclosed gap, not a routine re-run: the 216/216 pass already contained TC13.11 exercising this exact sequence (open a note editor on row A, don't commit, begin a drag-pickup on row B) but only asserted row A's draft got committed, never checking WHICH element actually received the `dragging` class — a silent failure with zero thrown error that a check watching only for errors/wrong-data would never catch; fixed by strengthening TC13.11 and adding TC13.27 as the dedicated aisle-editor variant. Also independently settles a QA-vs-Developer discrepancy over the failure mode: reproduced both pre-fix (`git show 8b17aac`, isolated temp copy, never touches live project files) and post-fix behavior directly, and confirmed the pre-fix bug throws zero exceptions — it is a silent stale-clone failure (the old `<ul>` detaches from the document but the stale `<li>`'s own `parentNode` stays non-null, so `insertBefore` succeeds silently into a dead subtree instead of throwing), matching Developer's disclosed account, not QA's original thrown-exception trace. Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `c:/tmp/pw-test/s1-s13-run3-c1fix.log` (canonical, supersedes `s1-s13-run2.log`, left in place as history) and the independent repro script `c:/tmp/pw-test/vopping-c1-independent-repro.js`. Full narrative: `S13-drag-drop-reorder.md`'s "QA finding C1" section and Commands section (canonical copy, cross-referenced here rather than duplicated, same convention as prior rows). SUPERSEDED by the row below for cumulative-total purposes (S1-S13 are re-confirmed clean again there as the 219-check regression baseline) — do not cite this row's total going forward, cite the row below; S13's own AC coverage in this row is unchanged and remains its citation of record. |
| 2026-09-09 | S15 (in-place item-name editing) — Sprint 3, run after Developer's own self-verification landed (pushed sha `10c804e`, 33/33 S15-specific + 219/219 regression, reuses S7/S8's shared editor infrastructure with `'name'` added as a third `editingField` type) + full S1-S13 regression re-run | Same combined suite, `c:\tmp\pw-test\vopping-tests-tester-s1-s13-formal.js`, extended in place with a new Part 5 (S15) — the "same script, updated in place" pattern S13's own 216→219 re-verification used; the filename is retained from when S13 was the newest story (it was never a literal scope enumeration — Part 1 covers S1-S6/S12, Part 2 S7-S10, Part 3 S14/S16/S17, Part 4 S13, Part 5 S15), so every prior citation of that filename remains valid | 246/246 assertions passed (67 Sprint-1 + 81 Sprint-2 + 26 Sprint-3 S14/S16/S17 + 45 S13 regression baseline all re-confirmed clean + 27 new S15-specific checks; zero regressions in the 219-check baseline) | 246 | Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 67 (Part 1) + 81 (Part 2) + 26 (Part 3) + 45 (Part 4) + 27 (Part 5) = 246, matching the script's own single printed total ("246/246 passed") exactly. Part 5 covers all of S15-inplace-name-edit.md (TC15.1-TC15.16, 27 concrete assertions): dedicated pencil (✎) edit-icon not double-tap/long-press (TC15.1), in-place primary-line editor with no second-line growth (TC15.2), immediate persistence + display update (TC15.3), position/checked/note/aisle all preserved across the edit (TC15.4), blank/whitespace-reverts-to-original (TC15.5), no S10 frequency-counter interaction (TC15.6), no S6 undo interaction — no entry created, no clobber of a pending target (TC15.7), QA finding R11 re-sort reads the committed name not the live draft (TC15.8/TC15.9), QA finding M9 same-row drag-block extends to the name editor (TC15.10/TC15.11), S13 cross-row commit extends to the name editor (TC15.12), QA finding M16 nested-control precedence (TC15.13), mutual exclusivity with name as a third editor type (TC15.14), draft-survives-an-unrelated-action (TC15.15), and saved-name truncation unchanged (TC15.16). Two script-authoring bugs were found and fixed while writing this pass (neither an app defect — a too-tight row-height tolerance in TC15.2, and a type-appends-to-prefilled-value slip in TC15.9) — see `S15-inplace-name-edit.md`'s Results section for the full disclosure. Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `c:/tmp/pw-test/s1-s15-run2.log`; full narrative and verbatim PASS lines in `S15-inplace-name-edit.md`'s Results and Commands sections (canonical copy, cross-referenced here rather than duplicated, same convention as prior rows). SUPERSEDED by the row below (S19/S20): the drag-era suite this row ran is retired — S19 removes the drag behavior its Part-4 TC13.* cases and Part-2 S9 TC9.8/9.9 asserted, so running it as-is would fail on deliberately-removed behavior, not real regressions. Do not cite this row's total going forward, cite the row below. |
| 2026-09-09 | S19 (restore Up/Down, remove ALL drag-and-drop) + S20 (frameless icon restyle) — Sprint 3's PO-directed reorder-mechanism revert, run after Developer's own self-verification landed (pushed sha `0f76063`, net −286 lines: moveItem() reuses the existing reorder-undo handler, drag machinery fully removed, `.icon-btn` frameless) + a required regression-suite REBUILD superseding the drag-era `vopping-tests-tester-s1-s13-formal.js` (which would now fail on removed drag behavior, same supersession pattern S13 applied to S5's cases) | New canonical script, `c:\tmp\pw-test\vopping-tests-tester-s1-s20-formal.js` (Tester-independent, rebuilt from the s1-s13 suite — supersedes it as citation of record for the whole regression baseline; the s1-s13 script is retired as a stale drag-era artifact, like the three scripts before it). Retired the 45 S13 drag checks (Part 4) and replaced them with 13 S19 checks (Up/Down restored + a dedicated "drag is actually gone" confirmation); rewrote S9's TC9.8/9.9 AGAIN back to Up/Down visibility gating (Part 2: 81→80); rewrote S15's drag-coupled cases (TC15.10/11 retired — no drag to block; TC15.12 rewritten to trigger the cross-row commit via Up/Down; Part 5: 27→24); rewrote S14's TC14.3 for S20 (the enlarged glyph now exceeds the tag text, inverting the old comparison); added Part 6 (S20, 12 checks) | 222/222 assertions passed (67 Sprint-1 + 80 Sprint-2 + 26 Sprint-3 S14/S16/S17 + 13 S19 + 24 S15 + 12 S20; zero regressions) | 222 | **Current canonical figure — cite this row.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 67 (Part 1) + 80 (Part 2) + 26 (Part 3) + 13 (Part 4, S19) + 24 (Part 5, S15) + 12 (Part 6, S20) = 222. **SUPERSEDED 2026-09-10 by the S21-S25 row below (do not cite this row's total going forward — the s1-s20 script is retired as a stale free-text-aisle-era artifact, same as the four scripts before it).** **The cumulative total DECREASED (246→222) by design, not from lost coverage:** S19 removed a whole interaction mechanism (drag-and-drop), so its 45 dedicated checks are retired (replaced by 13 that verify the Up/Down replacement AND confirm drag is gone), and 3 S15 checks that depended on drag were retired/rewritten — the same net-negative supersession S13 itself produced when it retired S5's 7 Up/Down checks (74→67 in Part 1). Covers: S19 (test-plans/S19-restore-updown-buttons.md, TC19.1-TC19.10 — Up/Down swap, disabled top-Up/bottom-Down + no-op, immediate persist + survive refresh, undo self-inverse, nested-control precedence, and TC19.9's full press-hold-move-release confirming zero reorder / no `.dragging`|`.drag-placeholder` / no stuck `touch-action:none`) and S20 (test-plans/S20-frameless-icon-restyle.md, TC20.1-TC20.10 — border/background removed, 19.5px footprint held across all 6 icon roles, 36px row height unchanged, glyph enlarged to 1.15rem/18.4px, M20 `:hover` stays transparent + chrome-free `:active` scale(0.82), text tags out of scope, no behavioral change, delete color + `--accent` token preserved). Two test-script bugs found and fixed while rebuilding Part 6 (neither an app defect — a `file://`-inaccessible `cssRules` probe for `:active`, swapped to a real `mouse.down()` press; and a hardcoded accent hex, swapped to reading the live `--accent` token) — see `S20-frameless-icon-restyle.md`'s Results for the disclosure. Zero console/page errors, zero dialogs, zero non-`file://` network requests. Full transcript: `c:/tmp/pw-test/s1-s20-run2.log`; narrative + verbatim PASS lines in the S19/S20 test-plan files. |
| 2026-09-10 | S21-S25 aisle rework (S21 settings shell, S22 native `<select>` aisle field + persisted `state.aisles` set, S23 create/rename/delete aisles, S24 in-dropdown add, S25 note-field ≥16px) — independent Tester formal pass against pushed sha `027672a` + a full S1-S20 regression re-run carrying the S22/S16 net-supersession RETROFIT | New canonical script, `c:\tmp\pw-test\vopping-tests-tester-s1-s25-formal.js` (Tester-independent, rebuilt from `vopping-tests-tester-s1-s20-formal.js` — supersedes it as citation of record for the whole regression baseline; the s1-s20 script is retired as a stale free-text-aisle-era artifact). Retrofit: helper `setAisleFor()` rewritten to drive the native `<select>`; Part 2 S8 free-text cases (affordance/input/datalist/tag/free-text-editor) rewritten to the select (80→72); Part 2 S9 By-Aisle group label "Unassigned"→"Other" bucket label; Part 3 S16 ⚑ icon-only compact block rewritten to the M23 inline-select-in-all-modes supersession (26→22); Part 6 S20 removed-accent-icon sub-check retired (12→11); TC7.1/7.7/7.12/7.13/15.2/15.16/20.4 single-line/no-`.row-meta` expectations updated for S22's always-present select; added Parts 7-11 (S22/S23/S24/S21/S25) | 268/269 assertions passed (1 FAIL = defect D1, a Minor S23-rename-stay-open gap — NOT a regression; S21/S22/S24/S25 all clean); zero console/page errors, zero dialogs, zero non-`file://` network requests | 268/269 | **SUPERSEDED 2026-09-11 by the 269/269 row below — D1 is now fixed and re-verified green; do not cite this row's total going forward. Was the canonical figure at 268/269 with 1 open Minor defect D1.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: retrofitted S1-S20 baseline = 67 (Part 1) + 72 (Part 2) + 22 (Part 3) + 13 (Part 4, S19) + 24 (Part 5, S15) + 11 (Part 6, S20) = 209; new S21-S25 coverage = 19 (Part 7, S22) + 18 (Part 8, S23) + 9 (Part 9, S24) + 11 (Part 10, S21) + 3 (Part 11, S25) = 60; 209 + 60 = 269 checks, 268 pass. **Net baseline delta 222→209 (−13) is by-design supersession, not lost coverage:** S22 replaces the free-text aisle mechanism with a native `<select>`, so Part 2's free-text S8 checks (80→72, −8) and Part 3's S16 ⚑ compact checks (26→22, −4) are retired/rewritten to the new mechanism, and S20's removed-accent-icon sub-check is retired (12→11, −1) — the same net-negative supersession pattern as S13→S5 (74→67) and S19→S13 (246→222). **The single FAIL is defect D1 (Minor, OPEN), not a regression:** on an EXPLICIT invalid/colliding RENAME commit in Settings the editor closes+reverts instead of staying open with an inline message (violates S23 NB-3) — the rename is still correctly REJECTED (state unchanged, no data corruption); the create-side flow correctly stays open (the asymmetry pins it as an unintended gap); root cause = the Settings `focusout` revert handler lacks the `activeElement` guard the row-level editor handler has (script.js ~1608 vs ~1363-1365). Confirmed via `c:/tmp/pw-test/vop-probe-rename.js`. Routed to Developer. **S21, S22, S24, S25 are clean (Done citation); S23's Done-flip is blocked pending the D1 fix + a TC23.10 re-verify — this row becomes the clean 269 baseline once D1 lands.** Per-story plans: `S21-settings-menu-shell.md`, `S22-aisle-select-persisted-set.md`, `S23-aisle-crud-settings.md`, `S24-add-aisle-from-dropdown.md`, `S25-note-input-zoom-fix.md`. Full transcript: `c:/tmp/pw-test/s1-s25-run2.log`. |
| 2026-09-11 | S23 (create/rename/delete aisles in Settings) — defect D1 re-verification: the sole open Minor defect from the 268/269 row above is FIXED. Developer added the `document.activeElement` guard to the Settings rename `focusout` deferred-revert handler (script.js ~1614-1622), mirroring the row-editor's guard (~1363-1365), so an explicit invalid/colliding RENAME commit now STAYS OPEN with its inline error instead of reverting+closing (NB-3). Fix present in the local working tree, uncommitted at re-verify time (expected — Orchestrator commits after this re-verify). Independent Tester re-pass = full S1-S25 regression re-run + TC23.10 strengthened in place | Same combined suite, `c:\tmp\pw-test\vopping-tests-tester-s1-s25-formal.js` — no scope/filename change (Part 8 = S23). TC23.10 strengthened in place to explicitly assert the rename input STAYS PRESENT (`renInputOpen === 1`), not just the inline error span, so the D1 "stays open" clause is asserted directly rather than inferred — still one check, no count change. D1 re-verify independently corroborated by the multi-delay probe `c:\tmp\pw-test\vop-probe-rename.js` | 269/269 assertions passed (67 Part 1 + 72 Part 2 + 22 Part 3 + 13 Part 4 S19 + 24 Part 5 S15 + 11 Part 6 S20 + 19 Part 7 S22 + 18 Part 8 S23 + 9 Part 9 S24 + 11 Part 10 S21 + 3 Part 11 S25; zero regressions) | 269/269 | **SUPERSEDED 2026-09-11 by the S26+S27 row below (do not cite this row's total going forward — the `vopping-tests-tester-s1-s25-formal.js` script is retired as a stale S22-era-layout artifact: it would now FAIL on the deliberately-changed S26 single-line/primary-line layout, the same net-supersession pattern as S22→S8 and S19→S13). Was the canonical 269/269 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 67 + 72 + 22 + 13 + 24 + 11 + 19 + 18 + 9 + 11 + 3 = 269. **The +1 vs the 268/269 row above is TC23.10 now PASSING (D1 closed):** on an EXPLICIT invalid/colliding RENAME commit the editor now STAYS OPEN (`renInputOpen=1`) with its inline message (`renErr=1`, "That aisle already exists.") and leaves `state.aisles` unchanged — matching the create-side flow (the asymmetry that pinned D1 is gone). No check count changed (still 18 in Part 8 / 269 total): TC23.10 flipped FAIL→PASS in place, it was not a newly added check. Adjacent S23 paths re-confirmed clean in the same run: TC23.6 create-invalid still stays open with its message; TC23.2/TC23.9 valid rename commits+closes+cascades by normalized key; TC23.11 (M22) blur-dismiss of a rename still reverts+closes; TC23.4 delete-in-use still reverts affected items to the no-aisle bucket (`''`). Probe corroboration: `vop-probe-rename.js` now shows `renameInput=1, renameError=1` at ALL sampled delays (+10/+30/+60/+120/+250ms) vs pre-fix `renameInput=0` already at +10ms; final aisles unchanged. Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. **S23 is now clear for its Done-flip; S21/S22/S24/S25 remain clean.** Full transcript: `c:/tmp/pw-test/s1-s25-run3-d1fix.log`; probe transcript `c:/tmp/pw-test/vop-probe-rename-d1fix.log`. Per-story plan: `S23-aisle-crud-settings.md` (updated to 18/18 DONE). |
| 2026-09-11 | S26 (per-item aisle affordance moved to a PRIMARY-line collapsed-`<select>` icon; density fix — aisle-less+note-less rows are single-line again) + S27 (per-glyph row-icon font-size tuning, glyph-only within the fixed ~19.5px boxes) + the two-part iOS add-aisle fix (part-1 open-transition grace window; part-2 deferred-`focusout` commits a valid name on blur, not only on Enter) — independent Tester combined formal pass against pushed sha `7f6a327` + a full S1-S25 regression re-run carrying the S26/S27 layout RETROFIT | New canonical script, `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js` (Tester-independent, extended from `vopping-tests-tester-s1-s25-formal.js` — supersedes it as citation of record for the whole regression baseline; the s1-s25 script is retired as a stale S22-era-layout artifact). Retrofit (all in-place FAIL→PASS flips, NO count change to the 269 baseline): TC7.1/TC7.12 (single-line-when-empty RE-ESTABLISHED by S26 — S22 had superseded it), TC8.14 (M24: select on the PRIMARY line, aisle-less row emits NO `.row-meta`), TC16.8/TC16.9 (By-Aisle: control on primary line in every mode, per-row tag SUPPRESSED), TC15.2/TC15.16 (a plain row's name editor adds no second line — `.row-meta` count 0), TC20.4 (plain row single-line ~36px again), TC20.5 (delete glyph now S27-tuned to 1.35rem, ≥ the S20 1.15rem baseline), TC24.5 (C1 name-never-corrupted guard re-expressed via an INVALID name blurred past the grace window, since a VALID blur now commits), TC24.8 (M22 TIMING retrofit — the genuine-abandon revert must occur PAST the 400ms grace window, a within-window blur is legitimately skipped by the part-1 fix); added Parts 12 (S26), 13 (S27 G1-G4), 14 (add-aisle desktop matrix a-g + R14 cross-row re-proof) | 294/294 assertions passed (269 retrofitted S1-S25 baseline all re-confirmed clean + 25 new S26/S27/add-aisle checks; zero regressions) | 294/294 | **SUPERSEDED 2026-09-14 by the afcd725 S26+S27 FINAL-BUILD row below — the SAME `vopping-tests-tester-s1-s27-formal.js` script was extended in place to 308/308 (Parts 15+16 added for the post-7f6a327 undo-toast + iOS round-3/round-4 fixes); do not cite this row's total going forward. S26/S27's own AC coverage (Parts 12-14) is unchanged and carried forward there. Was the canonical 294/294 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 67 (Part 1) + 72 (Part 2) + 22 (Part 3) + 13 (Part 4 S19) + 24 (Part 5 S15) + 11 (Part 6 S20) + 19 (Part 7 S22) + 18 (Part 8 S23) + 9 (Part 9 S24) + 11 (Part 10 S21) + 3 (Part 11 S25) = 269 retrofitted baseline; + 11 (Part 12 S26 presentation+density C2) + 7 (Part 13 S27 G1-G4 per-glyph sizing) + 7 (Part 14 add-aisle iOS-fix desktop matrix a-g + R14 cross-row re-proof) = 25 new; 269 + 25 = 294. **The baseline stayed 269 (not a decrease): the 8 S26/S27 layout retrofits + 2 add-aisle retrofits are in-place FAIL→PASS flips of checks already counted in 269 — they re-point from the S22 two-line/second-line layout to the S26 single-line/primary-line layout, no checks added or dropped** (contrast the S22 268/269 and S19 246→222 rows, which retired whole mechanisms). Part 12 covers `test-plans/S26-aisle-icon-primary-line.md` (S26.1-S26.11): primary-line `.aisle-control` `<select>` (never in `.row-meta`), `pointer-events:none`+`aria-hidden` glyph overlay (N15), MEASURED 19.5×19.5px footprint (M25 — not assumed icon-btn parity), C2 density (aisle-less+note-less row single-line ~36px, no `.row-meta`), N14 `:empty` guard, `change`-commit intact, select persists across re-render (R14 structural invariant), Tag-when-set shown Manual/Alpha + SUPPRESSED By-Aisle + inert. Part 13 covers `test-plans/S27-row-icon-glyph-sizing.md` (G1-G4 + S27.5-S27.7): boxes fixed 19.5px (G1), non-note glyphs no longer all 1.15rem (G2), each ≥1.15rem (G3), no row growth (G4), shipped values (note-toggle 1.15rem reference / edit+delete 1.35rem / up+down shared 1.28rem), aisle overlay glyph 1.35rem (M26), base fallback retained (N16). Part 14 (the two-part iOS add-aisle fix desktop proxy): (a) Enter creates+assigns+persists+closes; (b) KEY — a valid name blurred with NO Enter (iOS Go proxy) COMMITS (part-2 fix); (c) empty+late-blur reverts; (d) empty+within-window-blur stays open (part-1 fix); (e) invalid/duplicate blurred reverts with no dup and no loop; (f) picking an existing aisle commits on `change`; + the R14 cross-row re-proof (open note editor on row B + change row A's aisle → both commit, select not destroyed, zero errors). The new add-aisle commit rule is gated to `field === 'new-aisle'`; the aisle-`<select>` `change`-commit handler + its `activeElement` picker-open guard are byte-for-byte untouched (verified by code inspection, script.js ~1439-1527, re-proven by the R14 check). NB-6 respected throughout (native picker open + on-device Go/blur commit + on-device glyph "match" are real-device signals, not desktop gates). Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `c:/tmp/pw-test/s1-s27-run1.log`. |
| 2026-09-14 | S26 + S27 FINAL-BUILD combined formal pass against pushed sha `afcd725` — the ONE final combined pass. Adds desktop coverage for the three fixes that landed AFTER 7f6a327: the UNDO-TOAST wiring (afcd725 — the toast's "Undo" is now a REAL `[data-role="toast-undo"]` button on the SAME `performUndo()`, previously inert text) and the iOS add-aisle ROUND-3 (a2c5f61, post-commit sentinel re-entry guard) + ROUND-4 (028150c, renderList focus-restore SKIPS re-focusing `data-role="aisle-select"`). Full S1-S27 regression re-run confirms S26/S27's own code is byte-unchanged from 7f6a327 and clean against afcd725. | Same canonical suite `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js`, EXTENDED IN PLACE (same filename/scope — the "S1-S27" name was never a literal enumeration; same in-place-extension pattern as S13 216→219 and S15 246): added Part 15 (undo-toast, 10 checks) + Part 16 (round-4 activeElement proxy + round-3 re-entry guard, 4 checks). Parts 1-14 (the 294 baseline) re-run VERBATIM and re-confirmed clean — S26/S27's own layout is byte-identical to 7f6a327, so NO retrofit was needed (only the add-aisle round-3/round-4 fixes and the undo-toast wiring landed after it, and none touch S26/S27's DOM/CSS). | 308/308 assertions passed (294 prior baseline all re-confirmed clean + 14 new afcd725-fix checks; zero regressions) | 308/308 | **SUPERSEDED 2026-09-14 by the S28+S29 Increment-1 row below (345/345) — the SAME `vopping-tests-tester-s1-s27-formal.js` script was extended in place (Parts 17+18) AND retrofitted in place for the S28 18-aisle taxonomy; running the pre-S28 version as-is would now FAIL on the deliberately-changed seeded aisle set. Do not cite this row's total going forward. Was the canonical 308/308 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 67 (Part 1) + 72 (Part 2) + 22 (Part 3) + 13 (Part 4 S19) + 24 (Part 5 S15) + 11 (Part 6 S20) + 19 (Part 7 S22) + 18 (Part 8 S23) + 9 (Part 9 S24) + 11 (Part 10 S21) + 3 (Part 11 S25) + 11 (Part 12 S26) + 7 (Part 13 S27 G1-G4) + 7 (Part 14 add-aisle matrix a-g + R14) = 294 prior baseline; + 10 (Part 15 undo-toast) + 4 (Part 16 round-4/round-3) = 14 new; 294 + 14 = 308. **The baseline stayed 294 (NO decrease and no retrofit): unlike the S22→S8/S19→S13 supersession rows, S26/S27's own layout is unchanged from 7f6a327, so Parts 1-14 are a verbatim re-run and the 14 new checks are purely ADDITIVE coverage for the three post-7f6a327 desktop-verifiable fixes.** Part 15 closes the long-standing undo-toast COVERAGE GAP (every prior suite only clicked the header `#undo-btn`, never the toast): the toast's real `[data-role="toast-undo"]` button restores for BOTH the S3 delete toast AND the S12 clear-checked toast, preserving name+checked+note+aisle+original-index (TOAST.3/TOAST.6); the header `#undo-btn` still works (TOAST.7); the stale-toast no-op guard holds — after a newer non-toast action clobbers the single-slot buffer, tapping the stale toast's Undo is a safe DISMISS-ONLY no-op, not a wrong-undo (TOAST.8); the exact toast textContent is byte-unchanged (`Deleted "X" — Undo` / `Cleared N items — Undo`, TOAST.1/TOAST.5); and the new `.toast-undo` button does not overflow the toast/viewport at 320/390px (TOAST.9). Part 16 — round-4 (028150c): with the aisle `<select>` focused AT commit-time (the iOS precondition), the rebuilt `<select>` is NOT re-focused after the change-commit rebuild, so the iOS picker-reopen-after-every-commit path cannot fire (R4.1); the aisle-only refocus-skip did NOT weaken focus-preservation for other controls (R4.2). round-3 (a2c5f61): a sentinel `change` re-fired on the SAME row within the 400ms post-create window does NOT re-open the reveal and snaps the select back to the created aisle (R3.1), while a genuine re-open AFTER the window still works (R3.2 — time+row scoped, never a permanent block). One test-authoring bug found and fixed while writing this pass (NOT an app defect): R4.1's first draft over-constrained the expected activeElement to `<body>`/`<li>` and drove the commit via `selectOption` (which doesn't guarantee the select is focused at commit-time); rewritten to focus the select first and dispatch `change` synchronously, asserting only that the rebuilt select is not re-focused — the app was correct all along (the draft's lone "fail" was the select correctly NOT being focused; activeElement had bounced to `#add-input`). The M24/R7 real-footprint overflow re-check runs as the SEPARATE targeted closure script `vopping-worst-case-row-s26-s27-closure.js` (9/9 PASS, re-confirmed clean against afcd725: collapsed `.aisle-control` MEASURES 19.5×19.5px at all four widths, zero overflow, row 36.17px, item-name space 117px@320→187px@390) — not folded into this cumulative total per the "Targeted closure checks" convention below. NB-6 respected: the iOS picker-reopen / Go-blur commit / on-device glyph "match" are real-device signals (PO-confirmed on-device), asserted here only via desktop structural proxies. Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `c:/tmp/pw-test/s1-s27-run2-afcd725.log`. Per-story plans: `S26-aisle-icon-primary-line.md`, `S27-row-icon-glyph-sizing.md`. **S26 AND S27 are both CLEAR for their Done-flip (308/308, 0 open defects).** |
| 2026-09-14 | S28 (versioned one-time 18-aisle taxonomy re-seed migration) + S29 (item→aisle dictionary graduation + matcher engine, DOM-free hook) — Auto-Aisle Increment 1 (Sprint 5). Independent Tester combined formal pass against the WORKING-TREE build (uncommitted at pass time: `aisle-dictionary.js` NEW, `index.html` +dictionary `<script>` BEFORE `script.js`, `script.js` +`migrateAislesV2` + the matcher block + `window.__voppingAutoAisle`) + a full S1-S27 regression re-run carrying the S28 taxonomy RETROFIT | Same canonical suite `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js`, EXTENDED IN PLACE (same filename/scope — the "S1-S27" name was never a literal enumeration; same in-place-extension pattern as S13 216→219, S15 246, and the afcd725 294→308 row). Retrofit — all in-place FAIL→PASS re-points to the S28 18-aisle taxonomy, NO count change to the 308 baseline: TC8.2 fresh-seed option list (8 old starters → 17 store-walk starters / 19 options); TC22.1 + TC22.9 migration/fresh-install aisle-set expectations updated for the post-S28 taxonomy AND for `migrateAislesV2` now post-processing every load; TC23.6/TC23.9/TC23.10/TC24.7 + Part-14 matrix(e) collision fixtures re-pointed from the removed "Dairy" starter to the surviving "Produce"/"Bakery" starters; Part-14 matrix(b)'s "novel" add-aisle name "Deli"→"Garage" (Deli is now a seeded starter); every fresh-seed `setAisleFor`/assert "Dairy"→"Dairy & Eggs"; TC22.14 "Meat/Seafood"→"Meat". Added Part 17 (S28, 11) + Part 18 (S29, 26). | 345/345 assertions passed (308 retrofitted S1-S27 baseline all re-confirmed clean + 37 new S28/S29 checks; zero regressions) | 345/345 | **SUPERSEDED 2026-09-15 by the S30 row below (379/379) — the SAME `vopping-tests-tester-s1-s27-formal.js` script was extended IN PLACE (Part 19) with S30's 34 purely-additive checks; do not cite this row's total going forward. S28/S29's own AC coverage (Parts 17-18) is byte-unchanged and carried forward there — this is an additive extension, NOT a retire-and-rebuild. Was the canonical 345/345 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 269 (Parts 1-11 unchanged) + 11 (Part 12 S26) + 7 (Part 13 S27) + 7 (Part 14 add-aisle) + 10 (Part 15 undo-toast) + 4 (Part 16 round-3/4) = 308 retrofitted baseline; + 11 (Part 17 S28) + 26 (Part 18 S29) = 37 new; 308 + 37 = 345. **The baseline stayed 308 (in-place taxonomy retrofit, NO count change and NO mechanism retired — contrast the S22 268/269 and S19 246→222 rows which retired whole mechanisms; here the 8-aisle→18-aisle taxonomy change re-points existing checks, and the 37 new checks are purely ADDITIVE).** Part 17 covers `test-plans/S28-versioned-reseed-migration.md` (S28.1-S28.11): fresh store-walk seed + `seedVersion` stamp (S28.1); pre-S28 device rename Dairy→"Dairy & Eggs" + Household→"Household & Cleaning" with item re-tag by normalized key incl. the lowercase "dairy" variant (S28.2/S28.3); "Meat/Seafood" kept as a custom aisle while "Meat"+"Seafood" added fresh (S28.4); M27 rename-in-place + appended-new-starters order for existing users (S28.5); no item re-mapped except the two renames + `seedVersion` stamped (S28.6); MERGE-not-duplicate when the rename target pre-exists (S28.7); N13 no-resurrect of a deleted rename-source AND of deleted carry-over starters (S28.8/S28.9); idempotency — reload after migration is a byte-identical no-op (S28.10) + the `seedVersion` gate is a hard one-time gate that never re-fires (S28.11). Part 18 covers `test-plans/S29-matcher-engine.md` (S29.1-S29.26): tiers 2-5 incl. quantity-strip + multi-word/trailing-descriptor tier-4 (S29.1-S29.6); the 68 cross-aisle collisions resolving canonical-first (broccoli→Produce, shrimp→Seafood, both tier 2 not Frozen — S29.7/S29.8) + the drumstick→Meat first-write-wins alias-vs-alias tie (S29.9); whole-token anti-examples ham/hamburger, corn/popcorn, ice/rice, pepper/peppercorns, oat/goat (S29.10-S29.14, all proven substring-safe by matchedText/aisle); tier-1 personal override precedence incl. empty-value-not-learned (Q2) and normalized-key (S29.15-S29.17); the data-integrity gate PASSING on the shipped dictionary (count 1286 across 18 keys) with the lemon→"difficult" whitelist entry (S29.18/S29.19), and the gate as a HARD test-only fail-fast that throws on collision/within-dup/non-object (S29.20); seed↔dict parity (S29.21); the R16 degrade seam — empty/null/malformed dictionary yields tier 5 with empty maps and NEVER throws, tier 1 still resolves dictionary-independently, and matchAisle(null|number|undefined) return tier 5 without throwing (S29.22-S29.24); a perf proxy — buildLookup < 25ms (2.0ms) and 300 matchAisle calls < 50ms (0.1ms) — plus a tier-short-circuit proxy (S29.25/S29.26). **3 test-authoring bugs found+fixed while writing this pass, NONE an app defect (the app was correct in all three):** matrix(b) picked "Deli" as a novel add-aisle name but S28 made it a seeded starter (app correctly rejected the duplicate) → fixture renamed to "Garage"; S29.4 + S29.26 assumed "boneless chicken breast" was a tier-4 token match but it is a real tier-3 EXACT ALIAS → Meat (correct) → re-pointed to "chicken breast (organic)" for a genuine tier-4. This increment is desktop-verifiable (pure migration + matcher logic; NO NB-6 device signal — the ⭍ glyph + ⌖-picker interaction arrive at S31/S32). Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `c:/tmp/pw-test/s1-s29-run2.log`. **S28 AND S29 are both CLEAR for their Done-flip (345/345, 0 open defects).** |
| 2026-09-15 | S30 (personal override map / learning — Auto-Aisle Increment 2, the tier-1 learning layer on top of S29's matcher). Independent Tester formal pass against the WORKING-TREE build (uncommitted at pass time: `script.js` +158 — `OVERRIDES_KEY`/`loadOverrides`/`saveOverrides`/`parseOverrideState`/`recordOverride`; `matchAisle` tier-1 defaulting to the persisted override map; `saveAisle` record-on-hand-pick + `{viaAuto:true}` suppress; `renameAisleByKey`/`deleteAisleByKey` override cascades; `window.__voppingAutoAisle` gains `getOverrides()` + `setItemAisle(id,aisle,viaAuto)`) + a full S1-S29 regression re-run. **PO decision 2026-09-15 (relayed by Orchestrator): a hand clear-to-no-aisle KEEPS any prior learned override (a later fill-empty auto-run re-fills via tier 1) — build is FINAL as-is, NO Dev tweak.** | Same canonical suite `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js`, EXTENDED IN PLACE (same filename/scope — the "S1-S27" name was never a literal enumeration; same in-place-extension pattern as S13 216→219, S15 246, afcd725 294→308, S28+S29 345). Added Part 19 (S30, 34 checks). NO retrofit and NO count change to the 345 baseline — S30 is a purely ADDITIVE learning layer (a new `vopping-overrides-v1` localStorage key + matcher tier 1); it changes no existing mechanism/DOM/CSS that the prior 345 checks assert, so Parts 1-18 are a verbatim re-run. | 379/379 assertions passed (345 S1-S29 baseline all re-confirmed clean + 34 new S30 checks; zero regressions); zero console/page errors, zero dialogs, zero non-`file://` network requests | 379/379 | **SUPERSEDED 2026-09-15 by the S31+S32 row below (404/404) — the SAME `vopping-tests-tester-s1-s27-formal.js` script was extended IN PLACE (Parts 20+21) with S31/S32's 25 additive checks AND carried 4 in-place stale-BY-DESIGN retrofits (TC8.2/TC8.2b/TC22.9/TC23.3b, for the S32 Auto-detect top option); do not cite this row's total going forward. S30's own AC coverage (Part 19) is byte-unchanged and carried forward there. Was the canonical 379/379 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 345 (Parts 1-18 unchanged) + 34 (Part 19 S30) = 379. **The baseline stayed 345 (purely additive, NO decrease and NO mechanism retired — contrast the S22 268/269 and S19 246→222 rows which retired whole mechanisms).** Part 19 covers `test-plans/S30-personal-override-map.md` (S30.1-S30.34): learn-on-hand-pick + persist to `vopping-overrides-v1` + survive-reload + tier 1 (S30.1-4); tier 1 BEATS the dictionary via a divergent-aisle hand pick — banana learned to "Snacks & Candy" resolves tier 1 ahead of the Produce canonical (S30.5-6); learn ONLY on a hand pick — `setItemAisle(viaAuto=true)` sets the aisle but records NOTHING, a subsequent hand pick DOES learn (S30.7-9); **THE PO clear-decision, asserted by KEY PRESENCE (not just `!== ''`, strengthening the Dev self-check's weak Scenario-4)** — a hand clear-to-no-aisle KEEPS the prior learned override intact ("eggs" still a key → "Dairy & Eggs") and `matchAisle("eggs")` still resolves it at tier 1 for a later fill-empty re-fill, while clearing a never-learned item records nothing (Q2) (S30.10-15); custom-aisle create from the dropdown learns + resolves tier 1 + is added to `state.aisles`, and R15 auto-assign of an absent aisle adds it to `state.aisles` WITHOUT learning (S30.16-21); the R1 defensive-parse corruption battery — not-JSON / top-level-array / literal-null / number all degrade to an EMPTY map with no crash + no new console errors, and a MIXED object drops the non-string value, the empty-string value, and the empty key while keeping the valid string→string pairs (S30.22-27); the S23 RENAME cascade updates the override TARGET so the learning survives (yogurt → "Chilled") + item retag (NB-2) + persists (S30.28-30); the S23 DELETE cascade DROPS the override (M28: DELETED, never blanked to `''`) + falls back to the DICTIONARY (yogurt → Dairy & Eggs tier 2, the static dictionary being unaffected by the `state.aisles` delete) + persists (S30.31-34). **S30 is desktop-verifiable — it carries NO NB-6 device signal** (the ⭍ list-run button + the ⌖ per-item Auto-detect option arrive at S31/S32). Zero test-authoring bugs this pass (the exact-value fixtures — milk/eggs/yogurt→Dairy & Eggs tier 2, banana→Produce tier 2, "protein powder"→tier 5 — were verified against `aisle-dictionary.js` before asserting). Zero console/page errors, zero dialogs, zero non-`file://` network requests across the entire run. Full transcript: `c:/tmp/pw-test/s1-s30-run1.log`. Per-story plan: `test-plans/S30-personal-override-map.md` (STATUS DONE). **S30 is CLEAR for its Done-flip (379/379, 0 open defects).** |
| 2026-09-15 | S31 (list-wide "Auto-aisle" fill-empty-only run — the ⭍ button in `.sort-controls`) + S32 (per-item "Auto-detect" option in the ⌖ aisle `<select>`) — Auto-Aisle Increment 3, the two on-demand triggers atop the S29 matcher + S30 learning. Independent Tester combined formal pass against the pushed WORKING-TREE build sha `3ac58fb` (`runAutoAisle()`; the `'aisle-bulk'` undo branch; the `#auto-aisle-btn` listener in `.sort-controls`; the S32 `data-autodetect` top `<option>` + its structural change-handler branch writing via `saveAisle({viaAuto:true})` with the tier-5 NO-MATCH GUARD; N12 reserving the "Auto-detect" name) + a full S1-S30 regression re-run carrying the S32 option-order RETROFIT. | Same canonical suite `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js`, EXTENDED IN PLACE (same filename/scope — the "S1-S27" name was never a literal enumeration; same in-place-extension pattern as S13 216→219, S15 246, afcd725 294→308, S28+S29 345, S30 379). Added Part 20 (S31, 16) + Part 21 (S32, 9). Retrofit — 4 in-place FAIL→PASS re-points to the S32 Auto-detect top option (`__VOPPING_AUTODETECT__` + `data-autodetect="1"` at index 0, no-aisle now at index 1, "+ Add new aisle…" still LAST, option count 19→20), NO count change to the 379 baseline: TC8.2 (starter-set identity now filters out the structural autodetect+sentinel, + asserts option[0].autodetect / 20-option total), TC8.2b (bucket option moved index 0→1), TC22.9 (fresh-install option count 19→20 + autodetect[0] + bucket[1] + sentinel[19]), TC23.3b (renamed bucket now index 1). Helper `aisleSelectInfo` extended to capture `data-autodetect`. | 404/404 assertions passed (379 retrofitted S1-S30 baseline all re-confirmed clean + 25 new S31/S32 checks; zero regressions); zero console/page errors, zero dialogs, zero non-`file://` network requests | 404/404 | **SUPERSEDED 2026-09-16 by the S35+S36 row below (441/441) — the SAME `vopping-tests-tester-s1-s27-formal.js` script was extended IN PLACE (Parts 22+23) with S35/S36's 37 additive checks AND carried in-place stale-BY-DESIGN retrofits for the S36 19-aisle taxonomy (S28/S29/TC8.2/TC22.1/TC22.9 + the S31.7 fixture); do not cite this row's total going forward. S31/S32's own AC coverage (Parts 20-21) is byte-unchanged and carried forward there. Was the canonical 404/404 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 379 (Parts 1-19; the 4 TC8.2/8.2b/22.9/23.3b retrofits are in-place FAIL→PASS flips, NOT count changes) + 16 (Part 20 S31) + 9 (Part 21 S32) = 404. **The baseline stayed 379 (the 4 retrofits re-point existing checks to the new option order — a stale-BY-DESIGN supersession like S22→S8, NOT lost coverage; the 25 new checks are purely additive).** Part 20 covers `test-plans/S31-list-wide-auto-aisle-run.md` (S31.1-S31.16): fill-empty-ONLY — a set aisle is never touched, a matched empty is assigned, a no-match empty is left `''` (S31.1-3); the THREE EXACT frozen toast strings, code-point-pinned (`Sorted 1 ·[U+00B7] 1 couldn't be matched —[U+2014] Undo`; `Sorted 3 — Undo`; the feedback-only `2 couldn't be matched` with NO live undo) (S31.4/6/7/8); the n=0 feedback run does NOT clobber the S6 slot (S31.9); ONE undoable `aisle-bulk` restoring every touched item to prevAisle `''` via BOTH the header `#undo-btn` and the toast's own armed Undo (proving `setLastAction` ran BEFORE `showToast`) (S31.5/10/11); the bulk action DISPLACES the single S6 slot (delete→auto-run→Undo reverses the bulk, the earlier delete stays) (S31.12); zero-eligible = fully silent no-op — no toast, no aisle change, slot untouched (S31.13); a STALE bulk-toast Undo after a newer non-toast action is dismiss-only (S31.14); the run records NO override (tier 1 stays clean) (S31.15); M29 `.sort-controls` no overflow 320/360/375/390px with the ⭍ button (S31.16). Part 21 covers `test-plans/S32-per-item-auto-detect.md` (S32.1-S32.9): Auto-detect is the STRUCTURAL top option (data-autodetect, never by label, never `selected`) (S32.1); selecting it runs the matcher + sets via `viaAuto` WITHOUT learning (S32.2/3); MAY overwrite an existing differing aisle on a match (Bakery→Produce) still without learning (S32.4/5); the NO-MATCH GUARD — a tier-5 item keeps its existing aisle intact, writes nothing, and the picker resets to the current aisle (never stuck on the transient Auto-detect option) (S32.6/7); N12 rejects a user aisle named "Auto-detect" as reserved (S32.8); rides the R14-safe change→`saveAisle`→`render` path with no new seam — Auto-detect on row A commits its match AND flushes a still-open note draft on row B, both `<select>`s survive, zero console errors (S32.9). **DESKTOP-verifiable EXCEPT the ⭍ glyph render + the ⌖-picker on-device interaction — the PO's NB-6 signals, asserted here only via desktop structural proxies (button `data-role`/click, `data-autodetect`, the picker-reset selected-value), running in parallel on-device.** The S26/S27 worst-case-row closure is unaffected (S31's button lives in `.sort-controls`, not the row; S32's added `<option>` does not change the CSS-collapsed select footprint). Zero test-authoring bugs this pass. Full transcript: `c:/tmp/pw-test/s1-s32-run1.log`. Per-story plans: `test-plans/S31-list-wide-auto-aisle-run.md`, `test-plans/S32-per-item-auto-detect.md`. **S31 AND S32 are both CLEAR for their Done-flip on the desktop-verifiable AC (404/404, 0 open defects) — the Done-flip ALSO waits on the PO's on-device NB-6 confirm (running in parallel).** |
| 2026-09-16 | S35 (dictionary correctness fix pass — data-only: ~16 missing bare head-word canonicals added + `chicken` RE-HOMED to Meat via canonical-beats-alias) + S36 (new "International" aisle — taxonomy 18→19 real + the v3 versioned re-seed migration + dictionary population incl. the tortilla MOVE out of Bakery) — Sprint 7 Auto-Aisle dictionary/taxonomy follow-ups. Independent Tester combined formal pass against the WORKING-TREE build (uncommitted, push HELD at pass time: `aisle-dictionary.js` +S35 bare head-word canonicals + a new Meat `chicken` canonical + the new `International` dictionary key with `tortilla`/`corn tortilla`/`flour tortilla` relocated out of Bakery; `script.js` +`International` in `AISLE_STARTER_LIST` at store-walk index 12, `AISLE_SEED_VERSION` 2→3, `migrateAislesV2` re-pinned to the LITERAL gate/stamp 2, a NEW `migrateAislesV3` pinned to the LITERAL gate/stamp 3, run V2-then-V3 after `loadState()`) + a full S1-S32 regression re-run carrying the S36 19-aisle taxonomy RETROFIT. Dev self-verified 113/113 on its own `vop-s35-s36-verify.js` (reference only — this is the independent pass). | Same canonical suite `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js`, EXTENDED IN PLACE (same filename/scope — the "S1-S27" name was never a literal enumeration; same in-place-extension pattern as S13 216→219, S15 246, afcd725 294→308, S28+S29 345, S30 379, S31+S32 404). Added Part 22 (S35, 22) + Part 23 (S36, 15). Retrofit — 12 in-place FAIL→PASS re-points to the S36 19-aisle taxonomy + the S35 dictionary, NO count change to the 404 baseline: TC8.2 (per-item `<select>` now 18 store-walk starters / 21 options, "International" at index 12); TC22.1 (pre-seedVersion migration union — "International" seeded in-slot at index 12); TC22.9 (fresh-install 18 starters / 21 options / sentinel[20]); S28.1 (fresh seed 18 real + seedVersion 3 + hook SEED_VERSION 3); S28.5 (existing-device append order — 10 v2 new starters THEN the single v3 "International" at the tail, length 19); S28.6/S28.9/S28.10 (seedVersion 2→3 + "International" now present via V3); S28.11 (REWRITTEN to assert THE data-safety fix — a v2 device does NOT re-run V2 / resurrect a starter when the global bumps to 3, while V3 adds "International" and advances 2→3); S29.18 (integrity count 1286→1324 across 18→19 keys); + the S31.7/S31.8 fixture swap ('rice' now RESOLVES via S35 so the no-match fixture item was changed to the genuinely-unmatchable 'wibblewobble'); + label-only touch-ups S29.21/S29.25. **6 of these 12 retrofits (TC8.2, TC22.1, TC22.9, S28.1, S28.5, S28.9) were caught by THIS independent pass BEYOND the 6 the Dev flagged (S28.6/S28.10/S28.11/S29.18/S31.7/S31.8) — a naive pass touching only the flagged 6 would have hit 6 more FAILs.** | 441/441 assertions passed (404 retrofitted S1-S32 baseline all re-confirmed clean + 37 new S35/S36 checks; zero regressions); zero console/page errors, zero dialogs, zero non-`file://` network requests; reproduced twice (`s35-s36-runA.log` + `s35-s36-runB.log`, both 441/441) | 441/441 | **SUPERSEDED 2026-09-16 by the S33+S34 icon-picker row below (cite that row, 482/482) — the SAME `vopping-tests-tester-s1-s27-formal.js` script was extended IN PLACE (Parts 24+25) with S33/S34's 41 purely-additive checks; do not cite this row's total going forward. S35/S36's own AC coverage (Parts 22-23) is byte-unchanged and carried forward there. Was the canonical 441/441 figure with 0 open defects.** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 404 (Parts 1-21; the 12 retrofits above are in-place FAIL→PASS flips, NOT count changes) + 22 (Part 22 S35) + 15 (Part 23 S36) = 441. **The baseline stayed 404 (in-place taxonomy/dictionary retrofit, NO count change and NO mechanism retired — contrast the S22 268/269 and S19 246→222 rows which retired whole mechanisms; here the 18-aisle→19-aisle taxonomy change + the S35 dictionary edits re-point existing checks, and the 37 new checks are purely ADDITIVE).** Part 22 covers `test-plans/S35-dictionary-correctness.md` (S35.1-S35.22): every added bare head-word canonical resolves at tier 2 to its intended aisle — cheese→Dairy & Eggs, rice/pasta/noodles→Pasta Rice & Grains, beef/steak/sausage/pork→Meat, fish→Seafood, vinegar/oil→Condiments & Sauces, cream→Dairy & Eggs, juice→Beverages, vitamins→Personal Care & Health, battery→Household & Cleaning (+ the "batteries" plural via alias) (S35.1-15); THE re-home — bare "chicken"→Meat at tier 2 (canonical-beats-alias) while "canned chicken"→Canned & Jarred stays intact at tier 2 (S35.16/17); regression — chicken breast/ground beef/olive oil/heavy cream/vegetable oil unbroken at tier 2 and "canned chicken breast" still Canned & Jarred (tier 2/3 exact fires before the new bare tier-4 tokens) (S35.18); compound-beats-bare — tuna steak→Seafood, chicken broth→Canned & Jarred, fish sticks→Frozen (a longer EXACT canonical wins over the new bare head-word) (S35.19); the new bare head-words EXTEND tier-4 coverage — canola oil→Condiments & Sauces, chicken parmesan→Meat (tier-4 via the new "chicken" token, resolving to Meat not Canned) (S35.20); validateDictionary CLEAN (count 1324/19, no throw) + still HARD fail-fast on a canonical-vs-canonical collision incl. one on the new International key (S35.21/22). Part 23 covers `test-plans/S36-international-aisle.md` (S36.1-S36.15): the v3 migration end-to-end via `localStorage` round-trips — fresh install seeds "International" in its store-walk slot (index 12) at seedVersion 3 (S36.1); an existing v2 device gets it appended at the TAIL exactly once, all prior aisles intact, NO item.aisle re-mapped, seedVersion 2→3 (S36.2); **THE N13 data-safety hazard — a v2 device that DELETED the "Deli" starter does NOT get it resurrected when the global bumped to 3 (V2 never re-runs), while V3 adds "International" once; items untouched (S36.3)**; idempotent reload byte-identical (S36.4); v3 no-resurrect of "International" itself after a user delete (S36.5); merge-not-duplicate by NORMALIZED key against a hand-made lowercase "international" (S36.6); a pre-S28 device runs BOTH V2 (Dairy/Household renames + item re-tag + v2 union) AND V3 (International), stamping straight to 3 — proving V2 stamps LITERAL 2 so V3 is NOT skipped (S36.7); dictionary/matcher — tortilla + corn/flour variants + their aliases MOVED to International (no longer Bakery) (S36.8-10), kosher matzo/matzah/gefilte fish/Manischewitz→International (S36.11), taco shells/tostadas→International (S36.12), **the CRITICAL compound International canonicals sesame oil / rice vinegar / rice wine vinegar / plum wine vinegar / rice noodles all resolve to International at tier 2 — NOT split to Condiments (bare oil/vinegar) or Pasta (bare rice/noodles), proving tier-2 exact beats the S35 bare tier-4 tokens (S36.13)**; stays-put — naan/pita/flatbread→Bakery, tortilla chips→Snacks & Candy (the move did NOT over-reach the substring), garam masala/curry powder→Baking & Spices (S36.14); seed↔dict parity holds (18 starters ≡ 18 real dict keys, onlyInStarters=[]/onlyInDict=[]) (S36.15). Zero test-authoring bugs this pass — all dictionary facts (aisle homes, canonical-vs-alias for matzo/battery/tortilla, the compound International entries) were verified against `aisle-dictionary.js` (working tree) before asserting. **This increment is DESKTOP-verifiable end to end — NO NB-6 device signal (International is a data string in a `<select>` option, not a glyph; same class as S28/S29).** Zero console/page errors, zero dialogs, zero non-`file://` network requests across BOTH runs. Full transcript: `c:/tmp/pw-test/s35-s36-runA.log` (reproduced `c:/tmp/pw-test/s35-s36-runB.log`). Per-story plans: `test-plans/S35-dictionary-correctness.md`, `test-plans/S36-international-aisle.md`. **S35 AND S36 are both CLEAR for their Done-flip (441/441, 0 open defects).** |
| 2026-09-16 | S33 (central icon registry + `iconFor()` accessor routing EVERY draw-site + persisted `vopping-icons-v1` override map, R1-defensive parse) + S34 (Settings "Icons" picker panel — per-action label + current glyph + Change→frozen candidate palette, per-action Reset + reset-all, applies+persists live) — the durable iOS-glyph-tofu fix (icon picker; the shipped picker doubles as the PO's on-device decision tool). Independent Tester combined formal pass against the WORKING-TREE build (uncommitted, push HELD): `index.html` (+`data-static-icon` on the 3 static glyph nodes), `script.js` (+`ICON_REGISTRY`/`iconFor()`/`parseIconOverrides`/`isValidIconOverride`/`applyStaticIcons`/`setIcon`/`resetIcon`/`resetAllIcons`/`assertPaletteClean` + the `window.__voppingIcons` hook + `renderIconsSection` + every draw-site routed through `iconFor()`), `style.css` (+the Icons panel CSS, M33 no-clip) + a full S1-S32/S35/S36 regression re-run. Dev self-verified 64/64 on its own `vop-s33-s34-verify.js` (reference only — this is the independent pass). | Same canonical suite `c:\tmp\pw-test\vopping-tests-tester-s1-s27-formal.js`, EXTENDED IN PLACE (same filename/scope; same in-place-extension pattern as S13 216→219, S15 246, afcd725 294→308, S28+S29 345, S30 379, S31+S32 404, S35+S36 441). Added Part 24 (S33, 28) + Part 25 (S34, 13). NO retrofit and NO count change to the 441 baseline — S33/S34 is a purely ADDITIVE icon layer (a new `vopping-icons-v1` key + a Settings section); with no override stored every icon renders byte-identically to today, so Parts 1-23 are a verbatim re-run. | 482/482 assertions passed (441 S1-S32/S35/S36 baseline all re-confirmed clean + 41 new S33/S34 checks; zero regressions); zero console/page errors, zero dialogs, zero non-`file://` requests; reproduced twice (`s33-s34-run1.log` + `s33-s34-run2.log`, both 482/482) | 482/482 | **Current canonical figure — cite this row (482/482, 0 open defects).** Arithmetic shown inline, matching the script's own printed Part-subtotal markers exactly: 441 (Parts 1-23, the S35+S36 row) + 28 (Part 24 S33) + 13 (Part 25 S34) = 482. **The baseline stayed 441 (purely additive — NO decrease and NO mechanism retired; with no override stored every icon is byte-identical to today, so Parts 1-23 re-run verbatim).** Part 24 covers `test-plans/S33-icon-registry-persistence.md` (S33.1-S33.13): registry order/size + defaults byte-identical to today's glyphs (R-DEFAULT across ALL 10 draw-sites — the 3 static index.html glyphs, the JS row icons on BOTH rows, the aisle overlay, and the auto-detect `<option>`); R-OVERRIDE + R-EVERYWHERE (a DISTINCT sentinel per action lands at its own site on every row, live, no reload); R-ISOLATION (delete-item vs settings-close, and auto-aisle-run vs auto-detect-option, never cross-set despite sharing a glyph); R-NOTHROW (iconFor(unknown)→U+25A1 square fallback, never throws); persistence across reload (key holds all 10, re-applied everywhere); the R17b content-constraint + R1 corrupt-store battery — literal null / malformed JSON / bare-string / number / top-level array [M31] / absent / non-string / over-length (>8 units) / control-char (U+0007) / whitespace-only / unknown-id / MIXED all drop to default with no throw and the list intact; and R17a — a markup-bearing override injects NOTHING (dropped by R17b before render; draw-sites also escapeHtml). Part 25 covers `test-plans/S34-icons-picker-panel.md` (S34.1-S34.10b): the Icons section renders BELOW Aisles; one row per action (label + current glyph previewed at the draw-site font-size, M34) + a Change control; Change opens the FROZEN candidate palette (exact glyphs + order + current-marked); M33 the palette/candidate boxes are NOT clipped (overflow visible — a tofu shows honestly on-device); R18 a candidate tap applies live + persists + keeps the palette open at the same scroll offset (proven with a DIRECT DOM click so the check isolates the re-render, not Playwright's auto-scroll); per-action Reset + global reset-all; N23 the non-emoji guard keys on emoji-PRESENTATION-default (text-default dingbats ✳/✷/✦/✧/★ pass, SPARKLES ✨ is caught, the U+2B4D tofu-confirm candidate is whitelisted); and a glyph swap preserves the structural Auto-detect option (`data-autodetect` + reserved value) and the settings-close `data-role` (still closes). **NB-6:** whether a chosen candidate GLYPH renders (tofu vs real) on the PO's iPhone is the PO's on-device acceptance signal — this pass verifies STRUCTURE / persistence / routing only, via desktop proxies (textContent equality, code-point pinning, structural survival, persistence round-trip). **INDEPENDENT NUL-byte re-verify (blocker check, task item 4):** a byte-level scan of `script.js` / `index.html` / `style.css` found 0 NUL and 0 stray control bytes (excl. TAB/LF/CR) and `node --check script.js` passes — the Dev's NUL-in-regex fix is confirmed repaired; no control byte in the shipped file. **Two test-authoring bugs found + fixed while writing this pass, NEITHER an app defect:** (1) the S34.6 scroll check first used `page.click()`, whose auto-scroll to the off-screen candidate moved the panel and made the pre-click scroll reading stale (the app correctly preserved the post-auto-scroll offset) — rewritten to a direct DOM `.click()` (now 100→100); (2) a literal U+0007 bell byte slipped into the test script's own control-char fixture line — replaced with the 6-char JSON escape and the test file re-scanned clean of control bytes. **S33 AND S34 are CLEAR for their Done-flip on the desktop-verifiable AC (482/482, 0 open defects) — the Done-flip ALSO waits on the PO's on-device NB-6 glyph pick (the Orchestrator dispatches the Dev to push for that pick on this clean pass).** Full transcripts: `c:/tmp/pw-test/s33-s34-run1.log`, `c:/tmp/pw-test/s33-s34-run2.log`. Per-story plans: `test-plans/S33-icon-registry-persistence.md`, `test-plans/S34-icons-picker-panel.md`. |

## Targeted closure checks — not part of the cumulative total

Not every script that runs against the live app is a regression re-run. These targeted layout
measurements are deliberately NOT folded into the running cumulative total (now 482, per the 2026-09-16 S33+S34
icon-picker row above) — they don't re-verify any story's own AC end-to-end the way every ledger row above does,
so folding their counts in would make the ledger's "sum the parts" arithmetic unverifiable.

**Current (definitive) worst-case-row measurement — 2026-09-11/14, post-S26+S27 (sha `7f6a327`, re-confirmed clean against the FINAL `afcd725`):**
`c:\tmp\pw-test\vopping-worst-case-row-s26-s27-closure.js` (9/9 passed, zero console/page errors).
Real DOM measurement + screenshots at 320/360/375/390px, R7's method / QA finding M25. The 6-control
worst case is now the both-fields-empty Manual-sort row: 5 primary-line `.icon-btn`s (note-toggle +
S15 edit + S19 Up + S19 Down + delete) + the S26 primary-line `.aisle-control` (a collapsed `<select>`,
NOT an `.icon-btn`, so its footprint is MEASURED per M25, not assumed at icon-btn parity). Result:
**the collapsed `.aisle-control` MEASURES 19.5×19.5px at all four widths; the 6-control row fits on a
single line — zero horizontal overflow, row height 36.17px, item-name space 117px@320px → 187px@390px.**
Screenshots `c:/tmp/pw-test/vop-worstcase-s26s27-6ctrl-{320,360,375,390}px.png`. **This SUPERSEDES the
pre-S22 `vopping-worst-case-row-6icon-closure.js` below** (which measured the removed free-text
aisle-toggle BUTTON, not the S22/S26 `<select>`).

**Superseded (2026-09-09, pre-S22 free-text aisle-toggle):** `c:\tmp\pw-test\vopping-worst-case-row-6icon-closure.js` (10/10 passed, zero console/page errors).
Real DOM measurement + screenshots at 320/360/375/390px, R7's method. **Axis B (both fields empty,
Manual sort) is now the MAX-crowding case: exactly 6 primary-line icons (note-toggle + aisle-toggle +
S15 edit + S19 Up + S19 Down + delete).** Result: **the 6-icon row FITS CLEANLY on a single line at
all four widths — zero horizontal overflow, no wrapping, row height 36.17px unchanged; item-name space
runs 117px @320px → 187px @390px** (the PO's "~20 characters even with Up/Down + edit" estimate holds).
Axis A (both note+aisle set) drops to 4 primary-line icons with content on a 66.61px second line, also
zero overflow at all widths. Screenshots: `c:/tmp/pw-test/vop-worstcase-6icon-{axisB-empty,axisA-set}-{320,360,375,390}px.png`;
transcript `c:/tmp/pw-test/worstcase-6icon-closure.log`. **This SUPERSEDES the earlier
`vopping-worst-case-row-s13-s14-closure.js` (17/17) evidence** below, which measured the pre-S15,
pre-S19 row (1 icon on Axis A, 3 on Axis B) — the definitive re-measurement Scrum Master deferred for
the S7/S8 crowded-row closure, now discharged here at the true post-S19/S20 6-icon worst case.

**Superseded (2026-09-09, pre-S15/S19):** `c:\tmp\pw-test\vopping-worst-case-row-s13-s14-closure.js`
ran a one-off check (17/17 passed) closing S7/S8's Tracked follow-up #2 gate (QA finding R7) against
the then-current S13/S14 row (both note+aisle set = 1 icon; both empty = 3 icons). Its icon counts
went stale when S15 added its edit icon and S19 restored Up/Down — re-measured definitively above.
Full historical evidence: `test-plans/S7-notes.md`'s "Worst-case-row closure evidence, 2026-09-09"
section (cross-referenced by `S8-aisle-designation.md`).

## How to add a row

1. Run the formal-pass script for the story (or the combined regression
   re-run script covering multiple prior stories).
2. Record: date, story ID(s) covered, exact script filename (full path
   if it lives outside this repo, e.g. `c:\tmp\pw-test\vopping-tests-...js`),
   raw pass/fail count for *that run*, and the new running cumulative
   total.
3. If the cumulative total requires arithmetic across multiple scripts
   (e.g. summing several stories' own counts), show the addition inline
   in the Notes column so it's checkable, not just asserted — vacking's
   R10 finding was exactly an uncaught arithmetic slip that propagated
   into 8 separate downstream citations because nobody could re-derive
   it from the ledger row itself.
4. When citing this total anywhere else (chat, TEAM_LOG.md, a story's
   own Results table), name this file and the row's date/script — e.g.
   "193/193 per REGRESSION_LOG.md, 2026-09-10 row, vopping-tests-s1-s3-formal.js."
